# Round 005: the-ledger-prints-api-time

- Goal: cost-per-round
- Started: 2026-09-25T11:41+07:00  Ended: 2026-09-25T11:58+07:00
- Sessions: 2E032574-72B6-473D-9CA1-6E49F50E7BAB (claude-sonnet-5)   Archives: 2E032574-72B6-473D-9CA1-6E49F50E7BAB
- Base: eb7e2fb   Result: 19485bc on `cost-per-round/round-5`, PR #160
- Cost: $2.89 · 9426k in (98% cached) · 34k out · 0h 14m

## Prompt

The text ledger of `kitterm goal cost` prints the bill's
`totalAPIDuration` in an `api` column beside `wall`, per round and in the
goal's total; a dash where a round has no API time; `--json` unchanged.

## Floor

before: green (eb7e2fb: `KittermCLITests` 124; swift test 886 on the
same `Sources/` tree in `daemon-last-words` round 5)
after: green (9 new; swift test 895, run by the crew and again by the
foreman; `KittermCLITests` 133; the Linux docker pipe, `Build complete`,
run by the crew; no daemon change, so no bench and no web floor)

## Effects

- behaviour: `GoalLedger` gives each row `apiDurationMs`, the sum of its
  bills' `totalAPIDuration` when every bill in the row came from a
  transcript, else nil, the gate `--json` already uses. The table prints
  it with the `wall` format in a six-wide `api` column between `wall`
  and `tests`; the goal's total sums the rows that have it and prints a
  dash when none has.
- visible: the real ledger, run by the foreman on this branch:
  `002 the-daemon-refuses-the-combination 11.94 ... 16h17 12m17` for
  `proxy-is-a-boundary`, the overnight round the proposal named, and
  `001 the-wrapper-stops-taking-sigpipe 4.25 ... 14m40 7m21` for
  `green-ci-again`. `GoalLedgerAPITimeTests`, 9 cases.
- persistent: `Sources/KittermCLI/GoalLedger.swift`,
  `Tests/KittermCLITests/GoalLedgerAPITimeTests.swift` (new),
  `Tests/KittermCLITests/GoalCostTests.swift` (Chartered, below),
  `AGENTS.md` (the ledger's columns).
- Chartered: four assertions in `GoalCostTests.swift` pinned the table's
  layout, which the queue item requires to gain a column. Each now
  carries `api` in its header and an `api` cell in each row and total;
  every earlier cell is unchanged byte for byte.
  `testTheTableOfTheCorpusFixture` (`4m05`, `0m00`, a dash, total
  `4m05`); `testTheCostLineWhenTheArchiveNamesNoTranscript` (dashes, a
  line-sourced and an unbilled round);
  `testAShortShaWithNoDigitIsStillASha` (a dash, a round before the
  bill); `testCostUsageRefusals` (the header prefix). The item is the
  `STATE.md` queue row, from the proposal of round 4.

## Gap

class: none
evidence: the floor is green; the only existing test change is the four
Chartered assertions, each extended and none weakened.

## Decision

done

## Reflection

A round that mixes a transcript bill and a `Cost:` line prints no API
time, because a partial sum would read as the whole. The table in
`corpus/01-what-did-that-goal-cost.md` now lacks the `api` column; the
corpus is Frozen, so the human re-aligns it, as after round 4.
