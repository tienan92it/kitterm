# Round 010: the-page-is-the-pen-design

- Goal: agent-dashboard
- Started: 2026-09-18T17:28+07:00  Ended: 2026-09-18T18:35+07:00
- Sessions: 16440079-B7C1-4486-B1A3-352B0CABE12B   Archives: 16440079-B7C1-4486-B1A3-352B0CABE12B
- Base: 912c783 (main)   Result: 9f314dd, 9041756, 164ac97, b66cad2 on `agent-dashboard/round-10`, PR #134
- Cost: $38.45 · 41602k in (98% cached) · 292k out · 1h 04m

## Prompt

The human's. They reviewed the shipped page against the two frames in
`corpus/dashboard.pen` and said the code is wrong: the page follows the
frames exactly, and the one change to the frames is `MODELS` as the top
three by cost plus `Others`. The foreman exported the frames unchanged to
`corpus/04-pen-1200.png` and `04-pen-390.png`, read the differences off
them, and listed every one in the prompt: ten at 1200 (the band, USAGE,
QUOTA, VALUE's wording and rule, WHERE's summary and six columns, LEAKS
on two lines, the flat tree with a mark column and right-aligned fact
columns, the folds line) and eight at 390 (the brand line and the 2×2
counts, USAGE without a label, VALUE unfolded, WHERE and LEAKS absent,
MODELS unfolded, no vocabulary header, the same lines at 44 px, the
stacked folds). Where the frames and the corpus disagree, the frames
win.

One correction: `Others` sorted by spend into third place instead of
last; a `VALUE` label at 390 the frame does not draw; and the folds line
in the shot carried no `▸ Archived (N)`.

## Floor

before: green (912c783: swift test exit 0, vitest 1384 in 50 files)
after: green (Linux swift:6.1 build complete 71 s; swift test 827;
tsc clean; vite build ok; vitest 1411 in 52 files, 27 added, none
removed)

## Effects

- behaviour: the page reproduces the two frames. `RepositoryYield`
  lists each merged PR with its lines so WHERE can say `9 PRs` and
  `$0.014 a line` per goal. `sessions-tree.ts` is new: the flat tree,
  one line per level, mark column at the page edge, indent per level,
  four right-aligned fact columns, actions in the last cell. `Others`
  is always the last MODELS row.
- visible: the crew's `after-1200.png` and `after-390.png` beside the
  frames; `sessions-pen-panels.test.ts` and `sessions-pen-tree.test.ts`
  pin the wording and the column order of every panel and every line
  level; a `sessions-value-page.test.ts` case pins the MODELS row order
  at both widths.
- persistent: `Sources/KittermDaemon/RepositoryYield.swift`,
  `Tests/KittermDaemonTests/RepositoryYieldPullRequestTests.swift`
  (new), `Web/terminal/src/sessions.ts`, `sessions.css`,
  `sessions-model.ts`, `sessions-value.ts`, `sessions-tree.ts` (new),
  `theme-contrast-derive.ts`, seven test files, `AGENTS.md`.

## Chartered replacements, all named in the crew's note

The assertions that pinned the shipped shape, replaced with ones that
pin the frames: `sessions-band.test.ts` 4, `sessions-band-page.test.ts`
5, `sessions-css.test.ts` (task block → depth indent, WHERE+LEAKS drop,
band lines, column grid, no task fold), `sessions-line.test.ts` 5,
`sessions-value.test.ts` (rows, nouns, notes, summary),
`sessions-value-models.test.ts` 2 (`Others` last, was first when its
sum led), `sessions-value-page.test.ts` all. The ratchet
`theme-contrast.test.ts` is untouched: 23 pairs.

## Where the crew chose, the frames being silent

- A goal's counter (`r0/3`) sits in column four when the line has a
  cost, column two when it has none — the frame draws both.
- 390 keeps the state word and one fact on every line; the frame drops
  `[done]` on two lines.
- A done goal shows its two most recent done tasks at both widths; the
  older ones are hidden, not folded.
- Indent 16 px per level at 1200 and 12 px at 390; the frame draws 15
  and 29, which no token in the foundation produces.
- Panel labels are muted, not faint, because faint fails the ratchet on
  four themes.
- Coloured text (the unattributed WHERE row) is `.mark.wide`; the VALUE
  tile's rule is `.mark.bar.rule`. Both keep principle 5: colour is a
  mark, and a text rule that only marks stays inside the mark class.

## Heights

1200: 1953 px. 390: 2499 px on the real tree, against `goal.md`'s
condition of under 1200. The human's direction put the frames above the
condition; the number is now the human's to reset or strike.

## Gap

class: steering
evidence: the crew sorted `Others` by its sum because round 8's test
"a tail that sums past the leader" pinned that order, and the foreman
had accepted it. The human's words were "one row more". The correction
took two minutes; the two rounds it stood cost nothing but a wrong
picture.

## Decision

done. Every diff path is Free; every replaced assertion is chartered
and named; the floor is green on macOS and Linux.

## Reflection

The foreman replaced the human's frames with a tracing of the shipped
page, and the human closed Pen without saving. The frames are the
design. The foreman reads them, exports them, and hands the crew the
differences; it does not edit them. That is a fact for `facts.md`.

A debug daemon started for a screenshot overwrote `~/.kitterm/port`,
and this session's MCP bridge, which binds its port once, followed it.
A second fact: a daemon for a screenshot runs with its own state
directory, never the default.
