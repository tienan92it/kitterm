# Round 002: subscriptions

- Goal: agent-push
- Started: 2026-09-11T11:46+07:00  Ended: 2026-09-11T12:00+07:00
- Sessions: 9FC478C4-3B52-4984-9DEB-991B7BBB667D   Archives: the same
- Base: 019d5f1 (main)   Result: c005f49 on goals/subscriptions

## Prompt

Capability 2 from `plan.md`, sent as one `send_input` of 5751 bytes. It
named what round 1 settled so the crew would assume rather than
re-decide it: the origin carries no port, so re-subscription is not this
capability's problem but persistence is. It asked the crew to decide and
defend two things, the `410 Gone` owner and whether the file identifies a
session, and told it not to write the sender or the toggle.

## Floor

before: green (`swift test` 648 tests, vitest 1333 in 32 files)

after: green. The foreman re-ran the Swift suite in the crew's worktree
on `c005f49`: **668 tests, 0 failures**, 20 of them new. Web green: tsc
clean, vite build, vitest 1333. `KittermBench interactive-echo` against a
scratch daemon on a free port: p95 2.56 ms and 2.64 ms over two runs,
against a 50 ms gate. `plan.md` makes the bench a real gate for this
goal because it touches the event path; these routes touch no output
path and the store writes on its own queue.

## Effects

- **Two routes, and the grade gate is the first statement.**
  `servePushSubscription` opens with `guard grade == .full else { 403 }`,
  which the foreman read. A watch token is refused on both `POST` and
  `DELETE`. The answers are 201 stored, 200 updated, 400 on four bad body
  shapes, 403 on a watch or absent token, 404 on an unknown endpoint for
  `DELETE`, and 503 over a cap of 32.
- **The file follows `respawn.json` rather than inventing a style.**
  `~/.kitterm/push.json`, mode `0600`, `{"version":1,"subscriptions":[…]}`,
  loaded once at start, whole-file atomic rewrite per change, unknown
  version reads as empty, unknown field decodes away. The foreman saw the
  tolerant read exercised: the suite logs
  `ignoring …/push.json: DecodingError.dataCorrupted` and continues.
- **A duplicate endpoint updates rather than rejects**, and the crew
  defended it: the page cannot know whether the daemon still holds its
  subscription, so it posts on every load, and a rejection would turn an
  ordinary re-post into an error. The browser's latest keys are the true
  ones. The first `createdAt` survives.
- **The `410 Gone` owner is capability 3**, because capability 3 is what
  sees the answer. It calls the same
  `PushSubscriptionStore.remove(endpoint:)` that `DELETE` uses and
  iterates `store.all`, so this file's contract does not change later.
- **The file identifies no session, project, token or VAPID key.** A
  subscription names a browser; the notification names a session at send
  time. That keeps the corpus request satisfiable without putting session
  identity on disk.
- visible: 20 tests. `PushSubscriptionRouteTests` covers the grades, the
  duplicate, the removal, the 400s and the file mode, and drives real
  processes for the two cases that matter: `SIGTERM` then restart, and
  `POST /api/upgrade/takeover`. Both answer the re-post `created: false`,
  which is the property round 1's origin decision depends on.
  `PushSubscriptionStoreTests` pins the shape, the version guard and the
  cap.
- persistent: seven files. One new source, two new test files, three
  touched sources, and `docs/architecture.md`.

## Gap

class: none

evidence: the persistence claim is proved against real processes on both
paths a daemon can change identity, and the grade gate was read in the
source rather than inferred from a passing test.

## Decision

done. Capability 2 is complete on `goals/subscriptions` at `c005f49`.

propose (three, none blocking: `AGENTS.md`'s HTTP API list does not name
the two new routes, which is outside this round's authority. Capability 3
needs a VAPID key pair, which belongs in its own `0600` file rather than
in `push.json`. Capability 4 may want `GET /api/push/subscriptions` for
the toggle's state; `plan.md` row 2 does not ask for one, and the crew
did not add it.)

## Reflection

The prompt spent its first paragraph on what round 1 had already settled,
and the crew spent none of its round re-deciding the transport. It went
straight to the two questions that were genuinely open and answered both
with a reason rather than a preference.

The persistence tests are the round's real value. The origin decision
from round 1 is only worth anything if the daemon still holds the
subscription after it restarts, and that is a claim about two different
code paths, a restart and a takeover. The crew tested both with real
processes instead of asserting the file round-trips.
