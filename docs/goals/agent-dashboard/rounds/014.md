# Round 014: four-defects-from-review

- Goal: agent-dashboard
- Started: 2026-09-20T14:21+07:00  Ended: 2026-09-20T14:45+07:00
- Sessions: C0C0B295-B914-4C89-BF72-4FE257EA2ABF   Archives: C0C0B295-B914-4C89-BF72-4FE257EA2ABF
- Base: 941a9c9 (agent-dashboard/round-10, PR #134)   Result: ef0e941 on the same branch
- Cost: $12.85 · 18676k in (99% cached) · 58k out · 0h 21m

## Prompt

The human's four findings on the built page, each with the foreman's
diagnosis: a quota window past its reset read `resets 1d 17h ago` (the
scratch daemon's reading was a stale copy; the wording was the bug);
the working mark rested on `●` and tried braille where the design
draws `◐ ◓ ◑ ◒`; the fold rows' triangle sat off the row baseline (a
`<summary>` with its own layout); a done goal inside `N done` wore a
triangle that opened nothing.

## Floor

before: green (941a9c9: vitest 1438 in 55 files)
after: green (tsc clean; vite build to scratch; vitest 1436 in 55
files: 8 spinner cases out, 6 in; no Sources/ change)

## Effects

- behaviour: a past window keeps its percentage and fill as
  `.mark.idle` (faint) and its reset cell reads `reset · read 1d 19h
  ago` from `ageSeconds`. `spinner.ts` holds one cycle, the quadrant,
  resting on `◐`; `pickCycle` and the canvas measure are gone, so
  nothing can fall back to `>`. `.mark.disclosure` is inline-flex and
  centred; the summary keeps the row line. A done goal inside the fold
  carries all its tasks, closed by default; a goal with no tasks wears
  no mark.
- visible: Chromium samples of the working mark at 0/100/200/300 ms:
  `◐ ◓ ◑ ◒` at 1200 and at 390; `◐ ◐ ◐ ◐` under reduced motion.
  Triangle centre minus name centre 0.00 px on every fold row and goal
  row at both widths. `round14-1200.png` and `round14-390.png` in the
  crew's scratchpad, read by the foreman: the fold open with one goal
  inside opened to its four tasks, the others closed, one with no
  tasks unmarked.
- persistent: `Web/terminal/src/sessions-model.ts`, `sessions-tree.ts`,
  `sessions.ts`, `sessions.css`, `spinner.ts`, `fake-page.ts`, six test
  files, `AGENTS.md`, `docs/architecture.md`.

## Chartered replacements

The braille and block cycles and the `●` rest in `spinner.test.ts` and
`sessions-line.test.ts`; the summary's own layout in
`sessions-css.test.ts`; the fold's goal line in
`sessions-pen-tree.test.ts`. Named in the note.

## Gap

class: none
evidence: the note's measurements, the floor and the shots agree.

## Decision

done.

## Reflection

The foreman copied `usage-limits.json` into the scratch daemon's
state directory two days ago, so the human reviewed a stale reading
and spent a question on it. A scratch daemon for review copies the
data files fresh at each rebuild, or not at all.
