# Round 001: templates-and-cli

- Goal: goal-folders
- Started: 2026-09-09T11:15+07:00  Ended: 2026-09-09T11:40+07:00
- Sessions: 7FA3974A-0FEF-4252-8100-F450DB904875   Archives: 7FA3974A-0FEF-4252-8100-F450DB904875
- Base: 2fd99bc (goals/goal-folders)   Result: ff453ce, 20ba55c, ac58bbe, 8d59e4b on the same branch

## Prompt

Capability 1 from `plan.md`, sent as one `send_input` of 5086 bytes: read
`LOOP.md` for the layout, this goal's files, corpus request 04, the
round 3 record of the previous goal, and the template and CLI sources;
stay on the branch; the seven pieces (the split templates, both sets
embedded with the golden test, `project init` writes the two project
files, `goal new` and `goal list`, tests with one shared fixture, the
skipped repository fixture re-pointed, AGENTS.md); the tiers; the floor
with the Linux build; a hand check on a temp checkout.

## Floor

before: green (`swift test` 588 tests, one skipped, vitest 614, bench p95
3.40 ms). The skipped test read this repository's package at the path the
layout change moved.
after: green (`swift test` 598 tests 0 failures, vitest 614, bench p95
2.46 ms, Linux build ok; the foreman re-ran `swift test` and vitest with
the same counts).

## Effects

- behaviour: the crew split `examples/goals/` into `LOOP.md` and
  `facts.md` plus `goal/` with five entries, embedded both sets, wrote
  `GoalCommand.swift` with `new` and `list`, made `project init` write
  the two project files, re-pointed the repository fixture, and described
  the layout in AGENTS.md. Four commits, 25 minutes, no correction.
- visible: `kitterm goal list` on this repository prints
  `goal-folders	active` and `projects-and-knowledge	done`.
- persistent: sixteen files changed. Tests: the golden test pins both
  sets and the `# STATE: <slug>` heading; `GoalCommandTests` (9): `new`
  writes five entries with the slug, refuses an existing folder and a bad
  slug, `list` order with a skipped folder, `list` on this repository;
  the init tests pin two files; `CLIFixture.files(under:)` is shared by
  three test files. The foreman ran the built binary on a temp checkout:
  `project init` wrote two files and registered; `goal new demo` wrote
  five entries with `# STATE: demo`; a second run refused with nothing
  written; `goal list` printed `demo	active`.
- tier note: the crew changed four pinned assertions that encoded the old
  seven-file `project init` (`testPackageShape` and three init tests) so
  they pin two files. Existing test files are Frozen by the letter of
  `LOOP.md`, but the pins encoded the layout `goal.md` replaced on the
  human's direction; the foreman accepts the change and the crew's
  proposal below narrows the tier.

## Gap

class: harness
evidence: the pinned layout in four tests; a layout change on the human's
direction must edit them, and the tier does not say so.

## Decision

done. Capability 1 is complete on `goals/goal-folders` at `8d59e4b`. The
session is archived.

propose (`LOOP.md` Authority: a test that pins a layout `goal.md`
replaces is Propose, not Frozen; the crew names it in the note and the
foreman accepts or refuses in the record.)

## Reflection

The first round on the per-goal layout. One skipped test was the only
sign the layout had moved under the daemon; round 2 makes the daemon
read every goal folder. `goal new` replaces every `<slug>` in `STATE.md`,
which the template relies on; a template change that adds a second
placeholder must keep that rule.
