# Round 001: three-levels-no-filters

- Goal: workspace-ledger
- Started: 2026-09-16T14:05+07:00  Ended: 2026-09-16T14:51+07:00
- Sessions: 9FD009BD-F22C-4A4D-8AB9-4D06A8EA12F5   Archives: the same
- Base: e586688 (goals/dashboard-and-usage)   Result: 259e38e on goals/three-levels

## Prompt

Capability 1 from `plan.md`, sent as one `send_input` of 6469 bytes. It
carried the human's words, named the three buckets and why "working"
reads from a live label rather than the status word, listed every rule
the `fleet-catch-up` rounds paid for, and asked the crew to shape the
heading model so capability 5 can hang a number on it without a rewrite.

## Floor

before: green (vitest 1222 in 36 files)

after: green. The foreman re-ran the web floor on `259e38e`: tsc clean,
**1233 tests in 37 files, 0 failures**. Nine filter cases retired with
the feature, nineteen new level cases, one new ratchet pair. Round 4 of
`fleet-catch-up`'s four CSS absences still pass, 5 of 5. No Swift.

## Effects

- **Three levels, and the degenerate case handled rather than hidden.**
  `levels(...)` returns a workspace section per parent directory, each
  holding project sections, each holding its rows and its goals in three
  buckets. **A heading appears only over two or more projects**, so
  `NgheNhanTrading` gets one and `kitterm` sits at the top level, because
  a heading reading "Workspace" over `kitterm` alone says nothing. The
  name is the directory's own, which the crew calls the one name the
  human gave it.
- **Working reads from a live label, not the status word.** A goal is
  working when a session carries its `goal:` label, and `done` wins over
  a lingering label. That is the distinction the prompt asked for and the
  fixture exercises: `kitterm` has an `active` goal with no crew, which
  shows under pending.
- **Two absences, two remedies, two lines.** A registered project with an
  empty package prints "no goal folder"; a discovered checkout prints
  "not registered", because its knowledge route answers 404. The crew's
  reasoning is right: they are different problems and the reader fixes
  them with different commands.
- **A loose shell goes to the workspace whose directory holds its cwd.**
  The foreman for `NgheNhanTrading` runs in that directory, and the crew
  put it above that workspace's projects because that is where a reader
  looks for it. Anything outside every workspace falls to a last "No
  project" section that appears only when it has a row.
- **The filters are gone, feature and all.** The foreman checked each
  symbol: `Filter`, `Kind`, `currentFilter`, `applyFilter`, `chipGroups`,
  `CHOICE_KEY` and `isNarrowed` are absent from both files, and neither
  the search placeholder nor "Clear filters" is in the DOM. `group()`
  stayed, as the plan said.
- **The cost slot is shaped and empty.** `Heading` is `{name, path}`;
  capability 5 adds cost and cache share beside the name, keyed by the
  workspace directory or the project root, and a goal's number sits on
  `GoalLine` beside its round. Nothing was added now, because there is no
  source yet.
- visible: four images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/wl-round1/`. **The foreman
  read the 390 px after-image.** Heights: 390 px 1400 to **1159**;
  1200 px 1179 to 1118. Heads are 44 px, no horizontal scroll, and the
  phone menu's right edge sits at 366 of 390.
- persistent: five files under `Web/terminal/src/`, and the extended stub
  at `wl-round1/stub.mjs`, which now carries two workspaces, a crew on a
  working goal, an active goal with no crew, and an unregistered
  checkout.

## Gap

class: none

evidence: nineteen cases over the grouping model cover every shape the
plan named, the filter feature was checked symbol by symbol rather than
by eye, and the heights are measured in the page.

## Decision

done. Capability 1 is complete on `goals/three-levels` at `259e38e`.

propose (`AGENTS.md`'s fleet-view paragraph still describes the filters
and the tools row. Capability 5 rewrites that paragraph when the page
stops changing shape; doing it now would only need doing again.)

## Reflection

The crew's two decisions were the ones the prompt could not make for it,
and both answers are better than the question. A loose shell belongs to
the workspace whose directory holds it, because that is where the
foreman for that workspace runs. And an empty package and an
unregistered checkout are different absences with different remedies, so
they get different words rather than one shared "no goals".

One thing it saw and left, which capability 5 should take: at 390 px the
goal line cuts its next action to "Roun…", because the title, the round
and the next action share one line. Adding a cost to that line without
fixing it first would make it worse.
