# Round 015: cost-column-and-pr-links

- Goal: agent-dashboard
- Started: 2026-09-20T14:55+07:00  Ended: 2026-09-20T15:30+07:00
- Sessions: 6772F01F-87BE-4328-BE32-3A6369459333   Archives: 6772F01F-87BE-4328-BE32-3A6369459333
- Base: 808e13d (agent-dashboard/round-10, PR #134)   Result: 23a30a4 on the same branch
- Cost: $16.42 · 23922k in (99% cached) · 76k out · 0h 30m

## Prompt

The human's: the tree's number column mixed `$75.11`, `round 6` and
`r0/3`; make it the cost at every level, and make `PR #N` a link to
the pull request. The foreman synced the Pen frames first.

## Floor

before: green (808e13d: vitest 1436 in 55 files)
after: green (swift test 831; Linux docker build complete; tsc clean;
vite build to scratch; vitest 1441 in 56 files, 5 added)

## Effects

- behaviour: column two is the cost in the range at every level — a
  goal's `roundsInRange` sum, a task's round `Cost:` line, a session's
  transcript bill (`GET /api/sessions/<id>/cost`, every 30 s, counted
  when its day is in range; a running session has none) — a dash with
  none; column three holds a task's PR and a session's model. `round
  N` and `rN/M` are the name's tooltip. `PR #N` is an `<a>` in the
  accent with an underline, on task lines and WHERE task rows, whose
  href is `pullRequestBase` + N; `GET /api/projects` carries the base
  from `git remote get-url origin` (`PullRequestBase.swift`, SSH and
  HTTPS forms, none for a non-GitHub remote).
- visible: `round15-1200.png`, read by the foreman: every number in
  column two is a dollar amount or a dash; `PR #134` underlined in the
  accent. `PullRequestBaseTests` (4), `sessions-pr-links.test.ts` (4).
- persistent: `Sources/KittermDaemon/PullRequestBase.swift` (new),
  `HTTPAPIHandler.swift`, `DaemonServer.swift`,
  `Tests/KittermDaemonTests/PullRequestBaseTests.swift` (new),
  `Web/terminal/src/sessions-tree.ts`, `sessions-model.ts`,
  `sessions-value.ts`, `sessions.ts`, `sessions.css`, `AGENTS.md`,
  `docs/architecture.md`.

## Chartered replacements

The `round N` and `rN/M` column assertions in `sessions-pen-tree` and
`sessions-line`; the plain `PR #N` text in `sessions-where-columns`.
Named in the note.

## Gap

class: none
evidence: the floor is green on both platforms; the shot shows the
column and the link; every diff path is Free.

## Decision

done.

## Reflection

Nothing cost time. The Pen sync before the round took two minutes and
gave the crew one picture to match.
