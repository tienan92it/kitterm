# Round 005: review-fixes

- Goal: projects-and-knowledge
- Started: 2026-09-08T17:13+07:00  Ended: 2026-09-08T17:52+07:00
- Sessions: CF9A578D-EB88-438C-AD4C-5CA3D85EC004   Archives: CF9A578D-EB88-438C-AD4C-5CA3D85EC004
- Review sessions, archived: 9D4E1BBC (security), 253AA2C5 (daemon), BD047352 (reuse), 71E79508 (a11y)
- Base: fc27370 (goals/goal-loop-skill)   Result: 4136163 to f772b56, thirteen commits on goals/review-fixes

## Prompt

The review gate the human set on 2026-09-08 before any push: four
reviewers, one dimension each, static and read-only through git, on
`main...goals/scaffold-and-docs`, alongside round 4 under the
three-session cap. The foreman deduped the four reports into a ranked
list (R0 blocking, R1 to R10 required, nits optional, six items left with
reasons) and sent it as one `send_input` of 3334 bytes: one commit per
finding, a test for every fix a test can hold, hand verification of R0
and R4 at 390 px with screenshots.

## Floor

before: green (`swift test` 553 tests 0 failures, vitest 504, bench p95
2.52 ms)
after: green (`swift test` 558 tests 0 failures, vitest 559 passed in 31
files, bench p95 2.49 ms; the foreman re-ran `swift test` and vitest with
the same counts)

## Effects

- behaviour: the crew applied R0 to R10 as eleven commits named for the
  finding, then two nit commits, in 39 minutes with no correction.
- visible: `round5-menu-390.png` (the row menu escapes the card, Kill
  reachable on a single-row card) and `round5-chip-focus-390.png` (Space
  on a chip keeps focus on it), in the job's `round5/` directory.
- persistent: 24 files changed against `goals/goal-loop-skill`, 849
  insertions and 459 deletions. Tests added: the event loop never calls
  into `ProjectStore` (9 loop calls before, 0 after), the resolution
  cache hit, the button name builders, the per-theme WCAG contrast test
  with a known-below table, the `gitdir:` target, the symlink refusal,
  the 0600 and 0700 modes. `ProjectInitTests.swift` folded into
  `ProjectCommandTests.swift` with its assertions intact.
- deviations the crew stated: R1 moves the poll resolution to a serial
  queue rather than deleting it, because a Frozen cwd-poll test needs it;
  R2 changed the pinned payload fields only, as the prompt allowed; R6's
  class-to-token mapping was checked by computed style, not vitest.
- left: daemon nits 5, 6, 8 (a hash-based id breaks a pinned `app-2`
  assertion), reuse 7, a11y 23, plus the six "Leave" items.

## Gap

class: none for the fixes. The review itself found the largest gaps of
rounds 1 and 2 after the fact: file I/O on the event loop (world rule),
and a row menu that the phone screenshot did not show open (surface).

## Decision

done. The stack is ready for the push the human asked for. The session
is archived.

propose (`tokens.css`: raise the `--ui-text-muted` mix; under 4.5:1 on
10 of 16 themes. Outside the goal's diff.)

propose (`plan.md` capability 1: the row still names counts on
`GET /api/projects`; the route now returns identity only.)

## Reflection

Two rules for the loop. First, a review gate before the push found one
blocking and twenty-two should-fix items that five green floors did not,
because the floor holds earned behaviour and the reviews judge against
rules the floor does not encode; a per-theme contrast test and a
loop-call counter now encode two of them. Second, the foreman committed
the state update to the base branch after the round 5 prompt went out;
the crew happened to branch later, so the stack held, but the order is a
rule: commit to the base, then send the prompt. Both go to `LOOP.md` as
proposals.
