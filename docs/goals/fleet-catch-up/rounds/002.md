# Round 002: each-fact-once

- Goal: fleet-catch-up
- Started: 2026-09-15T16:47+07:00  Ended: 2026-09-15T17:06+07:00
- Sessions: AD77F6F8-A8C0-44C0-ADC4-96067C4C5C87   Archives: the same
- Base: f332f88 (goals/a-row-is-one-line)   Result: 508c8df on goals/each-fact-once

## Prompt

Capability 2 from `plan.md`, sent as one `send_input` of 4805 bytes,
with the inventory's "says the same thing twice" section as the
worklist. It asked the crew to decide what a card's count says when its
attention sessions have moved to the strip.

**One correction inside the round.** The crew removed the proposals chip
as the plan said, and reported that `STATE.md`'s proposals count then
printed nowhere, because the strip only read a round record whose
decision starts with `propose`. The foreman ruled that once is not zero:
`STATE.md` is the foreman's source of truth and often carries proposals
no decision line does, `foreman-harness` round 4 being one. The strip now
reads the count.

## Floor

before: green (vitest 1321 in 34 files)

after: green. The foreman re-ran the web floor on `508c8df`: tsc clean,
**1321 tests in 34 files, 0 failures**. Ten chartered cases went with
the functions the plan removed; eleven new cases replaced them. The
ratchet is green and `KNOWN_BELOW` is untouched.

## Effects

- **A session in the strip is not a row.** `cardRows(rows, items)` drops
  every strip session from the cards; `stripIds` names them. The four
  attention sessions in the fixture appear once each at the top and
  nowhere else.
- **The card counts only the rows it lists**, and the crew's reason
  holds: the strip names the project on every item, so a count of strip
  sessions on the card is the same fact as a number, and a reader who
  sums cards against the strip counts four sessions twice. A tally
  answers what is still running; a session that waits or failed is not
  running. A project whose every session is in the strip prints no
  count; "no live session" prints only when the project owns none.
- **The proposals count reads from `STATE.md`.** One strip item per goal
  that has any, with the count, the goal title, the project, and "Open
  record NNN" or "Open STATE.md" when no record exists. The record's
  decision line is the item's text only when it starts with `propose`. A
  goal with `proposals: 3` and a decision of `done` shows once at the
  top; a goal whose count is zero shows nothing whatever the decision
  says. The card's record link hides while the strip carries it.
- **The `goal:` and `crew:` sub-headers are gone**, the slug prints once
  beside the title, and `.crew-head`, `.tag` and `.tag.proposals` left
  the CSS.
- **The `needs input` and `failed` filter chips are gone on purpose.**
  Those sessions are in the strip, unfiltered, where a returning reader
  looks. The crew said so in its note so the next round does not restore
  them.
- visible: five images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/fcu-round2/`. **The foreman
  read `fcu-r2-after-390-proposal.png`**: the strip reads "3 proposals ·
  the foreman's own tools stop lying to it · kitterm · Open record 005",
  the four attention sessions appear once, and "No project" reads
  "1 idle". Heights at 390 px: 2448 to 2196 on the corpus fixture, from
  2724 before round 1.
- persistent: five files under `Web/terminal/src/`. Round 1's stub server
  was not on disk, so this round wrote `stub.mjs` in its scratchpad; it
  serves `dist/` and six JSON routes on port 62102.

## Gap

class: steering

evidence: the plan said "choose the strip" for proposals and the strip's
source was narrower than the chip's, so following the plan lost a fact.
The crew caught it and reported it as a loss to weigh rather than
shipping it silently, which is why the correction cost nine minutes and
not a round. The plan row should have named the source, not just the
place.

## Decision

done. Capability 2 is complete on `goals/each-fact-once` at `508c8df`.

## Reflection

Two rounds in, the page has lost 528 px at phone width and gained
nothing, which is the goal. What made this round work was the crew's
last paragraph: "two things to weigh", naming a loss the plan had caused
and asking rather than deciding. A crew that reports what its brief made
it lose is worth more than one that follows the brief exactly.

One thing seen and left for round 3: the strip item headline is the
session's name, and when a session is named after its folder the project
tag beside it repeats the same word, as in "needs input NgheNhanTrading
NgheNhanTrading". Capability 3 reorders the strip and should print the
project only when it differs from the name.
