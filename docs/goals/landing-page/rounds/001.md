# Round 001: the-page-is-the-frames

- Goal: landing-page
- Started: 2026-09-20T16:33+07:00  Ended: 2026-09-20T16:50+07:00
- Sessions: 3D7FFC5A-DCAC-4BB6-96C2-A2E762429750   Archives: 3D7FFC5A-DCAC-4BB6-96C2-A2E762429750
- Base: 539fca2 (origin/main + the goal package)   Result: f311485 on `landing-page/round-1`, PR #135
- Cost: $10.81 · 13959k in (99% cached) · 75k out · 0h 21m

## Prompt

Capability 1: `site/index.html` reproduces `Landing 1200` and `Landing
390` — the band, the centred hero, the six snapping pages with their
specimens, the footer — keeping today's copy control and the
`Open my terminal` probe. Motion is round 2; the DOM carries
`data-page` and `data-enter` now.

## Floor

before: green (the old page validates and renders)
after: green (html-validate 0 errors; headless Chromium at 1200×800
and 390×760 with no console error and no horizontal scroll)

## Effects

- behaviour: one file, inline CSS and one script, JetBrains Mono from
  Google Fonts with a system-mono fallback, the `.pen` tokens as
  custom properties. The port field is gone (`goal.md` excludes a
  form); the miss line stays. Snap is mandatory; the footer carries
  `scroll-snap-align: end` so the last page can reach it.
- visible: `landing-1200.png` and `landing-390.png` beside the
  exports, read by the foreman: every band within 2 px at 1200 and 4
  px at 390. Contrast: 16 pairs, all pass; lowest text pair the accent
  on the surface at 5.17:1; lowest mark 6.38:1. Copy: `[copy]` wrote
  the command to the clipboard and read `[copied]` for 1.6 s.
- persistent: `site/index.html`, `.htmlvalidate.json` (new).

## Where the crew chose

The 390 layout below 768 px; the `$` prompt in the accent at both
widths (the frame paints it white at 390); `[copy]` faint at 1200 and
accent at 390, as each frame draws it.

## Gap

class: none
evidence: the shots match; the floor is green; only `site/` and the
validator config changed.

## Decision

done.

## Reflection

Nothing cost time. Seventeen minutes from prompt to note.
