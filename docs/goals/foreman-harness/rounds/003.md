# Round 003: skill-drops-curl

- Goal: foreman-harness
- Started: 2026-09-10T16:05+07:00  Ended: 2026-09-10T16:40+07:00
- Sessions: 79463E11-E4D4-4807-BF80-B5DCA6E97381   Archives: the same
- Base: 9009fbe (main)   Result: 7d600c3 on goals/skill-drops-curl

## Prompt

Capability 2 from `plan.md`, sent as one `send_input` of 5140 bytes:
remove the workaround from the three skills, keep the rest of the
section, update the embedded copies and the docs, and check it against a
real dialog through the toolset alone. The prompt said which parts of
"Read before you type" were the product of earlier rounds and must not
change.

**The pane sat idle for twenty minutes first.** The foreman spawned it,
ran the floor, and then turned to another goal without sending the
prompt. Nothing was lost but the time. The rule is in the reflection.

## Floor

before: green (`swift test` 636 tests 0 failures, vitest 629, bench p95
2.48 ms)

after: green on all four gates. The foreman re-ran `swift test` in the
crew's worktree on `7d600c3`: 646 tests, 0 failures. Web green, bench p95
2.52 ms, the Linux build from a tar of `HEAD` in 69.67 s.

## Effects

- **The workaround is gone from all three skills**, and `grep -c curl`
  over them returns zero. The trust-dialog step now moves the mark with
  `send_input keys=["down"]`, reads the screen, and presses Enter with
  `send_input keys=["enter"]`, one keystroke per call. It says in one
  sentence why a key goes by name rather than as text, so the next reader
  does not try `text` again and rediscover what round 1 measured.
- **The rest of the section is intact**: the permission-dialog rule, the
  in-progress-turn rule, the cooked-reader recovery, and the five
  numbered steps, each the product of a round that paid for it.
- **The live check, through the toolset alone**, following the skill's
  own steps so the text tested itself. A fresh untrusted directory, a
  session running `claude`, and no `curl`, no shell, no `printf`:

  | call | what the pane showed |
  |---|---|
  | `read_screen` | the trust dialog, mark on `No, exit` |
  | `send_input keys=["down"]`, 3 bytes | mark on `Yes, I trust this folder`, no stray characters |
  | `send_input keys=["enter"]`, 1 byte | Claude's banner, empty prompt |

  Corpus request 01 satisfied end to end. The crew ended the session it
  spawned.
- persistent: seven files. `ForemanSkillsTests` gains three assertions on
  the shared section: no `curl`, and both calls present.

## The block, and the ruling

`swift test` came back red at 645 of 646.
`GoalsLayoutDocsTests.swift:72` asserted that the skill **contains**
`printf '\033[B' | curl`. `LOOP.md` makes an existing test file Frozen,
so the crew stopped and proposed rather than editing, which is correct.

The foreman approved the edit rather than waking the human, on this
reasoning: completion condition 2 of `goal.md` requires the skill to
answer the dialog with the toolset and to carry **no** `curl` in its
text. The test and the goal contradict each other outright, and `goal.md`
is both Frozen and the objective the human approved, so it is the higher
authority. The assertion encoded the contract this capability exists to
replace, which is the goal being carried out rather than a widening of
scope. The replacement keeps the assertion's intent: the skill names one
way to press a key, and it is not `text`. The `XCTAssertFalse` on the
escape spelling stays beside it.

## Gap

class: harness

evidence: two of the loop's own tests disagreed with the goal they serve.
`GoalsLayoutDocsTests` exists to stop the docs drifting from the layout,
and it did its job: it caught a change to a documented instruction. It
could not know that this change was the point. A test that pins a
sentence in a document will always stop the round that rewrites that
sentence; that is the cost of pinning prose, and it is worth paying, but
the tier rule should name it so the stop is expected rather than a
surprise.

## Decision

done. Capability 2 is complete on `goals/skill-drops-curl` at `7d600c3`.
The session is archived.

propose (`LOOP.md` Authority: an assertion that pins the text of a
document a round is chartered to rewrite is Propose, not Frozen, when the
goal's own completion condition requires the change. Same family as the
open count-pin and layout-pin proposals; three rounds have now hit it.)

## Reflection

The foreman lost twenty minutes by spawning a pane, running its floor,
and never sending the prompt. The loop already says to commit to the base
before sending the prompt; it does not say to send the prompt at all,
because that was assumed. It is now a rule in `facts.md`: after the
floor, either send the prompt or record why not.

The round's own evidence is the best kind: the crew ran the skill's steps
as written to test the words it had just written, in a real dialog, with
the tools the skill names. That is the difference between a document that
claims a procedure works and one that has been executed.
