# Round 002: the-skill-holds-the-procedure

- Goal: loop-and-skill
- Started: 2026-09-23T12:47+07:00  Ended: 2026-09-23T13:10+07:00
- Sessions: 61AD6FFA-D1B1-47EC-90F7-958DE4FC724C (claude-fable-5-1)   Archives: 61AD6FFA-D1B1-47EC-90F7-958DE4FC724C
- Base: a5592bd   Result: bde1581, 94af778 on `loop-and-skill/round-1`
- Cost: $5.36 · 2780k in (95% cached) · 38k out · 0h 15m

## Prompt

Capability 2: the skill keeps tiers 3 and 4 and sheds tiers 1 and 2,
pointing at `<knowledge>/LOOP.md` for the contract; plus round 1's
three leftovers. No correction.

## Floor

before: green (a5592bd)
after: green (`swift test --filter KittermCLITests` exit 0; `swift
test` exit 0; the Linux docker pipe)

## Effects

- behaviour: the skill 572 → 445 lines; `LOOP.md` 329 → 332 and the
  template 328 → 331, because "Commit after every round" moved in.
  `Round record` and its six subsections, `Labels`, `Stop rules` and
  `Direction` left the skill; `Direction`'s tier 3 lines (set
  `Updated`, the next action, `kitterm goal new` in the shell, commit
  every direction edit) became one `Monitor` bullet. `Goal or chore`
  became `Chores`: what the foreman does, citing `LOOP.md` for the
  test of what is a goal.
- visible: **`comm -12` over the two files' `##` headings is empty** —
  thirteen shared headings at the start of the goal, none now. A
  sentence audit over 328 old sentences found 16 absent, each named in
  the note. `loopRules` is checked against the three `LOOP.md` copies
  only and `skillRules` against the two skill copies; no sentence is
  pinned against both.
- persistent: `examples/foreman/foreman-loop.md`,
  `Sources/KittermCLI/ForemanSkills.swift`, `GoalsTemplates.swift`
  (`loopHistory` gained another hash), `docs/goals/LOOP.md`,
  `examples/goals/LOOP.md`, `Tests/KittermCLITests/GoalsLayoutDocsTests.swift`.

## Gap

class: none
evidence: the overlap is empty, the floor is green, and every deleted
sentence is either in `LOOP.md` or named.

## Decision

done. The goal's completion condition holds; see `STATE.md`.

## Reflection

The measurement that opened this goal — thirteen shared headings, ten
of the skill's sections naming no tool — is the measurement that
closes it. A duplication worth removing can be counted before and
after; one that cannot be counted is probably a judgment, not a defect.
