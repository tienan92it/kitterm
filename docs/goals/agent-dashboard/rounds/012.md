# Round 012: where-columns-per-filter

- Goal: agent-dashboard
- Started: 2026-09-19T17:31+07:00  Ended: 2026-09-19T17:52+07:00
- Sessions: 452962C1-D2C1-4B7A-81D6-63879B12E944   Archives: 452962C1-D2C1-4B7A-81D6-63879B12E944
- Base: 0b41da9 (agent-dashboard/round-10, PR #134)   Result: 6432dae on the same branch
- Cost: $5.60 · 7270k in (98% cached) · 29k out · 0h 09m

## Prompt

The human asked why the WHERE section's values were not the design.
Round 11 had changed the unit cell and kept round 10's count and
pull-request cells; the foreman had chartered only the unit rows, so
the crew read the narrower scope. The prompt named the three cells per
filter as the Components frame draws them: project `10 goals · 83 PRs ·
$11.17/PR`, goal unchanged, task `1 round · PR #122 · $0.010/line`,
role `86% · 34.0 h · $61/API hour`.

## Floor

before: green (0b41da9: vitest 1421 in 52 files)
after: green (tsc clean; vite build to scratch; vitest 1425 in 53 files,
4 added, 3 assertion groups replaced; swift test not run, no Sources/
change)

## Effects

- behaviour: the count and pull-request cells read per filter as the
  frame draws them; a session count moves to the row's tooltip; a
  project with a remote and no merged PR prints a dash; a task grouping
  groups a goal's rounds by task name, so a task of two rounds prints
  `2 rounds` and `2 PRs`.
- visible: `where-{project,goal,task,role}-1200.png` in the crew's
  scratchpad, read by the foreman beside the frame;
  `sessions-where-columns.test.ts` pins the three cells per filter.
- persistent: `Web/terminal/src/sessions-value.ts`,
  `sessions-value.test.ts`, `sessions-where-columns.test.ts` (new),
  `AGENTS.md`, `docs/architecture.md`.

## Chartered replacements

Project row `92 sessions` → `5 goals`; task rows' wall time → `1 round`;
role rows `114 sessions` + `12.8 API h` → `69%` + `12.8 h`. Named in
the note, each with the frame row it now matches.

## Gap

class: steering
evidence: round 11's prompt said "match its columns" and chartered only
the unit rows. A charter that names fewer cells than the sentence asks
for is read as the smaller of the two. Name every cell the charter
covers.

## Decision

done.

## Reflection

The task filter lists every round of the range, 26 rows and
`65 more rounds`, where the goal filter caps at four and `20 more
goals`. The frame draws four example rows and says nothing about a
cap. A question for the human, not a defect.
