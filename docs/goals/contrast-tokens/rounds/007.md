# Round 007: sink-the-well

- Goal: contrast-tokens
- Started: 2026-09-11T14:27+07:00  Ended: 2026-09-11T14:45+07:00
- Sessions: A7DCF575-0524-4D8E-8738-46DDA09A0490   Archives: the same
- Base: 2fb2903 (goals/sink-the-well)   Result: 7a45e87 on goals/sink-the-well-work

## Prompt

Capability 6 from `plan.md`, the goal's last queued item, sent as one
`send_input` of 5496 bytes. It gave the crew round 5's figure of 13 and
told it to re-derive rather than trust it, named round 6 as the model for
weighing the trade, and said that if clearing all 13 cost the recess it
should clear what it could and say why it stopped.

## Floor

before: green (vitest 1310 in 34 files)

after: green. The foreman re-ran the web floor on `7a45e87`: tsc clean,
**1310 tests in 34 files, 0 failures**. `theme-contrast.test.ts` holds at
726 either side. `swift test`, the bench and the Linux build were skipped;
the diff holds two files and no Swift.

## Effects

- **13 cleared, 0 new, 0 worse, and four pairs went whole.**
  `KNOWN_BELOW` falls from 97 to 84 over 31 pairs. Levers now reach 18 and
  **72 are the theme's own colours**.
- **The value is `color-mix(in oklab, var(--ui-bg) 25%, black)`,** and the
  depth is the round's real finding. The 13 entries clear at 88, 88, 87,
  82, 66, 66, 61, 61, 59, 41, 36, 36 and 26 percent of `--ui-bg` left in
  the well, so **all 13 need 26%**, which is `#000000`–`#010203` on all 17
  themes. Nothing between 30 and 60 percent looks different from black.
- **The brief's fear did not happen.** It worried the well would come to
  match the page; the well only deepens. Separation from `--ui-bg`, before
  and after: `nord` 1.074 to 1.663, `one-dark` 1.057 to 1.490, and
  `github-dark` 1.016 to 1.110. Today's well is a smaller step than
  `--ui-surface`'s lift on every theme; at 25% it sits about where
  `--ui-border` sits above the page.
- **Three costs the brief did not name, and the crew named them.** The
  well is black on every theme and no longer carries the page's hue,
  though the syntax colours still do. `--ui-bg-sunken` also paints
  `.extra-keys`, `.file-picker-header`, `.file-picker-hint` and
  `.intro-logo`, so those go black too. And a light theme would get a dark
  grey well under dark text, which no bundled theme is; the polarity test
  names the day one arrives.
- **The plan row's premise was wrong and the crew corrected it.** The row
  called `--ui-bg-sunken` "the file preview's background". It is that and
  four other surfaces. The crew found this itself and screenshotted the
  soft key row without being asked.
- visible: six images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/ct-round7/`: the preview before
  and after on `nord` and `one-dark` at 1200 px, opened by a real path
  click, and the soft key row before and after on `nord` at 390 px.
  **The foreman read the after-images of both.** The preview reads as a
  recessed panel with the code in a black field inside the theme's card,
  and the syntax colours are more legible than before. The key row reads
  as a keyboard tray with legible caps.
- persistent: two files. The probe the crew used to search the depth is
  gone; the foreman checked.

## Gap

class: steering

evidence: the plan row told the crew that `--ui-bg-sunken` is the file
preview's background, and it paints four more surfaces. The foreman wrote
that row from round 5's grouping without grepping the token. A row that
names a token should name everything the token paints, because the crew
will either discover it late or not at all.

## Decision

done. Capability 6 is complete on `goals/sink-the-well-work` at
`7a45e87`.

## Reflection

The round did what rounds 2, 3, 5 and 6 did: it re-derived the number it
was handed, and the number held. It also did what round 6 did, which is
weigh the cost before spending it. What is new here is that it reported a
cost the brief had not thought to ask about, and gathered the evidence for
it unprompted. A brief that names one surface got a round that checked
all five.

The goal's shape is now settled. 84 entries remain, 72 of them a theme's
own colours that `goal.md` protects and the ratchet records as permanent.
The 12 that levers still reach are spread thin, with no cheap one left.
This is the point at which the goal stops being worth another round, and
its completion conditions are written so that it can close here.
