# Round 011: the-approved-adjustments

- Goal: agent-dashboard
- Started: 2026-09-19T16:52+07:00  Ended: 2026-09-19T17:24+07:00
- Sessions: 9BAB024C-ECAB-470F-86C6-13191D0E1209   Archives: 9BAB024C-ECAB-470F-86C6-13191D0E1209
- Base: 4cd1db9 (agent-dashboard/round-10, PR #134)   Result: 01bfb87 on the same branch
- Cost: $25.95 · 38987k in (99% cached) · 116k out · 0h 28m

## Prompt

The human's. They amended `dashboard.pen` in Pen over four passes with
the foreman drawing on the canvas and them reviewing, then saved and
approved. The prompt listed the nine differences from round 10's page:
`MODELS` under `QUOTA` with a divider; the quota as bars with a caution
state at 80% (`#f4a261`); one 140 px name column across `QUOTA`,
`MODELS` and `WHERE`; unit costs as `/PR`, `/line`, `/API hour`; no
action control on the page; the disclosure triangle alone on project
and goal rows, none on a workspace; `•` pending; two mark-size tokens;
a green done mark. The exports went to the foreman's scratch directory;
none entered the corpus. No correction.

## Floor

before: green (4cd1db9: swift test 827, vitest 1411 in 52 files)
after: green (swift test 827; tsc clean; vite build to scratch; vitest
1421 in 52 files, 16 added, 6 replaced)

## Effects

- behaviour: the page reproduces the approved frames at 1200 and 390.
  `--ui-caution` joins the owned colours; `--ui-success` is green;
  `--mark-size` and `--mark-size-disclosure` join the tokens. The
  actions cell is gone from the line; the triangle folds a project's
  or a goal's rows.
- visible: the crew's `after-1200.png` and `after-390.png`, read by
  the foreman beside the exports; `sessions-quota` pins 79 accent and
  80 caution; `sessions-no-input` pins no action control.
- persistent: `Web/terminal/src/sessions.ts`, `sessions.css`,
  `sessions-model.ts`, `sessions-tree.ts`, `sessions-value.ts`,
  `theme-contrast-derive.ts`, eight test files, `AGENTS.md`,
  `docs/architecture.md`.

## Chartered replacements, named in the crew's note

quota ASCII cells (3, `sessions-quota`); `a line` / `an API hour` rows
(4, `sessions-value`); panel order and notice (2, `sessions-value-page`);
the actions cell, `[new]`, `⋯`, `[Dismiss]` (`pen-tree` 1, `band-page`
1, `line` 1); the project spinner and the goal's amber mark
(`pen-tree`); the `·` glyph (`line` 2); the 1ch mark and the grey
success (`css` 2); the font-size rule admits `.mark` and
`.mark.disclosure`. `KNOWN_BELOW` untouched; the 3:1 non-text check
now covers five hues, lowest green 4.68 and caution 5.62 on `nord`.

## Where the crew chose, the frames being silent

- Fold state lives in memory; every row is open on load.
- Proposal dismissal is gone with `[Dismiss]`; a proposal counts until
  the next round.
- The daemon-restart notice keeps its `[Dismiss]`: a page notice, not a
  line action.
- The API-hour rate prints in whole dollars, as the frame draws it.
- `LEAKS`'s second line keeps `·` as a list bullet, not a mark.

## Gap

class: none
evidence: the note, the floor and the shots agree; every diff path is
Free; every replaced assertion is named.

## Decision

done.

## Reflection

The foreman's floor script built `vite` into the checkout's `dist`,
which the installed daemon serves, so for a few minutes the live page
was the branch's. Rebuilt from `main`; the script now builds to
scratch. Fact for `facts.md`.
