# Round 003: say-it-on-the-page

- Goal: daemon-last-words
- Started: 2026-09-10T22:25+07:00  Ended: 2026-09-10T22:45+07:00
- Sessions: 4E7337E8-ECCA-40DA-992E-13A4349F1A4C   Archives: the same
- Base: a056b3d (main)   Result: 39127a2 on goals/say-it-on-the-page

## Prompt

Capability 3 from `plan.md`, sent as one `send_input` of 5450 bytes: one
line above the cards for `previous=unrecorded` only, dismissible per run,
the decision in a pure function with vitest cases, the page's own
accessibility rules from rounds 5 and 7 of `projects-and-knowledge`, and
proof on a scratch daemon at two widths. The prompt named the three keys
capability 2 emits and told the crew to skip the Linux build, because two
other crews were live and swap was at 8.3 of 9.2 GB.

## Floor

before: green (vitest 629 in 32 files, `swift test` 646, bench p95 2.48 ms)

after: green. The foreman re-ran the web floor in the crew's worktree on
`39127a2`: tsc clean, **654 tests in 32 files, 0 failures**, 25 more than
the base. The crew's own run agrees. `swift test` stayed at 646 of 646,
which is the right answer: the diff holds no `Sources/` file, so the
Swift suite cannot move. Bench p95 2.49 ms. The Linux build was skipped
by instruction.

## Effects

- **Four cases, each proved against a real process.** The crew ran a
  scratch daemon on port 51082 under `KITTERM_STATE_DIR` and never
  touched 3418.
  - unrecorded, after `kill -9` while it held two sessions: "The daemon
    restarted. The previous run was last alive at 10:35 PM and lost 2
    sessions."
  - clean, after `kitterm stop`: nothing.
  - takeover, after `POST /api/upgrade/takeover`: nothing, with the same
    pid, the same epoch, and both session ids kept.
  - previous absent, on a first start: nothing.
- **The decision is a pure function.** `restartNotice(started, dismissed,
  clockTime)` in `sessions-model.ts` takes one `daemon.started` event and
  returns `{text, key}` or `null`. Eight vitest cases, two more than the
  brief asked for: the crew added "one lost session" for the singular,
  and a case where `previousAliveAt` or `previousSessions` is missing or
  is not a number. Its reasoning is that both values are the line's own
  claim, so the line must not appear without either.
- **The dismissal keys on the epoch.** It held across a reload and five
  polls, and the next `kill -9` produced a new epoch and a new line.
  Focus on Dismiss survived a repaint, under
  `data-focus dismiss:restart:epoch:<id>`, which is the rule round 5 of
  `projects-and-knowledge` paid for.
- visible: three screenshots under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/dlw-round3/`:
  `dlw-round3-restart-1200.png` shows the line with its Dismiss control;
  `dlw-round3-restart-390.png` shows it wrapping to two lines with no
  horizontal overflow; `dlw-round3-takeover-1200.png` shows no line and
  two live sessions after the upgrade. The foreman read all three.
- persistent: five files, all under `Web/terminal/src/`. The contrast
  test gains a third surface, `tint`, and one `it` per theme.

## Gap

class: none

evidence: every case is asserted twice, once in the pure function and
once against a daemon that was really killed or really upgraded. The
foreman re-ran the floor and read the three screenshots rather than
taking the note's word. The one open item is inherited and unchanged:
round 1's `restarted` reason still has no writer.

## Decision

done. Capability 3 is complete on `goals/say-it-on-the-page` at
`39127a2`. The session is archived.

propose (`Web/terminal/src/tokens.css`, outside this round's paths:
`--ui-text` is already under 4.5:1 on `solarized-dark` and `synthwave-84`,
and the 8% danger tint takes them to 3.85 and 3.30. Both are new
`KNOWN_BELOW` rows. The same row also pins `.strip-item.failed`, which
nothing covered before. The fix belongs to `contrast-tokens`, not here.)

## Reflection

The crew read the other live crew's branch naming and followed it without
being told, and it named the collision before it could become one: its
new `tint` row needs an sRGB mix helper, and `contrast-tokens` is
rewriting how that same test derives its pairs. Two crews touched one
file in the same hour and the second one said so in its first note.

The cost a rule can prevent is the collision itself. The foreman gave two
goals the same file in the same hour without checking. The queue is
scheduled by `Updated` date, not by the files a capability names, so
nothing in the loop could have caught it. That is written as a fact:
**before starting a round, read the other open rounds' plan rows and say
in the prompt which file they share.**
