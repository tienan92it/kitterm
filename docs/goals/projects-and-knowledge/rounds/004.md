# Round 004: goal-loop-skill

- Goal: projects-and-knowledge
- Started: 2026-09-08T16:48+07:00  Ended: 2026-09-08T17:12+07:00
- Sessions: 883D5436-EAD6-486C-B80D-B79AECE5B80B   Archives: 883D5436-EAD6-486C-B80D-B79AECE5B80B
- Base: 9fbe914 (goals/scaffold-and-docs)   Result: 0b1d16e, 950f794, 97fa22e on goals/goal-loop-skill

## Prompt

Capability 4 from `plan.md`, sent as one `send_input` of 5241 bytes: read
the package, corpus request 03, rounds 001 to 003, `docs/foreman.md`,
`examples/foreman/*`, and the round 3 embed-and-golden pattern; branch
`goals/goal-loop-skill`; the five pieces (rewrite `foreman-loop.md` from
`LOOP.md` with the named sections in order, one `goal:` rule in
`review-crew.md` and `triage.md` with the shared text aligned, the
"One foreman for every project" section in `docs/foreman.md`,
`kitterm skills install [--dir]` and `skills list` with embedded skills and
a golden test, tests); the authority tiers; the floor; commit, do not
push; post one note under 1900 bytes.

## Floor

before: green (`swift test` 543 tests 0 failures, vitest 504, bench p95
2.49 ms)
after: green (`swift test` 553 tests 0 failures, vitest 504, bench p95
3.19 ms; the foreman re-ran `swift test` and vitest with the same counts)

## Effects

- behaviour: the crew rewrote `foreman-loop.md` to 354 lines with the
  sections Rules, Read before you type, Scan, Schedule, One round,
  Monitor, Reports, Direction, Stop rules, Round record, Labels; added the
  `goal:` rule to the two other skills; wrote `ForemanSkills.swift`,
  `SkillsCommand.swift`, two test files with ten tests, and the docs
  section. Three commits, 24 minutes, alongside two review sessions
  reading the same checkout through git with no interference.
- visible: `kitterm skills install --dir <tmp>` prints `wrote` for three
  files, then `unchanged` for three on a second run; `kitterm skills list`
  prints the three names and descriptions.
- persistent: ten files changed, all Free tier
  (`git diff --name-only goals/scaffold-and-docs`). The foreman ran the
  built binary on a temp directory with the same result. The installed
  copies under `~/.claude/skills/` differ from the new skills by 347, 44,
  and 41 lines; `kitterm skills install` with no `--dir` replaces them.

## Gap

class: harness
evidence: two mismatches between `LOOP.md` and practice, found while the
crew turned the file into a skill. "One round" step 2 spawns with
`input:"claude\n"`, but rounds 1 to 4 spawned with no input, ran the floor
in the shell, then sent `claude`, and the skill now states that order.
The label table says `resumed-from` holds an archive id, but a respawn
after an `epoch` change has no archive. Both are `LOOP.md` text, Propose
tier.

## Decision

done. The item `goal-loop-skill` is complete on `goals/goal-loop-skill` at
`97fa22e`, not pushed. The session is archived.

propose (`LOOP.md` One round step 2: spawn with no input, run the floor
in the shell, then send `claude` and read the screen; the skill already
says this.)

propose (`LOOP.md` Labels: `resumed-from` holds the archive id, or the id
the pane held before an `epoch` change.)

## Reflection

Writing a procedure down as a skill is a check on the procedure: the
crew found two places where `LOOP.md` and the rounds disagreed, which
three rounds of running it had not surfaced. The review gate ran in
parallel with this round under the three-session cap, and the git-only
read rule kept the reviewers off the crew's working tree. One outside
event: an `AskUserQuestion` approval arrived from a hook that named a
session id the daemon no longer had, so the fleet view showed an approval
with no pane to link. That is a fact for the product, in `facts.md`.
