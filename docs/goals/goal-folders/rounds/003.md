# Round 003: dashboard-per-goal

- Goal: goal-folders
- Started: 2026-09-09T12:20+07:00  Ended: 2026-09-09T12:52+07:00
- Sessions: 3CF90F08-966E-419C-B539-B912A4CEBD2F   Archives: 3CF90F08-966E-419C-B539-B912A4CEBD2F
- Base: c6c6de8 (goals/goal-folders)   Result: 54f99a5, 01a6ad5 on the same branch

## Prompt

Capability 3 from `plan.md`, sent as one `send_input` of 4497 bytes: read
`LOOP.md`, this goal's files, rounds 001 and 002, the previous goal's
rounds 006 and 007 for the accessibility rules, and the fleet view
sources; stay on the branch; the seven pieces (every goal on the card,
expanded or one line by status; `goal:` sub-headers per goal with an
unmatched group; proposed items per goal with a per-goal dismiss key;
pure model functions with tests; the accessibility rules on every new
element; corpus request 04 with screenshots; AGENTS.md); the tiers with
the pinned single-goal assertions named; the floor.

## Floor

before: green (`swift test` 605 tests 0 failures, vitest 615, bench p95
2.47 ms)
after: green (`swift test` 605 tests 0 failures, vitest 625, bench p95
2.85 ms; the foreman re-ran `swift test` and vitest with the same
counts)

## Effects

- behaviour: the crew rewrote the card's goal block into a list over
  `goals[]` with `goalBlocks`, extended `goalGroups` with the unmatched
  groups, made the dismiss key per goal, and described the card in
  AGENTS.md. Two commits, 32 minutes, no correction.
- visible: `gf-round3-sessions-1200.png` and `-390.png` (the kitterm
  card: `goal-folders` expanded with `round 2 of 3`, `active`, the next
  action, `proposals: 1` linking to its `STATE.md`, `record 002`;
  `projects-and-knowledge` as one line, `done`, `record 008`; the crew
  under `goal: goal-folders` with a `round 3` chip),
  `gf-round3-unmatched-390.png` (a `goal: unknown-goal` rule at the end
  of the card), `gf-round3-proposed-390.png` (two proposing goals of one
  fixture project give two strip items; Dismiss stored
  `fixture:goal-folders:2` and the other stayed). Corpus request 04's
  card and route expectations hold; its CLI expectations held in round 1.
- persistent: six files changed, all under `Web/terminal/src/` and
  AGENTS.md. Tests added in `sessions-knowledge.test.ts`: `goalGroups`
  unmatched groups (a stray label, no summary, an empty card, name
  order, none when every label matches), `goalBlocks` (expanded against
  one line, case, absent or unknown status, empty and null), `goalTitle`,
  `statePath`, `proposedItems` over several goals with a per-goal
  dismiss, the names with the goal, `recordLabel` with a folder. Pinned
  single-goal assertions changed, named in the note: three `goalGroups`
  tests where a row with a stray label moves from `rest` to an unmatched
  group, and `dismissKey` takes the slug. No new colour pair; the
  contrast test gained a comment only.

## Gap

class: none
evidence: the corpus request passed on the first run; the accessibility
rules from the two earlier reviews were carried by the prompt and held.

## Decision

done. Capability 3 is complete on `goals/goal-folders` at `01a6ad5`. The
session is archived. The budget of this direction is spent: rounds 1, 2,
and 3.

## Reflection

Three rounds delivered the layout end to end for the CLI, the daemon,
and the page, each round under 35 minutes, none needing a correction,
because every prompt named the earlier records and the rules they had
paid for. What remains is capability 4, the skill and the docs, which is
the part the standing foreman reads; until it lands, the installed skill
still reads one `docs/goals/STATE.md`, which this repository no longer
has. That is the reason `skill-and-docs` should be the next round rather
than a review gate.
