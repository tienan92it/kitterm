# Round 003: scaffold-and-docs

- Goal: projects-and-knowledge
- Started: 2026-09-08T16:19+07:00  Ended: 2026-09-08T16:40+07:00
- Sessions: D59EF8A5-6811-47C8-9253-3FABF3EE229A   Archives: D59EF8A5-6811-47C8-9253-3FABF3EE229A
- Base: 1531730 (goals/dashboard)   Result: da90765, 82a31af, 5ea3f47, 332e096 on goals/scaffold-and-docs

## Prompt

Capability 3 from `plan.md`, sent as one `send_input` of 3995 bytes: read
the package and rounds 001 and 002; branch `goals/scaffold-and-docs`; the
five pieces (generic templates under `examples/goals/`, `kitterm project
init` with the templates embedded as Swift strings and a byte-for-byte
golden test, the 16-tool table and the reserved label keys in
`docs/foreman.md`, the state tree in `docs/architecture.md` and
`AGENTS.md`, tests); the authority tiers; the floor; commit, do not push;
post one note under 1900 bytes.

## Floor

before: green (`swift test` 535 tests 0 failures, vitest 504, bench p95
2.47 ms)
after: green (`swift test` 543 tests 0 failures, vitest 504, bench p95
2.48 ms; the foreman re-ran `swift test` and vitest with the same counts)

## Effects

- behaviour: the crew wrote seven template files, `GoalsTemplates.swift`,
  the `init` subcommand, two test files with eight tests, and the two doc
  updates. Four commits, 8 minutes.
- visible: `kitterm project init <path>` prints one `wrote` line per file
  and the registered row. A second run answers
  `refusing to overwrite: <files> (nothing written)` and exits 1.
- persistent: fifteen files changed, all Free tier
  (`git diff --name-only goals/dashboard`). The foreman ran the built
  binary on a temp git repository with a scratch `KITTERM_STATE_DIR`: the
  seven files appeared under `docs/goals/`, `projects.json` held the
  registration with `knowledge: docs/goals`, and the second run refused
  with nothing written. The templates keep `LOOP.md`'s procedure, tiers,
  budget, stop rules, and record shape; the kitterm specifics are angle
  bracket placeholders.

## Gap

class: none
evidence: the corpus has no request for capability 3; the proof column
named the CLI test and the doc diff, both delivered. One text slip:
`plan.md` capability 3 says "all 15 tools"; the count is 16 since round 1.

## Decision

done. The item `scaffold-and-docs` is complete on
`goals/scaffold-and-docs` at `332e096`, not pushed. The session is
archived.

propose (`plan.md` capability 3: "names all 15 tools" becomes "names all
16 tools".)

## Reflection

The round with a small, well-bounded task and no corpus request ran in
8 minutes with no correction. The two larger rounds each surfaced one rule
gap that the unit tests had not, once because a count pin was Frozen and
once because a corpus fixture needed a parent folder registered. A corpus
request per capability finds gaps that the proof column does not; the
capabilities without one (3 and 4) should get one before the next budget.
Fact recorded: a live check of the CLI must run the built binary from
`.build/debug/`, because `swift run` needs `Package.swift` in the cwd.
