# Round 005: review-fixes

- Goal: goal-folders
- Started: 2026-09-09T15:45+07:00  Ended: 2026-09-09T16:12+07:00
- Sessions: BB7D86FB-D990-4849-8598-2425E32DC5B5   Archives: BB7D86FB-D990-4849-8598-2425E32DC5B5
- Review sessions, archived: 2C28ED41 (security), 82ACD7BA (daemon), 680199C4 (a11y)
- Base: 7090b34 (goals/goal-folders)   Result: 280cae5 to 857e7f0, fifteen commits on the same branch

## Prompt

The review gate the human set before any push: three reviewers on
`main...goals/goal-folders`, static and read-only through git, one
dimension each. Counts: security 0 blocking, 3 should-fix, 1 nit;
daemon 0, 6, 6; accessibility 0, 5, 9. The foreman deduped fourteen
should-fix items into ten (G1 to G10) in the job's
`review3/ranked.md` and sent them as one `send_input` of 3731 bytes:
one commit per finding, a test for every fix a test can hold, hand
checks at 390 px, the Linux build once alone.

## Floor

before: green (`swift test` 609 tests 0 failures, vitest 625, bench p95
2.53 ms)
after: green (`swift test` 615 tests 0 failures, vitest 629 passed in 32
files, bench p95 2.60 ms, Linux build ok; the foreman re-ran the whole
floor after the round with the same counts)

## Effects

- behaviour: G1 to G10 as ten commits named for the finding, four
  optional nits, and one AGENTS.md commit. Twenty-two minutes. The Fable
  usage limit stopped the session while it trimmed its note, after every
  commit, the floor, the Linux build, the screenshots, and the scratch
  cleanup were done; the human switched to Opus 5 and the foreman
  reconstructed this record from git and the screenshots. No note was
  posted, which is why the loop's rule is to post each digest as a note.
- visible: `gf-round5-sessions-390.png` (an `active` goal expanded with
  its slug beside the title, a `waiting` goal on one line with its
  proposals chip and record link, a `done` goal on one line),
  `gf-round5-unmatched-390.png` (`goal: unknown-goal (no folder)`),
  `gf-round5-nofolder-390.png` (a project whose package has no goal
  folder reads "no goal folder"), `gf-round5-sessions-390-full.png`.
- persistent: 21 files changed, 530 insertions, 297 deletions. The
  daemon reads a goal folder through one descriptor; the listing skips a
  child whose name fails the slug rule and caps at 64 folders; the CLI
  shares the daemon's listing, slug rule, refusal path, and status
  parser; four tests that pinned this repository's exact goal list now
  assert containment and order, so `kitterm goal new` on this checkout
  no longer turns the floor red.

## Gap

class: harness
evidence: the round's own note was lost to a usage limit at the last
step. The loop's records survived because every fix was committed as it
landed, which is the rule the dogfood round added. A note posted per
step, not per round, would have survived too.

## Decision

done. Round 5 is complete on `goals/goal-folders` at `857e7f0`. The
session is archived. The goal's five completion conditions hold; the
branch is ready to push.

propose (`examples/foreman/foreman-loop.md`: post the round's note with
`post_note` before the floor, and update it after, so a session that
dies at the end still leaves its evidence on the feed.)

## Reflection

Three review gates have now run on this package, and each found its
largest item in the layer the previous gate did not touch: the routes,
then the page, then the CLI and the tests. The rule this round adds is
about the loop, not the product: a crew's evidence must reach the feed
before its last step, because the last step is where a session dies.
