# Round 001: a-row-is-one-line

- Goal: fleet-catch-up
- Started: 2026-09-15T16:16+07:00  Ended: 2026-09-15T16:32+07:00
- Sessions: F665B8E0-A400-41DF-9E0A-7ABDF4D1A808   Archives: the same
- Base: 5b8f712 (goals/two-new-goals)   Result: f38dea5 on goals/a-row-is-one-line

## Prompt

Capability 1 from `plan.md`, sent as one `send_input` of 5196 bytes. It
quoted the human's brief, pointed at the inventory and the before-images,
and asked the crew to decide two things and defend each: what "how long"
measures, and what a named session in a subfolder shows for its place.

## Floor

before: green (vitest 1310 in 34 files)

after: green. The foreman re-ran the web floor on `f38dea5`: tsc clean,
**1321 tests in 34 files, 0 failures**, 11 new. `swift test`, the bench
and the Linux build are not on this goal's floor; the diff holds no
Swift.

## Effects

- **The row is a pure function.** `rowLine(row, now)` returns `{name,
  state, place, what, since}` and a null field is not painted. Eight
  cases over it, two over `stateLabel`, one over `spanLabel`.
- **Dropped from every row:** `pid`, `exit 0`, `attached` and `detached`,
  `N watching`, the shell name, the profile, program, task and round
  tags, and the project root. The exit code lives only in the state word,
  and `stateLabel` moved into the model and prints `exited` for code 0.
- **"How long" is since the last output**, one unit, `now`, `Nm`, `Nh`,
  `Nd`. The crew's reason is the right one: the last output is when the
  row's state began, whether a prompt that waits, a command that failed,
  or a shell that went quiet. A working row reads `now` while alive and
  grows when it stalls. No seconds, so a live row repaints once a minute
  at most.
- **Place is the cwd relative to the project root**, printed only for a
  named row away from the root; null at the root, which the card names.
  An unnamed row takes that path as its name, so `docs/postman` prints
  once where it used to print three times.
- **"What" is the agent's message, else the note, else `$ command`, else
  nothing.** The crew merged message and note because `post_note` is the
  agent's message by another route, and the old row already treated them
  as one.
- visible: four images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/fcu-round1/`, before and after
  at both widths on `github-dark`, from the corpus fixture through a stub
  server on port 62101 with no daemon. **The foreman read the 390 px
  after-image.** Every row is one line, or two when it carries a message,
  measured in the page at 46 px and 66 px. No row prints `pid`,
  `attached`, `zsh`, `exit 0` or the project root. The page is 2448 px
  tall, from 2724 at the same fixture before.
- persistent: four files, all under `Web/terminal/src/`. `KNOWN_BELOW` is
  untouched because the new rules were written as `.main .place`,
  `.main .what` and `.main .since`, so the block table already places
  them.

## Gap

class: none

evidence: eleven cases over the model, the fixture rendered through a
stub server so the images are reproducible, and the crew stated the
limit of its proof: the images prove the rendering, and the fixture's
durations are the crew's own.

## Decision

done. Capability 1 is complete on `goals/a-row-is-one-line` at
`f38dea5`.

## Reflection

The prompt asked for two decisions and got two decisions with reasons,
and both reasons are better than the ones the foreman had in mind.
"Since the last output" is the one measure that means the same thing for
a waiting prompt, a failed command and a quiet shell; the foreman had
been thinking of `heldSince`, which exists only while linger holds a
session. A prompt that names the decision and asks for the reason gets a
reason.

The crew also noted what it saw and did not touch: the strip still
repeats the four attention rows, and the goal cards still print the
template placeholders. Both are the next two capabilities, and the note
says so without reaching for them.
