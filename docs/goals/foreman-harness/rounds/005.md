# Round 005: rules-that-agree

- Goal: foreman-harness
- Started: 2026-09-10T22:52+07:00  Ended: 2026-09-10T23:14+07:00
- Sessions: 917FFDF8-6492-437E-81D4-A7E64BF60A02   Archives: the same
- Base: 0c61302 (goals/four-loop-rules-work)   Result: 61b3f78 plus the foreman's commit

## Prompt

Four defects, each quoted with its file and line, sent as one `send_input`
of 5904 bytes. The prompt said which defect was a wording error and which
was a contract error, and told the crew that the intent of round 3 was
correct and only the tier name was wrong.

## Why this round exists

**The foreman closed round 4 before the review ran.** Round 4 was marked
done, its session archived, and its record written. The human requires a
review before a merge, so the review ran next, and it found four defects
in the round already recorded as done. Two independent reviewers, one on
the procedure and one on test coverage, reported the same hole in the
test from different directions.

## Floor

before: green (`swift test` 647 tests 0 failures at `0c61302`)

after: green. Red at the crew's commit by construction, as round 4 was:
`swift test` at `61b3f78` was 647 with 4 failures, all in the new check
and all naming `docs/goals/LOOP.md`. The foreman applied the crew's
command and re-ran: **647 tests, 0 failures**. Web green: tsc clean, vite
build, vitest 629 in 32 files. The Linux build and the bench were skipped
by instruction, because the diff holds no daemon code and swap was at 8.5
of 9.2 GB.

## Effects

- **Defect 1: the case is a fourth tier, and it now has a name.** The
  Authority table gains **Chartered**: an assertion in an existing test
  file that pins a text, a count, or a layout this round must change. The
  crew replaces the assertion inside the round and keeps its intent. The
  foreman records the old assertion, the new one, and the line of
  `goal.md` or `plan.md` that requires the change. A Chartered assertion
  is not a proposal, so the human does not edit the file and the
  decision stays `done`. The crew's reasoning is right: round 3 needed
  the crew to replace the assertion in-round and to close the item, and
  the Propose tier forbids both, so round 4 named the wrong tier rather
  than the wrong intent.
- **The two neighbouring sentences are now true.** The Frozen row reads
  "Nobody changes these inside a round, except a Chartered assertion".
  The closing pair reads "The loop can change the product. It can restate
  the evidence that `goal.md` charters it to change. It cannot delete or
  weaken the evidence that decides whether the product improved."
- **Defect 2: a round is open while a live session carries `crew:<slug>`.**
  The old test was "no live session carries `goal:<slug>`", and a helper
  carries `goal:<slug>` by rule 5, so a lingering helper read as an open
  round. The new test is decidable from labels alone, and a helper never
  holds a round open.
- **Defect 3: the check now compares whole rule sentences.** It folds
  whitespace and case, covers 7 rules over 5 sources, and rejects two
  retired wordings. The crew's argument for why this is stronger: a
  four-word marker fits inside any sentence, including that sentence's
  own negation, but a sentence cannot contain its own negation.
- **Defect 4: the two notes carry different fields.** The first carries
  the shas so far, the diff file list, the tests added and any proposal.
  The second adds the floor results. The old text bound the whole list to
  "each `post_note`", which the pre-floor note could not satisfy.
- visible: **the foreman ran the planted-inversion test itself.** It put
  "The assertion is then Chartered, not Frozen only when a human retypes
  goal.md by hand; in every other case the assertion stays Frozen" into
  `docs/goals/LOOP.md` and ran the suite. One test failed, naming the file
  and printing the exact sentence the file must carry. The foreman
  restored the file and the suite returned to 647 of 647. This is the
  sentence round 4's marker check would have accepted.
- persistent: six files. Five from the crew, one from the foreman.

## Gap

class: harness

evidence: **a round can be recorded as done and then fail review.** The
loop closes a round on a green floor and a clean diff, and the human's
rule puts the review before the merge, which is after the round closes.
Round 4 was written up as complete, with a reflection, before any
reviewer had read it. Nothing in `LOOP.md` says a round's decision is
provisional until the review returns, so the record claimed more than the
evidence supported.

## Decision

done. Capability 4 is complete on `goals/four-loop-rules-work` at
`61b3f78` plus the foreman's commit.

propose (`LOOP.md`: name the handoff. A capability whose deliverable spans
the crew's files and a foreman-owned file is red at the crew's last commit
by construction. Round 4 and round 5 both were. The crew proposes a
decision value `handoff` and a Floor line that reads "red at the crew's
commit, green after the foreman's command". The foreman adds a second
part: the review runs before the merge and after the round closes, so
either the round record waits for the review, or a round's decision is
provisional until it returns. Round 4 shows the cost of leaving that
unsaid.)

## Reflection

The reviews earned their cost on the first run. Two reviewers, blind to
each other, found the same hole in the marker check and described it the
same way: a file can carry the exact marker while stating the rule
backwards. One of them wrote the inverted sentence out. The crew then used
that sentence as its own acceptance test, and the foreman ran it a third
time against the finished file. The finding travelled from a reviewer's
example, to a crew's test case, to a foreman's verification, without
anyone restating it from memory.

What cost time is in the gap above, and it is the foreman's: round 4 was
recorded as done before the evidence was complete. The fix is in the
proposal.
