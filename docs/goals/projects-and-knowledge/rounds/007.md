# Round 007: review-fixes-2

- Goal: projects-and-knowledge
- Started: 2026-09-09T10:00+07:00  Ended: 2026-09-09T10:38+07:00
- Sessions: DD69CE5A-1A6B-44BC-9749-66D96E677DB1   Archives: DD69CE5A-1A6B-44BC-9749-66D96E677DB1
- Review sessions, archived: 6D79D123 (security), 6A8BE79E (daemon), 874B7E90 (a11y)
- Base: 39f8c7e (goals/knowledge-on-dashboard)   Result: 97e8b6c to 1e9cee3, thirteen commits on the same branch

## Prompt

The review gate the human set: three reviewers on
`main...goals/knowledge-on-dashboard`, static and read-only through git,
one dimension each. The foreman deduped fourteen should-fix items into
eleven (F1 to F11) in the job's `review2/ranked.md` and sent them as one
`send_input` of 3920 bytes: one commit per finding on the same branch, a
test for every fix a test can hold, hand checks at 390 px, the Linux
build once alone.

## Floor

before: green (`swift test` 580 tests 0 failures, vitest 570, bench p95
2.56 ms)
after: green (`swift test` 588 tests 0 failures, vitest 614 passed in 32
files, bench p95 2.54 ms, Linux build complete; the foreman re-ran all
three local checks with the same counts and p95 2.50 ms)

## Effects

- behaviour: F1 to F11 as eleven commits named for the finding, plus two
  optional nits (control and bidi characters stripped from the summary
  fields; the goal block skipped when the summary carries only the
  project). Thirty-eight minutes, no correction.
- visible: `round7-sessions-390.png` (the `record 7` link, the next action
  wrapped to three lines, the decision line to two, the proposals chip as
  a link), `round7-record-tab-390.png` (`7.md` opens as a text page),
  `round7-dismissed-390.png` (Dismiss drops the item and the count, gone
  after a reload), `round7-stalled-390.png` (with one summary held the
  fleet painted at 70 ms and a spawned row at 10 ms; the fetch aborted at
  2 s).
- persistent: eleven files changed against the round 6 state commit.
  Tests added, Swift: a FIFO at the path answers 404 at once; a symlinked
  `rounds/` and a symlinked root answer 404; an unregistered id answers
  404 (replacing the fallback test); `rounds/7.md` by name with
  `lastRecord`; the comma budget; `nosniff` on the summary; the control
  and bidi strip; the two content-type pins moved to `text/plain`.
  Vitest: focus keys, the record path, `proposedItems` with a dismissed
  set, the name builders, `hasKnowledge`, and two contrast rows for the
  card head.
- deviations the crew stated: F11 landed before F6 in commit order; the
  Linux docker build re-points `.build/debug` at the Linux tree and the
  next `swift build` restores it. F1's render order is a manual check.
- left: the hard-link case and the serial queue, each with an accepted
  comment; the per-directory summary cache (over twenty lines); the
  muted ratio of the row's `round` chip (token level).

## Gap

class: none
evidence: every required item landed with its test; the four hand checks
passed on the first run.

## Decision

done. The branch `goals/knowledge-on-dashboard` at `1e9cee3` plus this
record is ready for the push and one PR to `main`. The session is
archived.

## Reflection

A second review gate on a one-round branch found no blocking item and
fourteen should-fix items, nine of them on the page. The first gate's
rules held (keyed focus, the live region, the strip cap, small text in
the text tokens), so the new findings were about the new elements only:
links, a proposal's life cycle, and pairs the contrast test did not
cover. Two rules for the loop from this round: a new element on the page
adds its contrast pair to the theme test in the same round, and a link
that opens a served file is checked on a browser that does not render
the file's type. Both go to `LOOP.md` as proposals.
