# Round 002: summary-per-goal

- Goal: goal-folders
- Started: 2026-09-09T11:48+07:00  Ended: 2026-09-09T12:12+07:00
- Sessions: 718F12D5-F2E0-4A41-BFDF-E6359CB08D1C   Archives: 718F12D5-F2E0-4A41-BFDF-E6359CB08D1C
- Base: 61580f1 (goals/goal-folders)   Result: 9451a26, 9772539, 0227aa5 on the same branch

## Prompt

Capability 2 from `plan.md`, sent as one `send_input` of 4999 bytes: read
`LOOP.md`, this goal's files, round 1's record, the previous goal's
rounds 006 and 007, and the knowledge sources and tests; stay on the
branch; the six pieces (the `goals` array with the folder name as slug
and one order rule shared with `kitterm goal list`, the file route
unchanged with a record-path test, `summaries()` over the folders with
two fixtures, the page reading `goals[0]` with no visible change, the
AGENTS.md bullet, the tests); the tiers with the pinned single-summary
assertions named; the floor with the Linux build; a hand check on a
scratch daemon.

## Floor

before: green (`swift test` 598 tests 0 failures, vitest 614, bench p95
2.48 ms)
after: green (`swift test` 605 tests 0 failures, vitest 615, bench p95
2.51 ms, Linux build ok; the foreman re-ran `swift test` and vitest with
the same counts)

## Effects

- behaviour: the crew wrote `KnowledgeFile.summaries()`, the `goals`
  array on the summary route, one comparator
  `KnowledgeSummary.isOrderedBefore` that `kitterm goal list` now calls
  too, moved the page onto `goals[0]`, and described the shape in
  AGENTS.md. Three commits, 24 minutes, no correction.
- visible: on a scratch daemon with this checkout registered the summary
  route answers `goal-folders` (active) then `projects-and-knowledge`
  (done) with `projects-and-knowledge/rounds/008.md` as its record, and
  the file route serves that record as `text/plain`.
- persistent: ten files changed. Tests added: the route lists every
  folder in order, an empty list for a package with no goal folder, the
  ETag changes when a second goal appears, this repository answers two
  goals and serves a record; the summaries list every folder, the list
  order is the one the CLI pins, this repository's own package; vitest
  `goalGroups` over several goals. Pinned single-shape assertions
  changed, named in the note: three route tests moved to `goals[0]` and
  `<slug>/rounds` paths with their fixtures moved into folders; the
  vitest `goalGroups` calls take an array.

## Gap

class: none
evidence: the corpus request's route and CLI expectations hold on this
repository's own package; capability 3 owns the card.

## Decision

done. Capability 2 is complete on `goals/goal-folders` at `0227aa5`. The
session is archived.

## Reflection

The order rule lives in one comparator the daemon and the CLI share, so
the card, the API, and `goal list` cannot disagree. The pinned
single-summary tests were the cost of round 6's shape; a shape that
returns a list from the start would have spared three edits.
