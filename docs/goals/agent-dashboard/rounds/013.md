# Round 013: every-figure-follows-the-range

- Goal: agent-dashboard
- Started: 2026-09-19T17:58+07:00  Ended: 2026-09-19T18:22+07:00
- Sessions: 132B31B4-4D3E-45B0-8846-BC5B7A168C6A   Archives: 132B31B4-4D3E-45B0-8846-BC5B7A168C6A
- Base: c545e02 (agent-dashboard/round-10, PR #134)   Result: eb0a6bb on the same branch
- Cost: $12.91 · 17740k in (99% cached) · 71k out · 0h 18m

## Prompt

The human's, three changes: the task filter's count is the task's
working time again; the goal filter lists every goal with no `N more
goals` row; every figure follows the USAGE range. The crew audits each
panel and says which followed the range already and which it fixed.

## Floor

before: green (c545e02: vitest 1425 in 53 files)
after: green (tsc clean; vite build to scratch; vitest 1438 in 55
files, 13 added; swift and Linux not run, no Sources/ change)

## Effects

- behaviour: **followed the range already** — VALUE and the WHERE
  summary (one from/to query to `/api/usage/daily` and `/api/yield`),
  WHERE project and role (rollup buckets), WHERE goal and task
  (`roundsInRange`), MODELS, the band, the tree's project and
  workspace costs. **Fixed** — the tree's goal cost read the route's
  all-time `costUSD` and now sums `roundsInRange`; LEAKS counted every
  record and now counts the range's rounds. One `chosenRange` feeds
  WHERE and LEAKS. A record with no `Started` day is in no range. The
  task count is `countdown(sum of durationMs)`, a dash with none. The
  goal filter lists every goal, the unpriced ones by name before the
  remainder.
- visible: `range-7d-vs-90d-1200.png` in the crew's scratchpad, read
  by the foreman: USAGE, MODELS, VALUE and the WHERE summary differ
  between the ranges; the priced goals do not, because every one of
  their rounds ran on 15–16 September, inside both.
  `sessions-range.test.ts` and `sessions-range-page.test.ts` pin a
  fixture that straddles the boundary.
- persistent: `Web/terminal/src/sessions-value.ts`, `sessions-model.ts`,
  `sessions-tree.ts`, `sessions.ts`, `fake-page.ts`,
  `range-fixture.ts` (new), two test files (new), `AGENTS.md`,
  `docs/architecture.md`.

## Chartered replacements

Round 12's task `1 round` → time; the `N more goals` row in
`sessions-where-columns` and `sessions-value`; LEAKS's all-time round
count → the range's; the goal-cost test takes a range; the pen-tree
fixture's rounds carry the costs so the frame's figures come through
the range. Each named in the note.

## Gap

class: none
evidence: the audit names a proof line per panel; the shot shows the
ranges differ where the data differs; every diff path is Free.

## Decision

done.

## Reflection

The task count prints `1h 2m`, not the frame's `1h 02m`; the crew
kept the page's own `countdown`. One glyph of difference in a
placeholder I drew; not worth a round. The task filter keeps its
`N more rounds` row, as the prompt said.
