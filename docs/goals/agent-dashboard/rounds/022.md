# Round 022: the-record-names-its-pr

- Goal: agent-dashboard
- Started: 2026-09-25T12:36+07:00  Ended: 2026-09-25T12:50+07:00
- Sessions: D950F247-93C3-4155-9A49-E14DEE01DDC2 (claude-sonnet-5)   Archives: D950F247-93C3-4155-9A49-E14DEE01DDC2
- Base: c8a92ec   Result: 1a6174d on `agent-dashboard/round-22`
- Cost: $1.96 · 5999k in (98% cached) · 21k out · 0h 11m

## Prompt

`KnowledgeSummary.roundRecord` reads the `PR #N` after `Result:` on the
`- Base:` line, the shape `LOOP.md` gives, as well as on a `- Result:`
bullet. The human saw no PR numbers on the fleet view.

## Floor

before: green (c8a92ec: swift test 907)
after: green (2 new; swift test 909, run by the crew and again by the
foreman; the Linux docker pipe, `Build complete`, run by the crew; no
page code changed, so no web floor, screenshots or page heights; the
parser runs on the knowledge queue, so no bench)

## Effects

- behaviour: `roundRecord` takes the PR from a `- Result:` bullet when
  the record has one, as before; else from the first `PR #N` after
  `Result:` on the `- Base:` line. A bullet wins when a record has both.
  A result given as a sha names no PR on either shape. A `PR #N` inside
  the `Base:` value before `Result:` is not the round's PR, the rule
  `GoalLedger` already follows.
- visible: the knowledge route over the real `docs/goals/` tree: 11 of
  85 rounds carried `pr` on the live daemon, 27 of 85 on a scratch
  daemon from this build (port 34189, its own state directory, killed
  after). Gained: `agent-dashboard` round 10 (PR #134), `landing-page`
  round 3 (PR #158), `green-ci-again` round 1 (PR #150), and 13 more.
  The other 58 records name a sha alone; a record is never rewritten,
  so those rounds keep no PR, and their task lines carry it from
  `STATE.md` (PR #161).
- persistent: `Sources/KittermDaemon/KnowledgeSummary.swift`,
  `Tests/KittermDaemonTests/KnowledgeRoundTests.swift` (two cases added,
  no line removed or changed), `AGENTS.md`.

## Gap

class: contract
evidence: `LOOP.md`'s record shape writes `Result:` on the `- Base:`
line, and the parser `LOOP.md` names read only a `- Result:` bullet, so
74 of 85 records named no PR to the route. `kitterm goal cost` read both
shapes, so the ledger was right and the page was not.

## Decision

propose (`LOOP.md`, "The round record": replace "the pull request is the
first `PR #N` on the `- Result:` line, so a result given as a sha names
none" with "the pull request is the first `PR #N` on a `- Result:`
bullet, else the first `PR #N` after `Result:` on the `- Base:` line;
the bullet wins when a record carries both; a result given as a sha, on
either shape, names none". The product change is done and green; the
sentence is Propose tier.)

## Reflection

Two readers of one shape drifted apart, and only the one the human looks
at was wrong. A test that runs both parsers over every record in
`docs/goals/` and compares the PR each one finds would catch the next
drift. The crew added its cases to an existing test file, which the
prompt allowed; nothing there was removed or weakened.
