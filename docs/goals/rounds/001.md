# Round 001: project-identity

- Goal: projects-and-knowledge
- Started: 2026-09-08T15:04+07:00  Ended: open, waiting on a decision
- Sessions: 84633C2C-5809-4326-A446-134ACD09F45F   Archives: none yet
- Base: df35dec (goals/knowledge-base)   Result: f1f6cc9 on goals/project-identity

## Prompt

Capability 1 from `plan.md`, sent as one `send_input` of 3679 bytes: read
`goal.md`, `plan.md`, `LOOP.md`, `facts.md`, `AGENTS.md`; branch
`goals/project-identity`; the seven pieces (store, `PtySession` field, row
fields and reserved labels, `GET /api/projects` and `?project=` filters,
CLI, MCP tool, tests); the authority tiers; the floor; commit, do not
push; post one note.

## Floor

before: green (`swift test` exit 0, vitest 472, bench p95 3.21 ms)
after: green (`swift test` 534 tests 0 failures, vitest 472, bench p95
3.54 ms on port 3418 and 4.17 ms on a scratch daemon; the foreman re-ran
`swift test` and vitest with the same result)

## Effects

- behaviour: the crew read the package, wrote `Projects.swift`,
  `ProjectCommand.swift`, four test files with thirty tests, and touched
  nine existing source files. It stopped at a frozen file and asked.
- visible: `git diff --name-only goals/knowledge-base` lists fifteen
  files, all under the Free tier. The note on the event feed carries the
  floor results and the test list.
- persistent: commit `f1f6cc9`. Verified live by the crew on a scratch
  daemon: a session in this repo reads a discovered project;
  `kitterm project add` registers it; the next `GET /api/projects` reads
  it as registered with no restart.
- deviations the crew stated: the project also resolves from the listing
  row when the kernel cwd differs from the last resolved cwd, because the
  cwd poll runs only while a controller is attached; a worktree outside a
  registered root resolves to that registered project; archive rows carry
  `project`; the `list_sessions` MCP tool gained a `project` argument.

## Gap

class: harness
evidence: `Tests/KittermCLITests/MCPToolsTests.swift:18` pins the MCP tool
count at 15. Advertising `list_projects` needs 16. The file is Frozen under
`LOOP.md`, so the tool is mapped and callable by name but absent from
`tools/list`, and Claude Code cannot see it. A one-line addition stopped
the round.

Runtime candidate, pre-existing:
`LiveTakeoverTests.testSecondTakeoverWhileOneIsPendingIsRefused` lost a
connection once in four full runs and the run hung past 600 s with the
successor daemon alive. It passed 3 of 3 isolated runs. Not touched.

## Decision

propose (`Tests/KittermCLITests/MCPToolsTests.swift`: change the pin from
15 to 16 so `list_projects` can be advertised; an addition, no assertion
weakened. After the human's answer the crew adds the schema entry and its
golden test as this round's one correction.)

propose (`LOOP.md` Authority: a count pin or a golden list in an existing
test is Propose, not Frozen, when the change adds and removes nothing.)

## Reflection

A pin on a count fails on every legitimate addition, and the Frozen rule
treats every existing test file alike. The rule cost one human round trip
for a one-line change. The proposal above narrows the tier. The crew's
own measurement, Foundation path resolution against `realpath(3)`, went to
`facts.md`.
