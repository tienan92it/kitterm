# Round 004: twenty-green-runs

- Goal: green-ci-again
- Started: 2026-09-25  Ended: 2026-09-25
- Sessions: none (a foreman measurement, no crew)   Archives: none
- Base: e343f0b   Result: e343f0b, PR #156 (no code changed)
- Cost: none recorded (no crew session)

## Prompt

None. Capability 4 is a measurement: the foreman reads
`gh run list --workflow=ci.yml --branch main --limit 21`.

## Floor

before: green (2026-09-23, round 3)   after: green (`ci.yml` on `main`
at `e343f0b`, run 36091861633)

## Effects

- behaviour: the foreman counted the `ci.yml` runs on `main`. The last
  twenty are green, from `7c8479d` (run 35574125205, 2026-09-21) to
  `e343f0b` (run 36091861633, 2026-09-25). The run before them,
  `7f30556` (run 35574095458), is the last red one, and it predates
  PR #150.
- visible: eight of the twenty ran after PR #150 merged (`41a3b02` and
  later); twelve ran before it. None of the six flaking cases failed in
  any of the twenty.
- persistent: this record and `STATE.md`.

## Gap

class: none
evidence: twenty consecutive green runs; the five completion conditions
of `goal.md` hold.

## Decision

done

## Reflection

Twelve of the twenty runs predate the fix, so the count proves less than
it says: a flake at one run in thirty can hide in twenty. Rounds 1 to 3
carry the stronger proof, 50 of 50 per case. A later goal that counts
green runs should count only runs after its fix.
