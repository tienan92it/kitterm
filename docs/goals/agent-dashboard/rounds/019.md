# Round 019: the-bill-behind-trailing-lines

- Goal: agent-dashboard
- Started: 2026-09-21T17:31+07:00  Ended: 2026-09-21T17:55+07:00
- Sessions: 96C35A99-A90F-4A95-8367-AB9A7A5C4D32 (claude-fable-5-1)   Archives: 96C35A99-A90F-4A95-8367-AB9A7A5C4D32
- Base: 321ad3c (agent-dashboard/round-18)   Result: 5db4a60 on the same branch, PR #142
- Cost: $7.53 · 7399k in (98% cached) · 47k out · 0h 12m

## Prompt

Found by round 18's own cost line: its transcript's `cost-state` line
sat behind two `queue-operation` records Claude Code appended at exit,
and `TranscriptBill` read any line after `cost-state` as a resume. The
prompt asked for the rule "an `assistant` line after the bill is a
resume; anything else is not", a measured tail size, and a rollup
re-read of the transcripts it had skipped.

## Floor

before: green (321ad3c: swift test 847, vitest 1452)
after: green (swift test 851; Linux docker build 55 s; KittermBench
interactive-echo p95 2.68 ms; tsc clean; vitest 1452; vite build to
scratch)

## Effects

- behaviour: `parseTail` walks the 64 KiB tail backwards over non-turn
  records; a `cost-state` line is the bill, an `assistant` line is a
  resume. Measured over 654 transcripts: bill plus trailing records is
  2686 bytes at most, so the window stays. `TranscriptUsage` carries
  `readerVersion` 2 and the rollup re-reads once any on-disk record of
  an older reader, keeping the days of gone transcripts (a format bump
  would have dropped them). The estimate needed no change.
- visible: four new Swift tests, each failing on the old reader
  (checked by stash). On the real machine: round 18's archive answers
  `hasBill: true`, $5.76; the 30-day USAGE went from 37 to 33 unbilled
  of 217 sessions and from $2,719.93 to $2,904.42 — five finished
  sessions had trailing records; the 33 left are running or died
  without a bill.
- persistent: `Sources/KittermDaemon/TranscriptBill.swift`,
  `TranscriptUsage.swift`, `UsageRollup.swift`, four test files,
  `AGENTS.md`, `docs/architecture.md`.

## Chartered replacements

`testALastLineThatIsNotCostStateIsNoBillYet`: its resume case appended
a `system` line and now appends an `assistant` turn; the
`queue-operation` case joined the new test.

## Gap

class: domain
evidence: the reader's rule was written before Claude Code appended
records after the bill. The loop's own cost line found it.

## Decision

done.

## Reflection

Round 18's cost line could not be written until round 19 shipped; the
foreman read the bill by hand from the transcript for both records and
says so on the line. The running daemon bills the two once it is
upgraded.
