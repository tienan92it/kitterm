# Round 004: four-loop-rules

- Goal: foreman-harness
- Started: 2026-09-10T18:00+07:00  Ended: 2026-09-10T22:35+07:00
- Sessions: 0E38D020-5E5C-4BBB-9049-80393A6AB24B   Archives: the same
- Base: a056b3d (main)   Result: 2c0370e + the foreman's commit on goals/four-loop-rules-work

## Prompt

Capability 4 from `plan.md`, sent as one `send_input`: write the loop's
four unwritten rules into `LOOP.md`, the example template, and the skill,
fold in the three open proposals, and add a check that the three copies
agree. The prompt named which parts of the procedure earlier rounds paid
for and must not change, and told the crew to work in its own worktree.

## Floor

before: green (`swift test` 636 tests 0 failures, vitest 629, bench p95
2.48 ms)

after: red at the crew's last commit, green after the foreman's. At
`2c0370e` `swift test` was 647 tests with 5 failures, all five in the new
check and all five naming `docs/goals/LOOP.md`, the one file the crew may
not write. The foreman applied the patch and re-ran in the crew's
worktree: **647 tests, 0 failures**. Web green (tsc clean, vite build,
vitest 629 in 32 files), bench p95 2.76 ms, Linux build 80.70 s from a tar
of `HEAD`.

## Effects

- **Four rules, in three places that now agree.** A killed attempt is
  separated from a failed round: it spends no budget, writes no record,
  and goes in `STATE.md` under `Failures` as `attempt killed: <ISO date>,
  <what died>`. `resumed-from` carries the id a pane held before an epoch
  change as well as an archive id. The crew posts its note before the
  floor, so a session that dies at its last step still leaves its
  evidence. A session a crew spawns inside a round carries `crew:helper`
  and keeps the round's `goal:` and `round:` labels.
- **Three proposals closed by one paragraph.** Rounds 1, 2 and 3 each hit
  the same family: an assertion pins a text, a count, or a layout that the
  round is chartered to change. The new Authority paragraph rules all
  three at once: such an assertion is Propose, not Frozen, when `goal.md`
  or the item's proof requires the change, and the crew replaces it,
  keeps its intent, and names the line that requires it.
- **The check that keeps them agreeing.**
  `GoalsLayoutDocsTests.testTheThreeLoopFilesCarryTheSameRules` asserts
  five markers over five sources: both `LOOP.md` files, the skill, and the
  two embedded copies. It names the rule and the marker one line at a
  time, so a drift reads as a sentence rather than a diff.
- **Two edits the rules forced that the plan did not name.** The skill's
  Monitor already wrote `resumed-from:<the id the pane held before>`, so
  rule 2 was a Labels-table correction only. Rule 1 reached further than
  its own bullet: Monitor step 4 recorded a *failed round with gap
  `world`* when a respawn did not restore the round, which is exactly a
  killed attempt spending the budget. It now records a killed attempt and
  stops the goal.
- visible: `git show 2c0370e --stat`; the five markers return
  `grep -cF` counts of 1, 1, 1, 1 and 2 in `docs/goals/LOOP.md`.
- persistent: six files. Five from the crew (`examples/goals/LOOP.md`,
  `examples/foreman/foreman-loop.md`, `Sources/KittermCLI/GoalsTemplates.swift`,
  `Sources/KittermCLI/ForemanSkills.swift`,
  `Tests/KittermCLITests/GoalsLayoutDocsTests.swift`) and
  `docs/goals/LOOP.md` from the foreman.

## Gap

class: harness

evidence: **a capability whose deliverable spans the crew's files and the
foreman's file cannot be green in one commit.** Rule 4 of this round says
the three copies of the loop must agree, and the check that proves it
reads a file the crew is forbidden to write. The crew did the only honest
thing available: it left the check failing, wrote the patch in a note,
proved the patch by running it against a copy in a scratch tree, and said
plainly that it had not run the suite green. The split is real and it is
correct; what was missing is a name for it, so the red floor reads as the
handoff it is rather than as a failure.

## Decision

done. Capability 4 is complete on `goals/four-loop-rules-work`: `2c0370e`
from the crew, the foreman's commit on top. Three proposals close with it.
The session is archived.

## Reflection

The crew proved a patch it was not allowed to apply. It ran its own
recipe against a copy of the frozen file in a scratch tree and diffed the
result against the file it intended, byte for byte, then handed over the
one command that reproduces it. The foreman ran that command and the
floor went green on the first try, with no reconciliation. That is the
pattern for every split-authority capability, and it is now the fact:
**when a round needs a change under a path it may not write, it delivers
a command that makes the change and the evidence that the command is
right, not a description of the change.**

The cost that a rule can prevent is the red floor with no name. The gap
above is written as a proposal, so a later round finds the handoff
already described.
