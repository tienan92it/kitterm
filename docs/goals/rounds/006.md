# Round 006: knowledge-on-dashboard

- Goal: projects-and-knowledge
- Started: 2026-09-08T18:32+07:00  Ended: 2026-09-08T19:05+07:00
- Sessions: 80159800-28D4-4675-8E38-E1888C9BFBC0   Archives: 80159800-28D4-4675-8E38-E1888C9BFBC0
- Base: 5fa5727 (goals/knowledge-on-dashboard, off main a64908e = v0.23.0)   Result: cf001d6, 533c4fa, bf15458, 61a7c45

## Prompt

Capability 5 from `plan.md`, sent as one `send_input` of 5668 bytes: read
the package, corpus request 02, rounds 001 to 005, and the watch-grade
decision in `STATE.md`; work on the existing branch; the six pieces (the
jailed read-only knowledge route, the parsed summary route, the card with
goal, round, status, next action, proposals, `goal:` sub-headers and
round chips, proposed items in the strip, docs, tests, the corpus
verification with screenshots); the authority tiers; the floor; commit,
do not push; post one note under 1900 bytes.

## Floor

before: green (`swift test` 558 tests 0 failures, vitest 559, bench p95
3.16 ms) on the v0.23.0 daemon after the in-place upgrade
after: green (`swift test` 580 tests 0 failures, vitest 570 passed in 32
files, bench p95 2.53 ms; the foreman re-ran `swift test` and vitest with
the same counts). The Linux build passes alone; it aborted once while
`swift test` shared `.build`, a fact for the next builder.

## Effects

- behaviour: the crew wrote `KnowledgeFile.swift` and
  `KnowledgeSummary.swift`, the two routes in `HTTPAPIHandler.swift`,
  the card and the model functions in the fleet view, and the docs. Four
  commits, 33 minutes, no correction. It posted three draft notes before
  the final one; the final note says which is final.
- visible: `round6-sessions-1200.png`, `round6-sessions-390.png`,
  `round6-proposed-390.png` in the job's `round6/` directory. The kitterm
  card reads the goal title, `proposals: 12`, `round 005`, the status
  line, and the next action; the fixture card reads `round 2 of 3` with
  a `goal: projects-and-knowledge` sub-header and a `round 2` chip; a
  `proposed` item sits in the strip with a link to the round record.
- persistent: eleven files changed against `main` beyond the state
  commit. Tests added: `KnowledgeRouteTests` (12: `..` plain and encoded,
  `./`, `//`, absolute answer 400; a symlink out, a symlinked directory,
  a symlinked knowledge directory answer 404; nested 200; 413 over the
  cap; the two content types; 404 for missing, directory, unknown id, no
  knowledge directory; the summary and its ETag 304; a discovered
  project; a watch token 200 and no token 403; zero store calls on the
  loop), `KnowledgeSummaryTests` (10, fixtures plus this repository's own
  package), `sessions-knowledge.test.ts` (12). Corpus request 02 verified
  on a scratch daemon on port 3499 with a fixture clone at `Round: 2 of
  3`: `../../Package.swift` answered 400, a watch token read `STATE.md`
  as `text/markdown`, repeated polls answered 304.

## Gap

class: none
evidence: the corpus request passed on the first run; the proof column's
jail cases and the parser fixtures are delivered. The summary's
`proposals` count reads 12 for this package because it counts every
bullet under the heading, which now includes decided items marked
"Decided"; the count is right for the file as written. Prune decided
proposals from `STATE.md` when the human takes them.

## Decision

done. The item `knowledge-on-dashboard` is complete on
`goals/knowledge-on-dashboard` at `61a7c45`, not pushed. The session is
archived. The budget of this direction is spent: rounds 4, 5, and 6.

## Reflection

The first round on the released v0.23.0 daemon: the crew's own row
carried `project: kitterm` while it worked, which is capability 1 in
use. The summary route parses two lines and one section from `STATE.md`,
so the state file's shape is now an interface; a change to the `- Round:`
line or the `## Next action` heading breaks the card. That belongs in
`LOOP.md` as a rule for the record shape. The review gate rule applies
before this branch is pushed.
