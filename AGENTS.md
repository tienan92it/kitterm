# AGENTS.md — kitterm

Guidance for coding agents working in this repo.

## Product shape

- **Browser terminal only** — xterm.js client served by a Swift loopback daemon
- Tab open = new shell; session id in `sessionStorage` keeps "tab = shell"
- Transient disconnects (sleep/wake, reload) **detach** the PTY: every output byte flows through a per-session **4MiB ring with absolute stream offsets** (`SessionLog`) — detached reads never pause, the ring rotates. The client counts received bytes and reconnects with `?since=<offset>`; the daemon replays exactly the gap (or the full ring + a resync flag when the offset rotated out, announced via the `logState` frame). Client auto-reconnects with backoff + on focus/online/visible; unreattached sessions are reaped after 5 min (suspending clock)
- **Sessions are URLs**: `/?cwd=<path>` deep-links a new shell (`kitterm open <path>`); `/?session=<uuid>` joins a session — first client is controller, later ones are read-only observers (128KB replay tail, resize broadcast, share button copies the link); `/?hist=<key>` selects a per-pane history file. Observers get a "Take control" overlay (client opcode 5) — live handoff, no reconnect; the demoted side flashes "Another device took control" and keeps watching
- **Session profiles** (`SessionProfiles.swift`): `~/.kitterm/profiles.json` names connect commands (`ssh vm`, `docker exec …`). `/?profile=<name>` spawns a login shell and injects `command\n` as type-ahead input (pre-reader `PtySession.write`, flushed at adoption) — visible, echoed, in the pane's history; if the transport exits you land back in the local shell. The URL carries only the *name*; the command comes only from the file. Reattach ignores the param; a respawn after daemon restart re-runs it. Splits and ⌘⌥T inherit the profile. The fleet view starts no shell (round 11 of `agent-dashboard`) and reads `GET /api/profiles` only to learn whether its token is watch-only; unknown names close 1008 with the reason. In-band OSC (133 marks, 9/777 notifications, OSC 7 cwd) flows through any transport, so remote shells stay fully legible — `kitterm integrate | ssh vm 'cat >> ~/.zshrc'` installs the snippet remotely
- **Fleet view** (`/sessions`: `Web/terminal/src/sessions.ts` paints what the pure, tested `sessions-model.ts`, `sessions-value.ts` and `sessions-tree.ts` decide — `levels`, `goalBuckets`, `band`, `attention`, `rowLine`, `taskLines`, `quotaPanel`, `usagePanel`, `usageHead`, `valuePanel`, `wherePanel`, `modelsPanel`, `leakLines`, `tree`, `visibleLines`; the design is the two frames `Dashboard 1200` and `Dashboard 390` of `docs/goals/agent-dashboard/corpus/dashboard.pen` (the single source of truth; the foreman exports a frame to a scratch directory when a crew needs it), with its `Components` frame for the quota bar, `WHERE` at each filter and the row marks, on the contract in `corpus/design-foundation.md`, all frozen). The page presents and monitors; it accepts no typed work and holds no action: there is no text field, no `[send]`, no reply line, no `[new]`, no `[Dismiss]`, no `[Allow]` or `[Deny]`, no `⋯` menu and no actions cell anywhere on it, and `Open the pane` is the way to act on an agent (`sessions-no-input.test.ts` pins both absences). The only buttons are the radio toggles, the push switch and the disclosure triangles. Top to bottom: **the band** (`band`): the brand `kitterm` at the left and four counts at the right, each a headline-size number with a body-size noun — `2 working` with the number in the accent, `4 need you` in the amber (every pending approval, every `needs-input` row, every `failed` row, and one item per open goal whose `STATE.md` counts proposals: `withProposed(attention(rows, approvals), proposedItems(...))`; the same count is the title badge; the noun is `<a href="#needs-you">` to the first marked line, `NEEDS_YOU_ID`), the range's spend in whole dollars `$2,316 30d`, and the session window's share with its countdown `19% quota · 3h 16m` (the most-used window when the reading has no session window; `–` with no reading). One fixed height, `--line-h` at 768 px and up and three lines below it, where the brand takes a line and the counts wrap to two rows of two; the box clips (principle 2). **The panels**, each a label in an 84 px gutter beside its content, stacked with no gutter on a phone, in the order `USAGE`, `QUOTA`, `MODELS`, one hairline divider (`.panel-divider`), `VALUE`, `WHERE`, `LEAKS` at both widths (a phone also draws a hairline above `MODELS`, the only labelled panel there); `QUOTA`, `MODELS` and `WHERE` share one name column (`--name-col`, 140 px, 92 px on a phone) so every bar starts on one x, and `MODELS`'s last column is `WHERE`'s (`--last-col`, 108 px) so the right edges line up: `USAGE` (`usageHead`, `usagePanel`; `GET /api/usage/daily` for the chosen range, asked every 30 s and at once on a toggle): the amount at the headline size with `4.43B tokens` and `20.9 h model time` beside it, the two radio groups `[x] cost [ ] tokens` and `[ ] 7d [x] 30d [ ] 90d` at the right (`localStorage` under `kitterm.sessions.usage`), one SVG bar per day in the accent with a `<title>` per bar and one sentence for a screen reader (`usageChartName`), the axis, and one note, `$1,446.00 apportioned across midnight` (`apportionedNote`; the rollup's age is the label's tooltip); a phone prints `30 days` in place of the facts and drops the peak caption and the note. `QUOTA` (`quotaPanel`, `GET /api/usage/limits`): one line per window, `Session (5h)` | a bar `--quota-bar` wide (220 px, 160 on a phone) drawn as a track on `--ui-surface` with the fill a `.mark.bar` | `19%` | the reset as a clock time in the viewer's zone, 24-hour, `resets today 20:20`, `resets tomorrow 04:00`, `resets Sep 25, 04:00`, `resets Jan 2 2027, 04:00` (`quotaResetTime`, round 17; today and tomorrow are counted between local midnights; the band alone keeps the `3h 16m` countdown), the reading's age after the last line; **at 80 % and over the fill and the percentage wear `--ui-caution`** (`quotaLevel`, `QUOTA_CAUTION_PERCENT`; the percentage is then a `.mark.caution.wide`), the label and the reset never change colour, a stale reading's fill is grey; **a window past its `resets_at` keeps its last value**, its fill and its percentage in the faint grey (`.mark.idle`), and its reset cell reads `reset · read 1d 19h ago` (`quotaReadAge`, from the reading's `ageSeconds`), never `resets … ago`; a phone prints the short label, the bar and the percentage; with no reading the content cell holds the sentence that says so. `VALUE` (`valuePanel`, `GET /api/yield` and the rollup): four tiles behind a 2 px accent rule — `merged pull requests`, `merged lines added`, `releases`, `hours of model time` (`merged PRs`, `merged lines`, `releases`, `model hours` on a phone), each with its unit cost, `$11.77 each`, `$47 an hour` — and one note, `kitterm, 30 days. Proxies for value, not value.` (`kitterm, 30 days` on a phone): the scope is the one counted checkout's name or `N repositories`. `WHERE` (`wherePanel`): the grouping selector kept in `localStorage` under `kitterm.sessions.where`, `goal` by default, with the counted checkouts' line at its right, `$976.74 in kitterm · 83 merged PRs · 58,853 lines · 24 releases`; then one row per group with four columns as the `Components` frame draws `WHERE` at each filter — spend, a count (`10 goals` at project, `6 tasks` at goal, the task's working time at task, `45m`, `1h 2m`, its rounds' wall time from their `Cost:` lines summed as `countdown` prints it, a dash with none; a role's share of the range's spend, `86%`, and the remainder's, `85%`, the same arithmetic; a project's sessions sit in its tooltip), the pull requests (`83 PRs`, `4 PRs`, `PR #124`, a role's API hours as `34.0 h`) and a unit cost, the noun after a slash with no article: `$11.77/PR` at the project grouping (the spend over the merged pull requests), `$0.018/line` at goal and task (from the lines the named pull requests added, `pullRequests` on the yield), `$61/API hour` at role, in whole dollars — and the note `85% names no round, so it cannot be valued`. The goal grouping lists every goal, a goal with nothing in the range as a dash row of its own, and has no `N more goals` row (round 13, the human's word); the task grouping folds the rounds with no `Cost:` line and no pull request behind `N more rounds`. A row with no source prints a dash, never a zero; the unattributed remainder (`no round record`, `no project`) is a row whose name, bar and spend wear the amber, the longest bar at the goal grouping; a bar is a `.mark.bar`. `MODELS` (`modelsPanel`): the top three by cost and one summed row, `Others`, last, whose title names the rest; name, bar, spend, sessions; a phone prints the spend in whole dollars and no count. `LEAKS` (`leakLines`): two lines, `? 37 of 51 rounds carry no Cost line` and `· 3 corrections in 51 rounds · 3 sessions under 95% cached, $33.54`, the rounds being the range's. **Every figure on the page follows the one range the `USAGE` toggles set** (7d, 30d, 90d; round 13), the `from` and `to` the rollup answers: the band's spend, `VALUE` and the `WHERE` summary line (the rollup and `/api/yield` are asked for it), `MODELS` and the role rows (the rollup's splits), a project's and a workspace's tree cost (the rollup's buckets), and every figure read from a round record — a goal's spend in `WHERE` and in the tree (`goalCost`), a task's row, the `LEAKS` round counts — which `roundsInRange` (`sessions-model.ts`) filters by the record's `- Started:` day; a record with no start day is in no range. A toggle change asks the two routes and repaints all of them at once (`sessions-range.test.ts` per panel, `sessions-range-page.test.ts` for the page). `WHERE` and `LEAKS` are absent on a phone; nothing folds. The cache share is on no heading; no quality rate, no rework rate, no dollar value for the human's time. Then **the tree**, under a hairline and its header `SESSIONS` with the whole state vocabulary — `◐ [working] ? [needs you] • [pending] ✓ [done] ! [failed] – [idle]` (`VOCABULARY`; not on a phone) — as flat lines (`sessions-tree.ts`, `tree`): no card, no surface fill, no rule per line, one hairline between top-level sections. **Every line is one line, five cells**: the mark at the far left of the page, one `--space-4` of indent per level (`--space-3` on a phone) on the name, the name (the only cell that truncates), then four right-aligned fact columns of fixed width — the state word, **the cost in the range and nothing else** (round 15, the human's word: a workspace's and a project's from the rollup's buckets, a goal's from its rounds in the range (`goalCost`), a task's from the `Cost:` line of the round its `STATE.md` line names (`taskCost`), a session's from its transcript bill (`sessionCost`, `GET /api/sessions/<id>/cost`, asked every 30 s for a row with an `agentTranscript`, counted when the bill's `startTime` day is in the range; a running session has no bill yet and prints the route's estimate as `~$4.20`, round 16, with `estimated from 12 turns · updated 12s ago` as the cell's tooltip (`sessionCostTitle`), the bill with no `~` once it lands, and the estimate on no figure above the line: the project's tooltip says `+ ~$4.20 running` (`runningEstimate`)), the dash `–` (`NO_FACT`) where the level has none, and no cost column at all with no rollup), a pull request (a task's `PR #124`) or a session's model, a trailing fact (a time, `1 agent`) — so the columns line up; `round 6` and a goal's `r6/3` are the name's tooltip (`taskTooltip`: `round 6 · PR #124`; `goalTooltip`: `r6/3 · next: …`; a goal whose proposals wait keeps the proposals as its tooltip); there is no actions cell; on a phone the columns give way to the state word and the cost (`data-narrow`; the time with no rollup). **`PR #124` is a link** (`pullRequestLink`: `<a class="pr-link" href="https://github.com/owner/repo/pull/124" target="_blank" rel="noopener">` around a `.mark.link.wide`, the accent as a mark's background with an underline like the band's `need you` link) wherever the page prints a pull request number — a task line, a `WHERE` task row (`unitsHref`) — when the project carries `pullRequestBase` from `GET /api/projects`; a project without one prints the number as plain text (`sessions-pr-links.test.ts`). **The marks per level** (rule A of the `Components` frame): a workspace wears no mark; a project and a goal wear the disclosure triangle alone, `▼` open and `▶` closed, in `--ui-text-faint` at `--mark-size-disclosure` (11 px), never a state, blank when nothing sits under the line (the cell is `--line-h` tall and centres its glyph, so a fold's `span` and a goal's `button` sit on one centre; a fold's `summary` is a `.line` with no layout of its own); a task and a session wear their state mark at `--mark-size` (14 px) in the 16 px mark column. The triangle is a `button.mark.disclosure` with `aria-expanded`; a click folds or opens the line's children (`toggledRows` in `sessions.ts`, in memory, the keys whose default the reader flipped; every row is open on load but a done goal inside the `N done` fold, which starts closed (`closed`, `isOpen`); `visibleLines` hides every following line at a greater depth), so a closed goal hides its tasks and their sessions and a closed project hides everything down to its `N done` fold. The levels (`levels`): a **workspace** is the parent directory of a project root, headed only when it holds two or more projects, an `h2` with the range's cost summed over its directory (`workspaceUsage`) and its working sessions, `1 agent`; a **project** line is an `h2` or `h3` with the triangle as its mark (no spinner: a working agent is its `1 agent` fact), its cost (`projectUsage`), its agents (the root path and "no goal folder" or "not registered" are the name's tooltip; the page starts no shell); its sessions under no goal; then its **goals** (`goalBuckets`, working first, no label over a bucket): each a line whose mark is the triangle (blank with nothing under it), whose name links the latest record else `STATE.md`, with the state word (`goalTag`: `[working]` when a live session carries the goal's slug in a `goal:` label, `[pending]`, `[waiting]`, `[stopped]`, `[needs you]` when its proposals wait, with the count on the name's tooltip; there is no dismissal, so a proposal counts until the goal's next round) and its cost (`goalCost`: the `Cost:` lines of its rounds started in the rollup's range, the dash with none; no rollup, no cost); its counter (`roundCounter`, `r6/3`) and the next action are the name's tooltip. **Under each goal sit its tasks, the fourth level** (`taskLines`, `shownTasks`): every task that is not done and the first two done ones, from `## Queue`, `## Failures`, `## Done` in that order — the mark, the slug, `[working]` when a session the project owns carries `task:<slug>` under `goal:<the goal's slug>`, its round's cost, `PR #N` as a link, with `round N · PR #N` on the name's tooltip — with the sessions that carry the label under the task. **The project's most recently finished goal shows open** with its last two done tasks (`latestDoneGoal`, the done goal whose latest round started last; a done goal a live session still labels shows open too, with the session under it), and the other done goals fold behind `▶ N done` at the goal's indent, each a line of the same shape inside: a done goal with tasks wears the triangle, closed, and opens to all its tasks; one with no task wears no mark (rule A, round 14). A **session** is one line (`rowLine`): the mark in its state's colour (the spinner turning for a working one, the `✓` in `--ui-success` for a done one), the name, `[working]`, `[needs you]` for both a hook report and a pending approval, `[done]`, `[failed (1)]`, its bill in the cost column (`sessionCost`), its model in the third (`rowModel`), how long since its output in the last; what it is doing and where it sits are the name's tooltip; naming, archiving and killing happen through the API and the pane, not here. A pending approval is a `.line-approval` in the same row under it: the amber mark, `approve <tool>`, the arguments (the one fact that drops, `fitLines`, `keptFacts`), how long, and `Open the pane`, where it is answered; an approval whose session is gone gets a row under "No project" (`orphanApprovals`). **Idle shells are not lines of the tree** (`isIdleShell`: `idle`, `exited`, no integration): they fold at the page's foot. **The folds** sit under a hairline on one line: `▶ Archived (61)` (every archive, page-level), `▶ 3 idle shells` (each a session line inside), and the push switch `[ ] Notify this device` (`pushToggle`: hidden for a watch client, disabled with the reason beside it for an unsupported browser, an http origin, a daemon before `GET /api/push/vapid`, or a `denied` permission); a phone stacks them. **The marks are characters** coloured through `background-clip: text` (`markGlyph`; the spinner in `spinner.ts` is the quadrant cycle `◐ ◓ ◑ ◒` at 90 ms, the only cycle, resting on `◐` under `prefers-reduced-motion` and in the legend; nothing is measured, because the mark's 16 px column is fixed), and the owned colours paint a mark's `background` and nothing else, never `color`: a mark is a one-character glyph, a bar, the tile's 2 px rule, or a run of text that carries a state (`.mark.wide`: the band's two counts, the quota's percentage at caution, the remainder's name and spend). **The surface is the terminal's and the sheet carries the design foundation** (`sessions-css.test.ts` pins both): the page-scoped `:root` in `sessions.css` defines the five space tokens, the three `font` shorthands, the two mark tokens (`--mark-size` 14 px, `--mark-size-disclosure` 11 px), `--line-h` (44 px below 768 px, 28 px at and above it) and the five owned colours (`--ui-accent`, `--ui-success` #52b788 dark / #2d6a4f light, painting the `✓` mark alone, `--ui-warning`, `--ui-danger`, `--ui-caution` #f4a261 / #b8641f, the quota at 80 % and over), while the ground and the three greys keep deriving from `tokens.css`; no rule writes a bare pixel for `margin`, `padding` or `gap`, the only `font-size`s are the two mark tokens on `.mark` and `.mark.disclosure`, no `border-radius` above 2 px, no shadow but a hairline, no `transition`, no `animation`; a toggle or a radio prints `[ ]`/`[x]`. Poll: `/api/projects`, `/api/sessions`, `/api/approvals`, `/api/archives`, `/api/usage/limits` every 2 s, with the `/api/events` long-poll as the repaint trigger; the signature that gates a repaint holds the tree's shape, not `lastOutputAt`. Every control carries a `data-focus` key, restored across repaints. A watch-only token (403 on `/api/profiles`) hides the usage, the quota, the four value panels, every cost, and the push switch. The pane's settings panel links to the page. No framework; the page uses the `--ui-*` tokens, so it follows the terminal theme, and `theme-contrast.test.ts` derives and pins every colour pair it paints
- **Split panes** (client): one browser tab holds a binary tree of panes (⌘D / ⌘⇧D split, ⌘⌥↑↓ / click focus, ⌘⌥T new browser tab in the focused pane's cwd). Layout + per-pane `{sessionId, cwd, histKey}` persist in `sessionStorage`; a reload restores the tree and reattaches each pane. Daemon is unchanged — N panes are just N WebSockets
- **Restart resilience**: the daemon polls each shell's cwd via `proc_pidinfo` (~2s, diff-gated) and pushes `cwd` frames, so a restored pane respawns where it was even when the shell emits no OSC 7. Each pane's `?hist=<key>` maps to `~/.kitterm/history/<key>` (set as `HISTFILE`, seeded once from the user's global history), so up-arrow survives a restart with that pane's own commands. A session's name and labels are written to `~/.kitterm/respawn.json` as they change (`RespawnHintStore`, called from the registry actor, capped at 256 entries, entries dropped on removal); a reattach whose id is gone consumes them, so the respawned shell keeps them and a foreman finds `crew:alpha` again under new ids
- **A run says how it ended**: the daemon writes `~/.kitterm/last-run.json` (`LastRun`, `"version": 1`) — `pid`, `startedAt`, `aliveAt`, `sessions`, and on a clean end `endedAt` plus `reason` (`stopped`, `restarted`, `takeover`). `runDaemon` begins the run, reading the previous record first because writing is what destroys it; `DaemonServer.stop()` records `stopped` with the session count taken before the shells go; `prepareHandoff` records `takeover` before the `exec`, so a live upgrade never reads as a death. A 30 s repeated task (`KittermConstants.lastRunRefreshSeconds`) refreshes `aliveAt` and `sessions` from the registry inside a `Task`, off the event loop and off the output path. A run the kernel kills leaves no `endedAt`; that absence is the signal, and it is why `aliveAt` exists — it names when the run was last alive to within one interval. Nothing writes `restarted` yet: `kitterm restart` reaches the daemon as the same `SIGTERM` `kitterm stop` sends
- `--lan` binds 0.0.0.0 with token auth for non-loopback peers (`?token=` → cookie; `~/.kitterm/token`); loopback stays trusted. Tokens carry a **grade**: `full` (everything) or `watch` (observe + read API only — never input, never `requestControl`, never spawn; the WS handler takes a watch-only path that can't reach `spawnNew`, and `POST …/input` answers 403). Three token kinds: ephemeral control (`~/.kitterm/token`), ephemeral watch (`~/.kitterm/token-watch`, powers the 👁 watch-link share button via `/api/lan` `watchToken`), and named persistent tokens (`kitterm token create <name> [--watch]`, SHA-256 hashes only in `~/.kitterm/tokens.json`, mtime-cached reload so revocation needs no restart). The auth cookie is set to exactly the presented token — a watch token can never ride a full-access cookie
- `--record` writes asciinema v2 casts to `~/.kitterm/recordings/`
- **Session labels** (`/ws?label=run:abc,node:build`) tag a session so a fleet of shells is attributable to graph nodes. They also declare intent: a labelled session was made by a program, so the registry holds it for `orchestratedSessionLingerSeconds` (1h, `--session-linger`, capped at 24h and validated in `start` where the user can see the error) after its last client leaves instead of the browser's 5 minutes — an orchestrator that crashes and restarts finds its nodes still running. **The clock reaps an idle shell, not a working session** (ADR 0002): at the end of a window, an orchestrated session with a program other than the shell in the foreground (`PtySession.foregroundIsShell`), or with output during the window, gets a fresh window, and the check repeats. A foreman never attaches, so before this rule every crew session was reaped on schedule while `claude` worked in it. An orchestrated shell at its prompt that printed nothing for a whole window still goes, an exited session still goes at the end of its window, and a browser tab's session keeps its 5 minute rule. A program a foreman forgot is held until `kill_session` or `DELETE`; `maxConcurrentSessions` is the bound. **The daemon says which sessions it holds, and never judges them**: the row carries `heldSince` (epoch millis; the first window the session survived, set once, cleared when a client attaches or the session goes), and every survival appends `session.lingered` to the feed with `reason` (`foreground` plus `program`, the row's `foregroundProgram`, from `PtySession.foregroundProgram`, or `output`) and `heldSince`. A foreman compares `heldSince` with its own tolerance and ends what it forgot (`docs/foreman.md`, step 5). `SessionLabels` validates strictly; a malformed tag is dropped rather than failing the connection
- The LAN and watch tokens **persist across restarts** (`~/.kitterm/{token,watch-token}`, 0600). They used to be minted every start, which revoked every share link whenever the daemon came back — including for a routine restart or upgrade, at the moment you are least likely to be at the machine that could print you a new one. A stored token is only trusted if it is owner-readable and looks like one this daemon wrote; anything else is replaced, so a damaged credential fails closed. `--rotate-token` is how you revoke
- `--retain-logs` spills each session's output to `~/.kitterm/logs/<id>.log` (0600, 64 MiB cap, rotating, deleted when the session is reaped) so `/commands/<n>/output` still answers for ranges the 4 MiB ring dropped. **Off by default** — shell output on disk, same reason `--record` is opt-in. All I/O is a `queue.async` hand-off, never awaited on the loop. **The file does not start at stream offset 0** (a shell prints before the session has an id to name a file after), so `SessionLogStore` tracks an `origin` and translates; assuming otherwise returns plausible wrong bytes
- Tab title is per-session (custom name + optional cwd folder), stored per session id; observers adopt the controller's title and cannot edit it. A session created or renamed over the API carries a **server-side name** (`POST/PATCH /api/sessions`), pushed on the `title` opcode; it fills in the tab title when the client has no custom name of its own (the local name still wins)
- `kitterm service install` runs the daemon from a per-user LaunchAgent — see **Service** below
- **Live upgrade** (`kitterm upgrade --live`, `docs/live-upgrade.md`): the daemon execs the staged binary in place. Same pid, same launchd job, same shells; only the sockets die and every client reconnects with `?since=` and gets the exact gap. `POST /api/upgrade/takeover` (loopback + full grade, no body) validates the target with `--help`, answers 200, then the daemon quiesces on the loop, writes `~/.kitterm/takeover/{state.json, rings/<id>.bin}` (`TakeoverState`, `formatVersion` 1: the old binary writes, the new one reads, unknown fields ignored, unknown version refused), sets `FD_CLOEXEC` on every fd but the carried masters (swift-nio sets none on Darwin sockets), and `execv`s `serve … --takeover <dir>`. The successor adopts each master (`PtySession(adopting:)`, no `openpty`, no spawn), restores the ring, the marks, `heldSince`, the recorder and the retained log by path and offsets, registers every session detached with no `session.created`, and continues the event feed in the **same epoch** with `daemon.started {takeover: "true"}`, so a foreman keeps every id. Rungs of the failure ladder: a refused route changes nothing; an `execv` that returns serves on from the same objects (`DaemonServer(config:carrying:)`); an unreadable state boots clean and closes the listed fds; a crash after `exec` is launchd's `KeepAlive`, which is why the CLI refuses `--live` without the service and on Linux. Not carried, by decision: sockets, approval holds, waiters, input in flight, the mark scanner's mid-sequence state
- **Foreman** — `kitterm mcp` is a stdio MCP server that hands an ordinary agent the crew-management toolset (spawn/name/watch/message/wait/note over the HTTP API); the daemon holds no orchestration logic. `docs/foreman.md` is the operating manual and `examples/foreman/` holds reference skills, embedded in the binary as `ForemanSkills` (golden test against the files) and installed by `kitterm skills install` into `~/.claude/skills/<name>/SKILL.md`. The boundary is deliberate: kitterm ships the machinery, the foreman is a prompt
- **The daemon does not emulate a terminal.** No grid, no cursor, no CSI/SGR, no character sets — screen state is entirely the client's business. The one bounded exception is `OscMarkScanner`, which matches OSC 133/633 in the output stream so shell-integration marks exist for sessions nobody is watching (an orchestrator-driven session used to record none, leaving `/api/sessions` state, `lastExit` and `/commands` empty). It scans for two ids and their terminators and nothing else; it runs outside `stateLock` on the event loop, and its cost is below KittermBench's noise floor because it `memchr`s for `ESC` rather than walking bytes. A **rendered screen** exists, but not here: `read_screen` (the MCP bridge) fetches the raw ring tail plus the pane's size from `GET /api/sessions/<id>/output` and renders it in the CLI process with `KittermScreen`, a bounded VT subset that only `KittermCLI` links — `KittermDaemon` has no dependency on it, so the build enforces the rule. `docs/adr/0001-read-screen-in-the-bridge.md` records the decision and the option it closes
- **No Node on the hot path** (daemon is Swift + NIO)
- No native Mac app
- PTY spawn uses `kitterm-spawn-helper` (must be beside `kitterm`) so the shell gets a controlling TTY — required for Ctrl+C → SIGINT

## Binary protocol (`KittermProtocol`)

| Dir | Byte | Payload |
|-----|------|---------|
| C→S | `0` | UTF-8 / raw input |
| C→S | `1` | `cols:u16` `rows:u16` (big-endian) |
| C→S | `2` / `3` | pause / resume (empty) |
| C→S | `4` | mark — **deprecated and ignored.** The daemon reads marks out of the PTY stream itself (`OscMarkScanner`), so an unwatched session still has an index; accepting these too would double every mark for an older client still sending them. Payload kept in the codec for old clients: `kind:u8` (0 A, 1 B, 2 C, 3 D) `exit:i32` BE (Int32.min = absent) `offset:u64` BE `cmdline:utf8` (≤2KiB) |
| C→S | `5` | requestControl (empty) — an observer takes over: the current controller is demoted to observer, both get fresh `role` frames. The whole swap runs in one event-loop tick (`ControlHandoff`, loop-confined), so no output/input can interleave mid-swap; the promoted side attaches `.sinceOffset(logHead)` = zero replay |
| S→C | `0` | raw PTY output |
| S→C | `1` | title UTF-8 |
| S→C | `2` | session meta (length-prefixed fields) |
| S→C | `3` | cwd UTF-8 |
| S→C | `4` | exit code `i32` BE |
| S→C | `5` | session id UTF-8 (reattach via `/ws?session=<uuid>`) |
| S→C | `6` | resize `cols:u16 rows:u16` BE (observer follows controller size) |
| S→C | `7` | role `u8` (0 controller, 1 observer) |
| S→C | `8` | logState `flags:u8` (bit0 resync) `offset:u64` `replayLen:u64` BE — sent once per attach, before any replayed output |

Flow-control defaults: ~2ms / 64KB batching, PTY pause at 4MB buffered outbound, resume at 1MB, hard close at 64MB.

## HTTP API (JSON off the I/O stream; read-only unless noted)

- `GET /api/health`, `GET /api/lan`, `GET /api/sessions` (mark-derived state per session; `?label=run:abc` filters, and each row carries its `labels`, its `name` and `note` when set; a filter that is not `key:value` is a 400, because an empty list would read as "that run has no sessions"). Each row also carries a **`mergedState`** — the crew vocabulary `working / needs-approval / needs-input / completed / failed / idle / exited / unknown` — plus `agent {status, message?, at}` (the session's latest hook report), `pendingApproval`, and `lastOutputAt`. `mergedState` is `MergedSessionState.merge`: after the two hard facts (`exited`, `needs-approval`) **the newer evidence source wins** — the latest hook report or the latest state-bearing mark. That is what makes a shell running `claude` work: its marks say "running" for hours (one preExec, no commandEnd until claude exits), every hook report is newer, so `working`/`needs-input`/`completed` track the agent's real turns; a human's later command produces newer marks, which then win. Never a state machine the daemon advances, so a session with no Claude hooks collapses to the exact `running/idle/unknown` (`state`, still sent) it always had. `lastOutputAt` is a fact for a caller's own "stuck" judgment; the daemon ships no such judgment itself. **`foregroundProgram`** is the name whatever took the terminal from the shell was started under (`claude`, `vim`, `cat`), and is absent when the shell itself is reading or nothing holds the tty yet — a pane running `claude` and a bare `zsh` used to look the same in `list_sessions`. The name is the leader's `argv[0]` (ADR 0004: `KERN_PROCARGS2` on Darwin, `/proc/<pid>/cmdline` on Linux), last path component, less a login shell's `-`, with the executable basename as the fallback — `~/.local/bin/claude` execs `~/.local/share/claude/versions/2.1.263` and keeps its pid and argv, so the executable path read `2.1.263` where the row must say `claude`. It is the same `tcgetpgrp` read as `foregroundIsShell`, made when the row is built, so the output path never pays for it and the value is never stale. **`agentSessionId`** and **`agentTranscript`** are the join to the Claude Code session that ran in the shell: every hook payload carries `session_id` and `transcript_path`, and `handleHookEvent` keeps both on the session (`PtySession.recordAgentJoin`, an `AgentJoin`) from every hook, blocking or not, so whichever of the four arrives first sets them and a second `claude` in the same shell replaces them. The path is stored as the hook gave it — absolute, under `~/.claude/projects/` — the way the row's `cwd` and `shell` are; the cost route reads its last line and the session list reads its tail for the model (**`agentModel`**, below). Both fields or neither: a session that never ran `claude` has neither, and a hook with half a join changes nothing. They survive a live upgrade with the session **`agentModel`** and **`agentModelName`** are the model the session runs on: the `message.model` of the last `"type":"assistant"` line of `agentTranscript`, as Claude Code wrote it (`claude-fable-5-1`) and as the fleet view prints it by the naming rule of `docs/goals/agent-dashboard/corpus/design-foundation.md` (`Fable 5.1`, `Opus 5 · 1M`, `Haiku 4.5`; an id the rule does not fit prints unchanged, `ModelName`). Absent, both, until the session has an assistant turn, and on a `"<synthetic>"` line: never a guess. `TranscriptModel` reads one 256 KiB `pread` of the tail on `TranscriptBill.queue`, off the loop and off the registry actor (measured 2026-09-18 over the 40 newest transcripts: the last assistant line sits within 4 KiB in 3, within 64 KiB in 39, within 256 KiB in all 40), and `TranscriptModelCache` keys the answer by path with the file's size and mtime, so a session list costs one `stat` per session with a transcript and a read only of a file that grew; a grown file whose window holds no assistant line (a long tool result) keeps the model the path last yielded.
- **Projects** (`Projects.swift`): every session row carries **`project {id, name, root, registered}`** and **`orchestrated`** (true for a labelled or API-spawned session). The daemon resolves the project from the session's cwd: the nearest ancestor of the cwd that is either a git checkout or a registered root in `~/.kitterm/projects.json` (`{version: 1, projects: [{id, name, root, knowledge?}]}`, `knowledge` defaults to `docs/goals`, written by `kitterm project add|init|list|remove`; `kitterm goal new|list` reads and writes the goal folders under it without touching the file, reloaded on mtime change like `tokens.json`) wins. The walk looks for `.git`, following a `gitdir:` file one level so a worktree resolves to its main checkout, bounded at 32 levels; the checkout names the project unless a registered root sits inside it, nearer to the cwd, or is the checkout itself. Outside every checkout the longest registered root that is a prefix of the cwd wins; else no `project` field. A registered root is a name and a configuration for one root, not a claim over everything beneath it: a checkout under a registered parent is its own discovered project (`docs/goals/foreman-harness/corpus/02-nested-checkout.md`). A discovered project's `id` is a slug of its folder name, `registered` false. A `project:<id>` label overrides the resolution (a registered id carries the registered project; an unknown one is reported as itself). The reserved label keys are the Labels table in `docs/goals/LOOP.md`; `SessionLabels` names them as constants. Roots are stored as `realpath`s because the kernel reports a cwd as `/private/var/…`. The resolution runs only when the cwd or the file changed (`PtySession.project(forCwd:)`, from the cwd poll and from the listing row for a detached session the poll does not cover), never on the output path
- `GET /api/projects` — the identity of every project seen, `{id, name, root, registered, knowledge, pullRequestBase?}`: registered (with no session too) or discovered from a live session's cwd. `pullRequestBase` is `https://github.com/owner/repo/pull/` when the root's `origin` remote is on GitHub (`PullRequestBase.parse` reads `git@github.com:owner/repo.git`, `ssh://git@github.com/owner/repo.git` and `https://github.com/owner/repo(.git)`; `RemoteOrigins` runs `git remote get-url origin` once per root every five minutes on its own queue, never on the loop) and absent for any other remote or none, so the fleet view prints the PR as plain text. No counts: the fleet view counts what it shows from the session rows, and a count here was a second merge per session per poll and an archive walk per tick that nobody read. Read-only, any grade. **`?project=<id>`** filters `GET /api/sessions` and `GET /api/archives` (an unknown id is an empty list, a malformed one a 400, like `?label=`); each archive row carries its `project` from its labels, then its archived cwd. The MCP tool `list_projects` proxies the route and `list_sessions` takes `project`
- `GET /api/projects/<id>/knowledge` — `{ok, project, goals}`: one summary per goal folder of the project's knowledge package (`docs/goals/`, or the registered `knowledge` directory), each parsed by `KnowledgeSummary` with no Markdown renderer: `slug` (the folder name; the `# STATE:` heading does not decide it), `goal` (the first `# ` heading of `goal.md` less `Goal:`), `status`, `round` and `budget` (the `- Status:` and `- Round: N of M` lines), `lastFloor`, `nextAction` (the first paragraph of `## Next action`, 512 bytes), `proposals` (top-level bullets under `## Proposals waiting on the human`), `lastRound` (the highest `rounds/NNN.md`), `lastRecord` (`<slug>/rounds/NNN.md`, the path the file route serves), `lastDecision` (the first line of that record's `## Decision`), and the goal's cost as the sum of every record's `- Cost:` line (`LOOP.md`'s shape, `KnowledgeSummary.costLine`, the parser `kitterm goal cost` reads with too): `costUSD`, `inTokens` (input, cache creation and cache read) and `cacheReadTokens`, all three absent for a goal whose records carry no line, so a goal that predates the bill prints no number rather than a zero; a record over the 256 KiB cap counts no cost. `rounds` is one entry per `rounds/<N>.md`, by number: `{number, task?, started?, costUSD?, durationMs?, pr?, correction}` — the queue item after `# Round NNN:`, the `YYYY-MM-DD` the `- Started:` line begins with, the record's `- Cost:` lines summed, the `PR #N` its `- Result:` line names, and whether it carries a `## Correction` heading; absent for a goal with no record. `tasks` is the goal's task list (`KnowledgeSummary.tasks`): one `{slug, state, round?, pr?}` per slug listed under `## Queue` (`pending`), `## Failures` (`failed`) and `## Done` (`done`) of `STATE.md`, in that order, each slug once; an item is a column-0 `- `, `* ` or `N. ` line, its slugs are the backticked kebab words on its first line before the first comma (so `` `a` (1) and `b` (2), round 1, `9784fd2`.`` yields `a` and `b` and not the sha), and `round N` and `PR #N` come from that first line; prose under a heading (`None.`) names no task; a slug in two sections keeps the state a reader needs most, `failed` over `pending` over `done`, at its first position; the key is absent when `STATE.md` has none of the three headings and an empty list when a heading lists nothing. `working` is never in the file: the page joins it from a live `task:` label. A goal folder is a direct child of the knowledge directory whose name is a slug (`ProjectStore.isValidID`, the rule `kitterm goal new` enforces), that is a real directory holding a regular `STATE.md` (`KnowledgeFile.summaries`): a child with another name or without a `STATE.md` is skipped, a symlinked child or `STATE.md` is refused and skipped like every other symlink, and at most 64 folders are read, in name order. The listing opens the knowledge directory once and each goal folder once and reads `STATE.md`, `goal.md`, `rounds/`, and every `rounds/<N>.md` relative to that descriptor; `kitterm goal list` calls the same listing. The order is `active`, `waiting`, `stopped`, `done`, then any other status, then by slug (`KnowledgeSummary.isOrderedBefore`, the comparator `kitterm goal list` sorts with too); a package with no goal folder answers an empty `goals`. A missing file or line leaves its field absent; a malformed file is never an error. Answers with an `ETag` over the whole body and honours `If-None-Match` with 304, so the fleet view's poll repaints nothing for an unchanged package. 404 for a project with no knowledge directory. Read-only, any grade (see **Security**)
- `GET /api/projects/<id>/knowledge/<path>` — one file under the knowledge directory, read-only and jailed (`KnowledgeFile`): the path is relative with no `..`, `.` or empty segment (400 otherwise, an absolute path included); the read walks one descriptor from the project root with `O_NOFOLLOW` on every component, so a symlink anywhere in the chain is refused (404), the root and the knowledge directory included, and the type and the size come from `fstat` on the descriptor the bytes are read from (a FIFO is refused at once); a directory or a missing file is 404; a file over 256 KiB is 413. Every file is served as `text/plain; charset=utf-8`, `.md` included, so a link opens as a page on every browser, with `nosniff` and a `sandbox` CSP like `/api/files/content`, never the file's own type. The project must be a registered one; a discovered project answers 404 on both routes. The store lookup runs in the task off the loop; every read runs on one serial knowledge queue and hops back with `Data`; the loop never touches the disk or the store lock. Any grade (see **Security**)
- `POST /api/upgrade/takeover` — **replace this daemon process in place** (`docs/live-upgrade.md`). Loopback + full grade, no body, no `--agent-control`: the caller is the machine's own user, and the route makes this process exec the binary on disk as that user. Answers `200 {ok, pid, executable, running, installed}` before the quiesce starts, so a caller inside a pane reads it; `409` while one is in flight, `422` when the target fails `--help`, `503` from a handler with no server behind it. What follows is the exec takeover described under **Product shape**
- `POST /api/sessions` — **spawn a session from JSON**, the programmatic counterpart of opening a tab. Body `{cwd?, profile?, labels?, name?, note?, cols?, rows?, input?}`; `input` is typed into the shell at its first read after any profile command, so `{"input":"claude\n"}` spawns an agent in one call. Answers 201 `{ok,id,name,wsPath,pagePath}` — **paths, not URLs**, because `/api/lan` owns external-URL construction and a duplicated rule is a stale link. 400 for an unknown profile, a non-directory cwd, or a malformed name/note/label (mirrors the WS 1008 reasons); 503 at `maxConcurrentSessions`. **The one route that creates a shell from a request body**, so it sits behind `--agent-control` + full grade like the input route — strictly more powerful than typing into an existing shell. An API-spawned session is **orchestrated by construction** (no label needed): the 1h linger holds it, and it survives its shell's exit with `exited`/`exitCode`, killed via `DELETE`. It starts **detached**, so the first browser to open its link is its controller
- `GET /api/sessions/<id>` — one session's listing row (the list-row shape plus `note`), for a deep-read without fetching the whole fleet. Read-only
- `GET /api/sessions/<id>/cost` — **the bill.** What the last `cost-state` line of the session's Claude Code transcript says (`TranscriptBill`, read from the row's `agentTranscript`): `{ok, hasBill, agentSessionId, agentTranscript, bill?, reason?}`, where `bill` carries the transcript's own field names unrounded — `totalCostUSD`, `totalDuration`, `totalAPIDuration`, `totalLinesAdded`, `totalLinesRemoved`, `startTime` and `modelUsage` per model (`inputTokens`, `outputTokens`, `thinkingTokens`, `cacheReadInputTokens`, `cacheCreationInputTokens`, `costUSD`) — so a ledger's `--json` passes them through. Nothing is computed: no price table, no sum over turns. A zeroed line (`modelUsage: {}`) is a bill of zero, `hasBill: true`. A transcript whose last complete line is not a `cost-state` line (a running session, one resumed after its last end, or a file cut mid-write) answers 200 with `hasBill: false` and a `reason` (`noCostStateLine`, `lastLineIncomplete`, `lastLineTooLong`, `costStateMalformed`), never zeros. 404 with `no transcript` for a session that never ran `claude`, and `transcript not found` with the path when the recorded file does not open. **Full grade only, no `--agent-control`**: the accounting is what a watch token exists to withhold, and reading it drives nothing. The read seeks to the end of the file and takes one 64 KiB `pread` on `TranscriptBill.queue`, off the loop, whatever the size of the transcript. **A running session gets an estimate** (`TranscriptEstimate`, round 16 of `agent-dashboard`): when the last line is not a bill, the route walks the `"type":"assistant"` lines after the last `cost-state` line, sums each request's `message.usage` per `message.model` (a line whose `requestId` is the previous counted line's is the same request; a `"<synthetic>"` line is not a turn) and prices the sum with `ModelPricing`, the one price table in the daemon (Anthropic's first-party rates, a cache write by its TTL from `usage.cache_creation`, long context at the standard rate; an id the table does not know counts its tokens under `unpricedModels` and no dollars), answering `estimated: true` and `estimate {costUSD, modelUsage, inTokens, cacheReadTokens, outTokens, turns, asOf, startTime, unpricedModels}` beside `hasBill: false`; the bill wins once it lands (`estimated: false`, no `estimate`), and `estimateReason` says `noTurns` or `transcriptTooLarge` with neither. `TranscriptEstimateCache` keeps the byte offset and the running sums per path, keyed by size and mtime like `TranscriptModelCache`, so an unchanged file is one `stat` and a grown file reads only the bytes past the last complete line it summed (a shrunk file or a backwards mtime is read from zero; a line that fails to parse is skipped); a file over 512 MiB is not estimated, because its first walk would hold the serial transcript queue for minutes (57 MB walks in 0.2 s, debug build). Measured 2026-09-20 over 141 finished transcripts over $1: the estimate sits 1.25% under the bill at the median (the bill also counts requests that wrote no turn) and further under on a session that ran subagents, whose files under `<session>/subagents/` it does not read. The bill and the rollup still price nothing
- `GET /api/archives/<id>/cost` — **the bill of an archived session**, the same shape as the session route, read through the join the archive keeps (`SessionArchive.agentJoin(of:)`). This is the route a foreman reads at collect time: Claude Code writes `cost-state` when it exits and archiving is what ends the session, so the live route answers 404 once the session is archived. Archive, then read. 404 `no such archive` with no `archive.json`, `no transcript` when the archive has no join, `transcript not found` with the path when the file does not open. Full grade only, like the session route; the archive file is read on the archive queue and the transcript on `TranscriptBill.queue`
- `GET /api/usage/daily?from=YYYY-MM-DD&to=YYYY-MM-DD` — **cost and tokens per day from the rollup the daemon keeps** (`UsageRollup`, `~/.kitterm/usage-daily.json`), so history outlives Claude Code's 30-day transcript retention. `{ok, timeZone, from, to, refreshedAt, recordedSessions, days, totals, models, projects, roles, lowCache}`: one entry per day in the range, zero-filled, oldest first, each `{day, costUSD, apportionedUSD, unsplitUSD, tokens {input, output, cacheCreation, cacheRead, requests}, sessions, unbilledSessions, models[], projects[]}`, with the same shape per project inside the day and per project over the whole range (`projects`, dearest first, each with `id`, `name`, `root`, `registered`). **`models`** is the split by model, per day and over the range, dearest first: `{model, name, costUSD, apportionedUSD, inputTokens, outputTokens, cacheReadInputTokens, cacheCreationInputTokens, sessions}`, the bill's own `modelUsage` field names with the name `ModelName` derives; a session across midnight puts each day's token share of its per-model dollars and tokens on that day. Since `agent-dashboard` round 6 a record also keeps the bill's `totalAPIDuration` and `totalLinesAdded`; a day and the totals carry `apiMs` (the hours of model time) and `measuredUSD` (the dollars of the sessions that carry a duration, so a rate an hour divides only those); **`roles`** is the range split by the transcript's directory, `root` then `crew` (a cwd under `.claude/worktrees/`), each `{role, costUSD, apportionedUSD, sessions, apiMs, measuredUSD, linesAdded}`; **`lowCache`** lists the billed sessions over $5 under 95% cached, `{sessionId, project, costUSD, cacheShare}`, dearest first. A billed record without the map or the duration is read again once while its transcript is on disk.
- `GET /api/yield?from=YYYY-MM-DD&to=YYYY-MM-DD` — **what each repository's own history delivered over the range** (`RepositoryYield`, read with `git` on its own queue, cached five minutes per root and range): `{ok, from, to, projects, totals}`, one entry per project `GET /api/projects` lists, each `{id, name, root, registered, yield: {checkout, remote, branch?, mergedPullRequests?, mergedLines?, releases?}}`. A merged pull request is a first-parent commit on `origin/HEAD` (else `origin/main`, `origin/master`, `HEAD`) in the range by committer date whose subject ends in `(#N)` or starts with `Merge pull request #N`; merged lines are their insertions; a release is a tag created in the range. A root that is not a checkout has every count absent; a checkout with no remote has the two PR counts absent and its releases counted; absent means no source, never zero. `totals` sums over the projects that have counts (`counted`) and says how many roots are checkouts. Same bounds, defaults and refusals as the usage route; full grade only. A record's map is kept on its `SessionRecord.models`; a record written before the rollup kept the map (before 2026-09-18) loads as it is, is read once more while its transcript is on disk so its split fills in, and once the transcript is gone its dollars are in the total and in no model's row, reported as `unsplitUSD` so `models` plus `unsplitUSD` equals `costUSD`. Both bounds are inclusive days in the rollup's own zone — the daemon's local zone, named in `timeZone` — `to` defaults to today and `from` to 29 days before; a range over 400 days, a bound that is not a day, or `from` after `to` is a 400. **Dollars are the transcript's own `totalCostUSD`, apportioned, not priced**: a session inside one day puts its whole bill on that day, exact; a session across midnight is split by each day's share of its tokens (all four kinds weighted equally), and `apportionedUSD` is the part of `costUSD` that came from such a split, so a page can say which numbers are a measurement. `unbilledSessions` had turns that day and no `cost-state` line yet: tokens in, dollars not. Tokens come from every assistant line's `usage`, each `requestId` counted once (one request is written as several lines with the same usage), subagent files under `<session>/subagents/` counted for the parent. The rollup refreshes on start and every five minutes on its own queue, never the loop: one listing per project directory, one `stat` per file, and a full read only of a transcript whose size or mtime changed (2.5 s for 779 transcripts and 382 MB cold, 19 ms unchanged, debug build). **It never loses a day**: a record whose transcript is gone is never visited, so it stays, in the zone it was read in. **Full grade only**, like the cost routes; 503 in a handler built without a rollup. The fleet view reads it for the range its toggles chose and prints the range's total, one bar a day, and each workspace's and project's dollars and cache-read share on its heading (see **Fleet view**)
- `POST /api/usage/limits`, `GET /api/usage/limits` — **the quota, captured from the statusline.** No file on disk carries it: Claude Code hands its statusline script a `rate_limits` object on stdin at every render (`five_hour`, `seven_day`, `spend_limit`, each `{used_percentage, resets_at}` in epoch seconds; present only for a Pro or Max subscriber, only after a session's first API response, each dropped once its own reset passes), and nothing else ever sees it. `POST` takes that object alone, as `jq -c '.rate_limits'` prints it (`UsageLimits.parse`: lowercase word keys, at most 8, a finite non-negative `used_percentage`, `resets_at` a positive whole number of seconds; an empty object is a reading with no window and is kept as one; over 4 KiB is 413), keeps the newest whichever session posted it, and answers `{ok, receivedAt, windows}`. `GET` answers `{ok, hasReading, receivedAt?, ageSeconds?, stale?, rateLimits?}` with the object as posted under `rateLimits`, `stale` past `UsageLimits.staleAfterSeconds` (3600); a daemon never given one answers `hasReading: false`, a plain answer and not an error. The reading lives in `~/.kitterm/usage-limits.json` (`UsageLimitsStore`, version 1, `0600`), rewritten when the windows change or a minute passed, so a restart keeps its true age. **Full grade only on both**, like the cost routes: the quota is the account's accounting, and a watch page hides the block. `kitterm statusline install` is what posts: it writes `<claude dir>/kitterm-statusline.sh`, points `settings.json`'s `statusLine.command` at it with every other key kept, and the wrapper posts in the background (`curl -m 2`, every descriptor closed, only when the object changed or a minute passed, skipped with no port file, no `jq` or no `rate_limits`) then hands the same stdin to the command that was configured before, on a marked line a reinstall reads back; the human's own script is never opened. The page draws one bar per window with its reset as a clock time in the viewer's zone (`resets today 20:20`; see **Fleet view**), prints how old the reading is, keeps a stale reading's bars muted, keeps a window past its reset at its last value with `reset · read 1d 19h ago`, and says in words when no reading has arrived
- `PATCH /api/sessions/<id>` — update `{name?, note?, labels?}`; an empty name clears it. **Full grade, no `--agent-control`**: metadata does not drive a shell, the same reason approval decisions sit outside the flag. A rename rides the long-dormant `title` opcode to every attached client, so open tabs follow without a reload; a client with its own custom name keeps it
- `GET /api/events?since=<seq>&timeout=<s>&session=<id>` — the **daemon-wide control-plane feed**, so a foreman watching a dozen crew sessions holds one parked request instead of polling `/api/sessions` for each. Returns events newer than `since` at once, or parks until one arrives (the `/commands/<n>/wait` shape — one loop, no streaming machinery), answering `{ok, epoch, events, next, pruned}`. `next` is the cursor to poll with; `pruned` is true when `since` aged out of the bounded ring (the `/commands` 410-honesty, without killing the poll loop). `epoch` is the daemon process's random id (`EventLog.epoch`): a restart resets `seq`, so a cursor alone cannot tell "quiet" from "the daemon I was watching is gone". A caller sends `epoch=` back; a mismatch — or a `since` past this daemon's head, which this daemon never issued — answers at once with `pruned` true and the new epoch's events from its first, `daemon.started` (`{epoch, version, pid}`, plus `takeover: "true"` when a live upgrade continued the epoch, plus **the previous run's summary** when the state directory held a record: `previous` (`clean` for a `kitterm stop` or `restart`, `takeover` for a live upgrade, `unrecorded` for a run that never wrote an ending — the kill this feed exists to explain), `previousPid`, `previousAliveAt`, `previousSessions`, and `previousEndedAt` when there was an ending. The keys are `previous`-prefixed because `pid` on this event means *this* run's pid, and `previous` is absent exactly when there was no record, so a first start needs no special value. `server.log` carries the same fact as one line. Appended by `DaemonServer.init` from what `LastRunStore.beginRun` handed `runDaemon` (`LastRun`, `PreviousRun`)). Read-only, any grade — observing what agents do. v1 event types: `daemon.started`, `session.created/renamed/exited/lingered/removed`, `agent.status`, `approval.pending/resolved`, `note`. `session.created` carries `respawnOf` when a pane's reattach found its id gone and got a fresh shell. The feed is `NIOLock`-guarded, not loop-confined: `SessionRegistry` (an actor) appends lifecycle events from off-loop, and completing a parked promise cross-thread is NIO-safe. Read-or-park happens under one lock (`EventLog.poll`), so an off-loop append cannot land between "nothing yet" and "park me" and strand the request; a `since` past the head answers at once as pruned, with the true `next`, rather than parking to its deadline. Waiters are capped daemon-wide (503 past the ceiling) and the timeout ceiling is `eventWaitMaxSeconds` (60), lower than a command wait's, because this route is any-grade
- `POST /api/sessions/<id>/events` — a crew agent posts a structured `note` ("plan ready for review") that lands on the feed and wakes a parked poll. The counterpart to `/api/hooks`: an agent *reporting*, not driving, so full grade **without** `--agent-control`. `{message, data?}`, message capped
- `GET /api/sessions/<id>/marks` — the raw shell-integration marks
- `GET /api/sessions/<id>/commands` — commands paired from marks: `{index, command, exit, startOffset, endOffset, running, startedAt, endedAt, durationMs, outputUrl}`. **`index` is stable for the session's life**: the mark window is bounded, so `SessionMarkStore` counts what it drops and numbering continues from there rather than restarting at 1 — otherwise a caller's saved index or `outputUrl` would silently come to mean a different command. An index below the window answers **410**, not 404. The timings and URL exist so a **caller can hang its own span on a command** — kitterm emits no traces, because whoever called it owns the trace context. Pass `traceparent:<w3c>` as a label and it comes back on every row, which is all the correlation a tracer needs
- `GET /api/sessions/<id>/commands/<n>/wait?timeout=<s>` — hold the response until command `n` finishes (default 30s, max 300). With the input route this is the `execute()` contract every agent framework wants: write a command, block for its exit code, read its output. Read-only, so no `--agent-control`. A timeout is **not** an error — `{running:true,timedOut:true}`, ask again. Waiting on a command that has not started yet is legitimate. Waiters are capped per session (503 past it). An index that has aged out of the mark window answers 410 rather than blocking until its deadline
- A command submitted through `POST /input` **names the command it creates** when the shell does not. Only kitterm's snippet and VS Code emit the command line (OSC 633;E); a shell carrying iTerm2's or Powerlevel10k's OSC 133 marks every command but never says what it was, so without this `command` is null on most real setups. A command line the shell *does* report always wins
- A **labelled session outlives its shell**: when the shell exits, the session is kept for the linger window with `exited: true` and `exitCode`, so `/commands` and retained output still answer — a node that crashed is the one worth inspecting. It cannot be attached to, and reading it does not reap it. An unlabelled (browser) session still goes immediately. A shell that dies mid-command closes that command with the shell's own exit code, so a waiter gets an answer instead of a command that runs forever
- `GET /api/files?path=<dir>&session=<id>` — list a directory so a caller can pick a file **or folder** and insert its **real path**, with no copy. This is the counterpart to dropping: a browser never reveals where a dragged file came from, so a drop must copy and the agent then edits the copy — browsing has no such problem, and a folder is just a path. Relative paths resolve against the session's cwd, `~` expands. Full grade only, which is the whole access story: anyone who can reach it can already type `ls`, and the daemon runs as the user, so the OS decides what is readable. Watch-only callers cannot type, so they cannot browse. No allowlist by design
- `POST /api/sessions/<id>/files` — save a file dropped into a session and answer with the path it landed at (`~/.kitterm/drops/<session>/<name>`, 0600, removed when the session is reaped). A coding agent takes context as paths, not uploads, so the browser drops a file and inserts the path at the cursor — never with a newline, so nothing runs on its own. Full grade, no `--agent-control`: that flag stops a *program* driving your shell, and this is a person dropping a file into their own browser. The name arrives in `X-Kitterm-Filename` and is rewritten (leaf component only, `[A-Za-z0-9._-]`), so the bytes can only land in kitterm's own directory. 16 MiB cap, buffered before writing
- `DELETE /api/sessions/<id>` — end a session and its shell now, the counterpart to the long detach window a labelled session gets. Destructive, so `--agent-control` + full grade, like the input route
- `POST /api/sessions/<id>/archive` — **cold storage.** Persist a finished session's evidence (metadata, commands, marks, output) to `~/.kitterm/archive/<id>/{archive.json, output.log}`, then end it. Destructive, so `--agent-control` + full grade like `DELETE`. Honest scope: it preserves what a session *did*, never what it *was* — there is no process to restore. "Resume" is a foreman convention (spawn a new session with the archived name/cwd and a `resumed-from:<id>` label), not a restore. `archive.json` is versioned (`"version": 1`), the same on-disk discipline as the live-upgrade handoff. It records `output: {base, pruned, bytes}` — byte 0 of `output.log` is absolute stream offset `base`, and the commands' offsets are absolute, so a reader slices `[start - base, end - base)`; the live rows' `outputUrl` is stripped because that route 404s once the session is gone. It also records `agentSessionId` and `agentTranscript` from the session's hooks when it has them — the path, not the file: Claude Code owns the transcript and may delete it, and the archive does not copy it. Archiving emits a `session.archived` event. The metadata is serialized on the loop and only `Data` crosses onto the archive queue (the `SessionLogStore` off-loop pattern)
- `GET /api/archives`, `GET /api/archives/<id>` (+ `/output`), `DELETE /api/archives/<id>` — list, read, and remove archived sessions. Reads are any grade; every archive file read runs on the archive queue and hops back with `Data`, never on the loop (a 64 MiB `output.log` must not freeze every pane). The delete is `--agent-control` + full grade
- `GET /api/sessions/<id>/output?tail=<bytes>` — the last `tail` bytes of the session's **whole** output ring (`application/octet-stream`, default 64 KiB, capped at `apiCommandOutputMaxBytes`), with `X-Kitterm-Cols` / `-Rows` (the PTY's size, also on the session row as `cols`/`rows`) and `X-Kitterm-Start` / `-Head` (absolute offsets, so a caller knows the tail begins at a cut). This is what a renderer *outside* the daemon needs to show what a TUI pane holds: a Claude Code pane is one long-running command whose raw bytes are spinner redraws and cursor moves, so `commands/<n>/output` cannot answer "what does the screen say". Ring only, read under the lock on the loop, any grade — the same exposure as the command-output route
- `GET /api/sessions/<id>/commands/<n>/output` — raw output bytes of command `n` (`application/octet-stream`), capped at `apiCommandOutputMaxBytes` (256KiB, tail on overflow); `X-Kitterm-Total-Bytes` / `-Truncated` / `-Pruned` headers. Precise for substantial output; tiny single-frame commands collapse to an empty range (see `SessionCommands`)
- `POST /api/hooks` — **an agent asks a human, and reports on itself.** A Claude Code `"type": "http"` hook posts its event here; for **`PermissionRequest`** the daemon **holds the response** until someone allows or denies, and the verdict travels back in the body (`hookSpecificOutput.decision {behavior, message}` — verified against Claude Code 2.1.260: allow ran the tool, deny refused it with the message shown) — status codes cannot block a tool call, only the JSON can. `PermissionRequest` is the held event because it fires **only when Claude would show a dialog**: an auto-allowed tool never reaches the daemon, so nothing that would not have waited on a human is slowed. **`PreToolUse` is never held** — it fires for every tool call, allowed or not, and holding it made an always-on hook unusable (every Read and Edit in a pane waited up to five minutes; An hit this within the hour of installing it). It records the `working` edge and answers `{}` at once. This is the one route that answers slowly on purpose, reusing the `commands/<n>/wait` machinery so nothing blocks the loop. **No decision is an answer**: on expiry it replies `{}`, and the agent shows its own dialog in its pane, so an unattended agent is never wedged. The pane is named by `X-Kitterm-Session`, which the hook config reads from `KITTERM_SESSION_ID` — exported into every shell the daemon spawns, because a hook config is static and cannot look it up. `Notification` records `needs-input` (carrying its `message`), `Stop` records `completed`, both answer `{}` at once — that is the `agent`/`mergedState` a `/api/sessions` row then shows. Unknown events answer `{}` and record nothing. **`PermissionRequest` never fires in `claude -p`** (print mode refuses instead of asking), so a non-interactive crew needs `--dangerously-skip-permissions` or `--allowedTools`, not a hold. **Full grade only**: the URL is always loopback, so this refuses nothing real, but an ungated route would let a watch caller spoof `completed`. The status lives on the `PtySession`. `kitterm hooks` prints the config (`PermissionRequest`, `PreToolUse`, `Notification`, `Stop`)
- `GET /api/approvals` / `POST /api/approvals/<id>` — what is waiting, and the answer (`{"decision":"allow"|"deny","reason":"…"}`). **Deciding is full grade only**: a watch token exists precisely to withhold it, and this gates on grade rather than `--agent-control` because that flag is about a *program* driving a shell while this is a person answering a question. 404 covers unknown, already-answered and expired alike — from outside they all mean "too late"
- `POST` / `DELETE /api/push/subscriptions` — the browser's Web Push subscription (`PushSubscription.toJSON()`: `{endpoint, keys: {p256dh, auth}}`) stored in `~/.kitterm/push.json` at `0600`, one per endpoint; `DELETE` takes `{endpoint}`. A repeat post is `200` where the first was `201`, because the page posts on every load. **Full grade only, without `--agent-control`**: a person registers their own phone, and a watch token exists to withhold the answer, so it does not get the question either. `PushNotifier` then sends one message per subscription on a transition into `needs-input`, `needs-approval` or `failed`, and forgets an endpoint the push service answers `404` or `410` for
- `GET /api/push/vapid` — `{ok, publicKey}`, the daemon's VAPID public key, base64url, the `applicationServerKey` the fleet view subscribes with. Full grade only, like the subscription routes: the key is not a secret, but a watch client cannot subscribe, so it gets nothing to try
- `POST /api/sessions/<id>/input` — **write.** The request body is raw bytes typed into the shell (include your own `\n`; send `\x03` for Ctrl-C). Capped at `maxInputBytes` (64KiB → 413). Returns `{ok,bytes}`; 404 unknown session, 409 closed. **`?enter=1` presses Enter after the body as whoever reads the terminal expects it** (`PtySession.typeLine`): a shell gets the body and a `\n` in one write, exactly as before, so a submitted command is still named; a program that took the foreground gets the body in **paced pieces** (`PtySession.inputPieceBytes`: 512 bytes every 50ms, with or without `?enter=1`), a 100ms settle, then a `\r`. Claude Code's prompt keeps a bare `\n` as text and never submits, and a chunk of text with `\r` on the end is one paste whose Enter is swallowed (measured: a separate key 20ms later submits, the same write or 0ms never does). The pieces exist because Claude Code **drops every full read but the last** of a paste that arrives as fast as it reads: the kernel meters one write into 1022-byte reads on Darwin and NIO waits for the reader between them, which loses nothing (8 KiB reaches a slow raw reader whole), yet 2.1.260 kept 59 of 2104 bytes from one write and 2.1.263 kept 16 of 8193. Pieces with a pause between them arrive whole on both (measured: 512 B/100ms, 1022 B/20ms, 2044 B/100ms at 8 KiB); 512 B/50ms sits inside that with a margin, and a 64 KiB body takes about 6.5s. Time paces the pieces because nothing on the master says the reader took them: `FIONREAD`/`TIOCOUTQ` on the master count the other direction, the slave is closed after spawn, and Darwin's kqueue write filter reports zero room. A **cooked-mode** reader is a different limit, and the route refuses it (ADR 0003): a tty in canonical mode keeps `PtySession.canonicalLineBytes` of a line with no newline (1024 on Darwin, `MAX_INPUT`; 4095 on Linux) and drops the rest — measured: Darwin kept 1024 of a 2051-byte line, rang a bell per byte after and never completed the line; Linux kept 4095 of 5005 — so a body over that typed at a `sleep`, a program still starting, or a shell in a here-doc arrives cut and the daemon cannot see it. A body over the cap answers **409** `{"ok":false,"reason":"cooked","error":…,"foregroundProgram":<name or null>,"limit":<cap>,"bytes":<count>}` while `PtySession.inputIsCanonical` is true (`tcgetattr` on the master, which reports the slave's mode on both platforms), with or without `?enter=1`; **`?force=1`** types it anyway. A short body into a cooked reader still types. Who reads is `foregroundIsShell`: `tcgetpgrp` on the master, then the group leader's `argv[0]` name against the spawned shell and a known-shell set — by name, not pid, because `exec claude` keeps the shell's pid and macOS's `/bin/sh` re-executes as `bash`; no group yet or an error reads as the shell. The body may be empty with `?enter=1` (Enter alone answers a dialog). The MCP `send_input` tool uses this route for `enter:true`, its default, and folds a trailing `\n` in the text into it. That tool also takes **`keys`** — `up`, `down`, `left`, `right`, `enter`, `escape`, `ctrl-c` — and builds the body from `MCPTools.keyBytes`, because a control byte does not survive a JSON string argument on every MCP client: one that strips control characters turns the Down arrow into the two bytes `[B` (measured 2026-09-09), and one that leaves the byte raw makes the JSON-RPC line unparseable, so `MCPBridge.run` skips the whole call. A name survives both, which is what lets a foreman answer a folder-trust dialog with the toolset alone. `enter` in `keys` is this route's `?enter=1`, never a byte the bridge picked, so it can only be the last key **Off unless the daemon was started with `--agent-control`** (else 403). Input interleaves with any attached controller — no separate role

## Security

- Bind `127.0.0.1` by default; `--lan` is the only path that widens it (0.0.0.0 + token auth)
- Enforce Host / Origin against loopback hostnames
- No multi-user model — shells run as the invoking user
- **Two listeners, never one.** The plain listener stays on loopback (unchanged pipeline, no TLS handler, so local latency and `kitterm open`'s `kitterm.localhost` URL are exactly as before — and `*.localhost` is already a browser secure context). `--tls-cert/--tls-key` add an encrypted listener on `port+1` (`--tls-port` to override) bound to `0.0.0.0`; when TLS is on, `--lan` no longer widens the plain one, so enabling TLS *removes* plaintext from the network. kitterm never generates certificates — self-signed trains click-through and iOS refuses `wss://` to an untrusted cert; `tailscale cert` is the documented source
- **`--trusted-host NAME`** (repeatable) = the public names the daemon answers to behind a proxy or overlay. **A request naming one is treated as remote and must present a token even though the proxy connects from loopback** — otherwise the proxy would leak loopback's unauthenticated full access. Verified live against `tailscale serve`: it forwards the original `Host` (so the rule fires — a tokenless request through the proxy gets 403, not local trust) and sends `X-Forwarded-Proto: https` (so the cookie gets `Secure`); the WebSocket upgrade survives the proxy. `AccessPolicy.isLoopback` always uses the real socket peer. `X-Forwarded-For` is deliberately not parsed (nothing needs client IPs; parsing it only adds a spoofing class); `X-Forwarded-Proto` is honoured *only* to decide the cookie's `Secure` flag, only from loopback peers, never for an access decision
- **`/api/lan`** reports the external base URL (TLS name+port, else proxy name, else LAN IP), which is what the ⧉ and 👁 share buttons build links from — the client needs no scheme logic (`defaultWsUrl()` derives `wss://` from `location.protocol`)
- The `/api/sessions/<id>/…` GET routes expose command lines and command **output** behind the same single access policy — anyone the policy admits can read what ran, and what it printed, in any session
- `--agent-control` adds the one write route (`POST …/input`). It sits behind the same access policy, so on a loopback daemon any local process can then drive any shell as your user; on `--lan` the token still gates it. Default off — enabling it is an explicit trust decision
- `/api/profiles` exposes profile commands behind the same policy; profile *names* from URLs are only ever looked up in the user-authored file, never executed directly
- The knowledge routes (`GET /api/projects/<id>/knowledge` and `/knowledge/<path>`) answer at **watch grade**, decided 2026-09-08 in the `projects-and-knowledge` goal: a project's `docs/goals/` is the same information class as a session's cwd and as `GET /api/projects`, both of which a watch token already reads, and the fleet view a watch link opens must show the goal and the next action. The jail is what keeps the grade honest: nothing outside the knowledge directory is reachable through them, a symlink is refused rather than followed, and the daemon never writes there. Registered projects only: a project a session's cwd discovered answers 404, so a watch token cannot read the package of every repository a pane visits

## State

Everything lives under `~/.kitterm/`, default port **3418**. The tree, in the
order `DaemonPaths.swift` declares it, is in `docs/architecture.md` under
"State on disk"; edit it there when `DaemonPaths.swift` changes.

## Distribution

Releases ship a universal macOS tarball and statically linked Linux tarballs
(`amd64`, `arm64`); `scripts/install.sh` unpacks the macOS one into a prefix
(default `~/.local`). The Linux ones use the same layout and are meant to be
extracted straight into `/usr/local`:

```
<prefix>/bin/kitterm              # sh wrapper, execs the real binary
<prefix>/lib/kitterm/kitterm      # argv[0] lands here, so the helper is a sibling
<prefix>/lib/kitterm/kitterm-spawn-helper
<prefix>/share/kitterm/web/       # prebuilt UI (StaticFileServer's installed root)
```

The wrapper exists so `SpawnHelperPath.resolve()` finds the helper beside argv[0].
**This layout is encoded in five places** — `scripts/build-release.sh`,
`scripts/build-release-linux.sh`, `scripts/install.sh`,
`StaticFileServer.candidateRoots()`, and `SpawnHelperPath` — so changing it means
changing all five, and nothing fails at build time if you don't.

Linux binaries must be linked with `--static-swift-stdlib`, **including the C spawn
helper**: SwiftPM links it against `libswiftCore` regardless, and a helper that needs
the Swift runtime execs fine and then kills every session on a host without a
toolchain. `build-release-linux.sh` refuses to package a build that still links it.

## Service

`kitterm service install|uninstall|status` writes a LaunchAgent
(`~/Library/LaunchAgents/com.kitterm.daemon.plist`, `KeepAlive=true`) pointing at
`<prefix>/bin/kitterm`.

While the agent is loaded **it owns the daemon**: `stop` boots it out before
signalling (a bare SIGTERM would just be undone by KeepAlive), `restart` kickstarts
it, `start` refuses, and reconfiguring means re-running `service install`. Any code
that stops or replaces the daemon must account for the agent — including
`scripts/install.sh`, which boots out before the file swap and re-bootstraps after.
`upgrade --live` needs the agent for the opposite reason: the daemon execs the new
build under the same job, and KeepAlive is what restarts it clean if that build
crashes after the exec.

## Control plane (`docs/goals/`)

- The repository is the control plane for agent work on it. `docs/goals/` holds the
  small package from "Building autonomous goal loops that deliver" (jx0.ca). Two files
  belong to the project: `LOOP.md` (procedure, authority tiers, budget, stop rules,
  round record shape) and `facts.md` (what a later round must not rediscover). Each
  goal is one folder, `docs/goals/<slug>/`: `goal.md` (objective, exclusions,
  completion condition), `plan.md` (the floor and the capability order), `STATE.md`,
  `corpus/` (approved requests), `rounds/` (one record per round). A goal's status is
  the `- Status:` line of its `STATE.md` (`active`, `waiting`, `stopped`, `done`); a
  goal never moves and there is no done folder
- Two files, two scopes. `facts.md` holds repository facts by topic, one dated bullet
  per fact with its source; only a repository fact goes there, and a goal-local finding
  stays in its round record; the human prunes it at every direction check. `STATE.md`
  is short: the `- Status:`, `- Round: N of M`, `- Rounds total:`, `- Last floor:`, and
  `- Updated:` lines, then `## Queue`, `## Failures`, `## Proposals waiting on the
  human`, `## Done`, and `## Next action`, and nothing else; narrative goes to
  `rounds/`. The card parses the status, round, and next-action lines, so their shape
  is an interface (`KnowledgeSummary`)
- Read `LOOP.md`, `facts.md`, and the goal's `goal.md` and `STATE.md` before you start a
  task here. Append a fact you had to measure to `facts.md`. Respect the authority tiers
  in `LOOP.md`: `goal.md`, `corpus/`, and an existing check are frozen inside a round;
  `plan.md`, `LOOP.md`, and `docs/adr/` take a proposal in the round record, not an edit
- The daemon reads this package for the fleet view and never writes it. The foreman
  (`docs/foreman.md`) writes each goal's `STATE.md` and `rounds/NNN.md`, appends to
  `facts.md`, and commits the package on the goal's branch after every round
- `examples/goals/` holds the generic templates: `LOOP.md` and `facts.md` for the
  project, and `examples/goals/goal/` for one goal folder. `kitterm project init <path>`
  writes the two project files into a project's knowledge directory and registers the
  project; `kitterm goal new <path> <slug>` writes `docs/goals/<slug>/` from the goal
  template with the slug in `STATE.md` and refuses an existing folder; `kitterm goal
  list <path>` prints each goal folder's slug and status; `kitterm goal cost <path>
  [<slug>] [--json]` prints the ledger (`GoalLedger.swift`): per goal and per round,
  dollars, tokens, the cache-read share of input, wall-clock, tests added (the first
  `N new` of the record's `## Floor`), files changed (`git diff --name-only
  <base>..<result>` from the record's `Base:` and `Result:` shas, a dash when either
  does not resolve), the decision and the PR the `Result:` line names, with totals per
  goal and a footer for the rounds that predate the bill. For every session a record's
  `Sessions:` line names, the archive's transcript (`agentTranscript`, under
  `KITTERM_STATE_DIR`) wins when `TranscriptBill` reads a bill from it, else the
  record's `Cost:` line at the same position is parsed; `--json` prints one object per
  round with the transcript's field names unrounded and `null` where a line-sourced
  round cannot know a number. `GoalsTemplates.swift` embeds
  both sets and `GoalsTemplatesTests` pins the two equal, so edit the file under
  `examples/goals/` and paste it into the constant. `GoalsLayoutDocsTests` greps
  `AGENTS.md`, `docs/foreman.md`, `docs/architecture.md`, `examples/`, and the embedded
  strings for a path of the old flat layout (a goal file or `rounds/` directly under
  `docs/goals/`, or a `done/` folder) and fails on any

## Coding standards

- Single responsibility per file/type
- KISS: simplest working solution first
- Do not copy large copyrighted chunks from ttyd / localterm
- Keep JSON off the I/O stream (health/API only)
- **No regressions in performance, core features, or UX** — verify with `swift test`
  and `KittermBench` before calling work done; nothing that adds blocking I/O to the
  event loop or per-connection path

## Commands

```bash
swift build
swift test
cd Web/terminal && pnpm install && pnpm build
swift run kitterm start|stop|status|restart
swift run kitterm statusline install         # wrap the Claude Code statusline so it posts the quota
swift run KittermBench                     # against a running daemon
./scripts/build-release.sh v0.1.0          # universal macOS tarball → dist/
./scripts/build-release-linux.sh v0.1.0    # static Linux tarball → dist/ (run on Linux)
```

On Linux only `swift build` works. `swift test` does not: several test files use
`Bundle(for:)`, which corelibs-XCTest lacks. `KittermBench` is excluded from Linux
builds entirely — it needs `URLSessionWebSocketTask`, absent from
corelibs-foundation — but it can measure a Linux daemon remotely with `--port`.
Check Linux with:

```bash
docker run --rm -v "$PWD":/src -w /src swift:6.1 \
  bash -c 'git config --global --add safe.directory "*"; swift build'
```

`KittermBench`'s `TUI-redraw` scenario is flaky — it intermittently reads ~193 B
instead of ~159 KB when it races shell startup. Re-run before treating it as a
regression.
