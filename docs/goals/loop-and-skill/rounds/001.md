# Round 001: loop-holds-the-contract

- Goal: loop-and-skill
- Started: 2026-09-23T12:08+07:00  Ended: 2026-09-23T12:40+07:00
- Sessions: F2933CC6-6E3B-4E7E-B768-6DD61A8FE17F (claude-fable-5-1)   Archives: F2933CC6-6E3B-4E7E-B768-6DD61A8FE17F
- Base: a04d207 (main)   Result: 48f712b, dc3f26f on `loop-and-skill/round-1`
- Cost: $7.25 · 8078k in (98% cached) · 40k out · 0h 14m

## Prompt

Capability 1: `LOOP.md` keeps tiers 1 and 2 and sheds tiers 3 and 4; a
new `## Parsed shapes` section holds every shape the product reads,
each with its reader. One correction, which approved the crew's plan
and answered two questions.

## Floor

before: green (a04d207: swift test 859, KittermCLITests 119)
after: green (KittermCLITests 117, 1 expected failure, exit 0; swift
test 860; Linux docker pipe 56 s)

## Effects

- behaviour: `docs/goals/LOOP.md` 367 → 326 lines, the template 369 →
  325. `## Parsed shapes` holds the `STATE.md` block, the task item
  rules, the whole round record block and the labels, each naming its
  reader. `One round`, its gap classes, `### The model`, `Reports`
  and its two subsections left for the skill, which already carries
  them verbatim. `Round record` and `Labels` were absorbed.
- visible: an audit script over 231 old sentences found 29 absent from
  the new file and the skill together, every one a restatement of the
  same step. `loopRules` keeps ten pins with the same words; two
  sentences moved to a new `skillRules` list checked against the skill
  file and `ForemanSkills.foremanLoop`. `project init` then `goal new`
  on a scratch directory wrote a `LOOP.md` byte-equal to the template.
- persistent: `docs/goals/LOOP.md`, `examples/goals/LOOP.md`,
  `Sources/KittermCLI/GoalsTemplates.swift` (`loopHistory` gained the
  old hash), `Tests/KittermCLITests/GoalsLayoutDocsTests.swift`.

## The crew corrected the foreman twice, in its plan note

1. `## Floor`, `## Decision` and `## Correction` **are** parsed —
   `kitterm goal cost` and `GoalLedger` read the first two,
   `KnowledgeSummary.roundRecord` the third. The prompt said the record
   headings are parsed by nothing. The whole record block therefore
   stays in `LOOP.md` as tier 1, and the file is larger than the
   foreman predicted.
2. "Commit after every round" is not in `LOOP.md` at all; it lives only
   in the skill. It is a tier 2 rule in the wrong file, and moving it
   would add a rule, which `goal.md` excludes. Left for round 2.

## Gap

class: steering
evidence: the foreman's correction asked for two edits — the intro's
line 3, and the two cross-references to the now-absent `One round` and
`Reports` — and the crew deferred both to the human rather than making
them. The correction did not say they were part of this round's
deliverable. They move to round 2's prompt.

## Decision

done.

## Reflection

The plan note paid for itself on its first use: two premises in the
foreman's prompt were false, and the crew found both by reading the
parsers before writing. A prompt that asserts what the code does must
say so as a claim to check, not as a constraint.
