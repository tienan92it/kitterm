# Round 017: quota-resets-at-a-time

- Goal: agent-dashboard
- Started: 2026-09-20T16:12+07:00  Ended: 2026-09-20T16:30+07:00
- Sessions: 4E1C8203-F38D-425E-BE81-60CD41E0C84D   Archives: 4E1C8203-F38D-425E-BE81-60CD41E0C84D
- Base: 7d9c098 (agent-dashboard/round-10, PR #134)   Result: fbe03ce on the same branch
- Cost: $5.80 · 7950k in (98% cached) · 26k out · 0h 10m

## Prompt

The human's: the QUOTA reset cell prints a local clock time, not a
countdown: `resets today 20:20`, `resets tomorrow 04:00`, `resets Sep
25, 04:00`. The Pen frames were changed first.

## Floor

before: green (7d9c098: vitest 1448 in 58 files)
after: green (tsc clean; vite build to scratch; vitest 1449 in 58
files; the two quota files also pass under UTC, Los Angeles, Auckland
and Kolkata)

## Effects

- behaviour: `quotaResetTime` in `sessions-model.ts`; days count
  between local midnights, rounded, so DST does not shift a day. The
  six cases: same day `resets today 20:20`; next day `resets tomorrow
  01:00`; four days `resets Sep 25, 04:00`; across the year `resets Jan
  2 2027, 04:00`; 23:55 → 00:05 `resets tomorrow 00:05`; a past window
  keeps round 14's `reset · read 4m ago`. The band's countdown stays.
- visible: `round17-1200.png` in the crew's scratchpad;
  `sessions-quota.test.ts` and `sessions-value-page.test.ts` pin
  `Date.now` so the day words hold in every zone.
- persistent: `Web/terminal/src/sessions-model.ts`, `sessions.css`,
  two test files, `AGENTS.md`, `docs/architecture.md`.

## Chartered replacements

The `resets Nh Nm` and `resets Nd` strings in the two files, each
named in the note with its new string.

## Gap

class: none
evidence: the six strings match the request; the floor is green in
four zones.

## Decision

done.

## Reflection

Nothing cost time.
