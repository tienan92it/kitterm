# Round 004: the-terminal-surface

- Goal: fleet-catch-up
- Started: 2026-09-15T17:22+07:00  Ended: 2026-09-15T17:45+07:00
- Sessions: 5EACE121-345F-46B9-9EDA-82FF131D060D   Archives: the same
- Base: 7a86e34 (goals/catch-up-first)   Result: 10309d0 on goals/the-terminal-surface
- Cost: $10.89 · 11066k in (98% cached) · 81k out · 0h 20m

## Prompt

Capability 4 from `plan.md`, sent as one `send_input` of 5347 bytes,
with the inventory's CSS worklist and round 3's leftover, the card head.
It asked for two decisions: the gutter mark, with how its state reaches a
reader who cannot see colour, and what distinguishes working from idle
without the dot's glow.

## Floor

before: green (vitest 1328 in 34 files)

after: green. The foreman re-ran the web floor on `10309d0`: tsc clean,
**1214 tests in 35 files, 0 failures**. The count falls by 119 because
seven colour pairs stopped existing when their tints and fills left the
CSS, across 17 themes, and rises by 5 for `sessions-css.test.ts`. The
ratchet shrank by those seven pairs' 23 entries, from 84 to 61, and
found no new pair. The foreman grepped `sessions.css` for the four
banned properties: one match each, and each is the comment that states
the rule.

## Effects

- **The surface is the terminal's.** The page is set in `--font-mono`.
  Cards are hairline sections. Buttons print in brackets, `[new]`;
  toggles as `[ ]` and `[x]`; links are underlined. No pill, no shadow, no
  tint behind text, no transition, and the dot's glow is gone with the
  dot.
- **Four gutter glyphs, one per meaning, not one glyph in three
  colours.** `>` working, `?` waits on a person, `!` failed, `-` quiet.
  The glyph carries the state without colour; the item's own state word
  sits beside it and the `sr-only` announce line gives the count, so a
  screen reader and a colour-blind reader get the same state. The mark
  is a masked shape rather than text, because accent and danger as text
  fail 4.5:1 on three to six themes and the ratchet may not grow; the old
  dot had the same standing.
- **The dot is gone.** A working row shows `>` in accent and an idle row
  `-` in success, so shape and colour both differ, and the state word
  says which. A working row's "since" reads `now` from round 1.
- **The card head is one line**: name, count if any, `[new]` at the right;
  the root path is gone. With a profile, the select sits left of `[new]`
  because the two are one action. The tally clips before the name does,
  which the crew fixed in its second commit after checking with a stub
  that had a profile.
- **`AGENTS.md`'s fleet-view paragraph is rewritten** for the shape the
  four rounds produced.
- visible: eight images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/fcu-round4/`, before and after
  on `github-dark` and `solarized-dark` at both widths. **The foreman read
  the `github-dark` 390 px after-image.** Height at 390 px: **1336**, from
  1678, from 2820 when the goal started. At 1200 px: 1175, from 2304.
  Rows and heading lines are 44 px; the phone menu opens with its right
  edge at 366 of 390.
- persistent: five files.

## What the crew saw and left, on record

The mark is a CSS mask, so a browser without mask support shows an empty
gutter; the state word still carries the state. The search box keeps its
surface fill, because its placeholder on the page background is an
unlisted pair that fails on four themes. The ratchet's tint group is now
empty, and the `Blocker` type keeps it so a tint that returns is named.

## Gap

class: none

evidence: every visual claim is a measured height or a grep count; the
ratchet shrank rather than grew; the two accessibility decisions each
name how the state reaches a reader who cannot see colour.

## Decision

done. Capability 4 is complete on `goals/the-terminal-surface` at
`10309d0`, and with it the goal's five completion conditions.

## Reflection

Four rounds, $35.79, and the phone page went from 2820 px to 1336 px
while gaining nothing. The `Cost:` lines are on all four records now,
back-filled from the archives' transcripts once the rule existed. Round
4 was the dearest at $10.89 and it touched the most: it read every rule
in a 400-line stylesheet and re-derived the contrast pairs.

The brief had two halves and the plan split them into three rounds and
one. Reading the four images in order, the third round is where the
page became easy to catch up on, and the fourth is where it stopped
looking like a web app. Both were needed and they were different work.
