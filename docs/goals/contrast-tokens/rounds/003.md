# Round 003: open-the-elevation

- Goal: contrast-tokens
- Started: 2026-09-11T00:05+07:00  Ended: 2026-09-11T00:35+07:00
- Sessions: E542E9EA-F2E0-45C2-9668-BFC4A1DD25F5   Archives: the same
- Base: 465ae54 (goals/derive-the-pairs-work)   Result: fae03f8 on goals/open-the-elevation

## Prompt

Capability 3 as the human's ruling rewrote it, sent as one `send_input`
of 6118 bytes: lower the lift on the four surface tokens and raise the
`.pane-close` opacity so the 178 combinations the muted tokens cannot
reach clear 4.5:1. The prompt named the cost the human had not yet seen,
asked for the surface separation before and after, and said that a page
whose cards have no edge is a worse page even with passing numbers.

## Floor

before: green (vitest 1335 in 32 files)

after: green. The foreman re-ran the web floor on `fae03f8`: tsc clean,
**1318 tests in 32 files, 0 failures**. The count falls by 17 because one
whole pair stopped existing, on all 17 themes. `swift test`, the bench
and the Linux build were skipped; the diff holds no Swift.

## Effects

- **One rule, seventeen themes.** `--ui-surface`, `--ui-surface-2`,
  `--ui-hover` and `--ui-active` drop from 7, 12, 17 and 23 percent of
  `--ui-lift` to 3, 6, 9 and 12. `.pane-close` stops fading to 0.55 on
  touch. The crew defends 3/6/9/12 as a measured knee rather than a
  taste: one point more wins back 10 entries, and one point less wins 3
  while killing the fill step.
- **134 entries left, from 178.** 44 dropped, and one whole pair is gone:
  `--ui-text-muted` on `--ui-bg` under the 55% veil, on all 17 themes. No
  ratio fell and no passing pair started failing.
- **The visible cost, measured by pixel and not by eye.** The card fill
  on `github-dark` goes from (26,30,36) to (18,23,29); the page
  background does not move. `--ui-surface` against `--ui-bg` falls from
  1.207 to 1.073 on `one-dark`, 1.205 to 1.072 on `dracula`, 1.202 to
  1.071 on `gruvbox-dark`. A card no longer reads as raised on its own.
  `--ui-border` does not move, so `.card` and `.settings-panel` keep the
  edge they had, at 1.51 on `github-dark` and 1.65 on `solarized-dark`.
  `.count`, `.tag` and `.card-head` had no border and now take an inset
  `--ui-border` shadow, with no layout change.
- visible: eight images at 1200 px under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/ct-round3/`, as `before-` and
  `after-` for `sessions` and `pane` on `github-dark` and
  `solarized-dark`. **The foreman read the `github-dark` session pair.**
  The cards sit closer to the page and still read as cards, because the
  border carries them.
- persistent: four files, all under `Web/terminal/src/`.

## The lever is spent, and it was never enough

This is the round's most useful result, and it corrects the reasoning
that led to the ruling. **Opening the elevation cannot empty
`KNOWN_BELOW`.** The crew measured the limit in both directions: with all
four tokens set to `--ui-bg` itself, 136 combinations still fail, which
is more than the 134 its chosen rule leaves. Sinking them below `--ui-bg`
bottoms out at 115.

The remaining 134, by what blocks them:

| count | blocker |
|---|---|
| 33 | the four surface tokens; the theme's own colours already fail on `--ui-bg` |
| 32 | under `--ui-accent-soft` |
| 24 | under the three opacities this round did not open |
| 19 | on `--ui-bg-sunken` |
| 12 | on `--ui-bg` |
| 11 | on the status tints |
| 3 | on `--ui-border` used as a fill |

## Gap

class: steering

evidence: the foreman recommended this lever to the human as "the only
route that can empty `KNOWN_BELOW`", and it is not a route that can. The
recommendation rested on round 2's reasoning that elevation was the
cause, which is true for the pairs round 2 could see, and on no
measurement of what elevation alone would leave. The crew ran that
measurement in its first minutes. A recommendation that names a lever
should carry the number the lever reaches before the human spends a
budget on it.

## Decision

done. Capability 3 is complete on `goals/open-the-elevation` at
`fae03f8`.

propose (the human rules again, and the round names the cheapest next
step: **opening the other three opacities** clears all 24 of that group
with the same one-line change `.pane-close` just took, on
`.settings-gear` at 0.35 and 0.65 and `.keyboard-toggle` at 0.75.
`--ui-accent-soft` is the next largest at 32. Neither empties the table
either, so the human should also decide whether `KNOWN_BELOW` is ever
meant to reach zero, which is what completion condition 3 requires
today.)

## Reflection

Two rounds in a row have now ended by proving that their own lever is
insufficient, and both proved it the same way: by setting the token to
its extreme and counting what still fails. That test costs minutes and it
is the one that decides whether a plan is worth running. It belongs in
the prompt for any round whose job is to move a value until a number
passes.

The crew also reported something outside its work that needed a person:
`~/.kitterm/pid` held a dead pid again, for the second time tonight, so
`kitterm stop` could not have found the live daemon. The foreman repaired
it. That is the right instinct, and the underlying defect is now in
`facts.md` twice over: `kitterm serve` writes its pid file before it
binds.
