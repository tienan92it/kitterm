# Round 002: the-name-never-clips

- Goal: steady-suite
- Started: 2026-09-16T11:20+07:00  Ended: 2026-09-16T12:02+07:00
- Sessions: 08A08A49-697C-4E7D-8A45-A1F45228755D   Archives: the same
- Base: 16ec2e1 (goals/two-small-fixes)   Result: 8f8e1b7 on goals/name-never-clips

## Prompt

Capability 2 from `plan.md`, sent as one `send_input` of 5281 bytes. It
pointed at the live screenshot showing the clip, said round 4 of
`fleet-catch-up` had tested with a narrower profile name, and asked the
crew to decide the order in which the four things on the heading give
way, and to say so plainly with a measurement if one line could not hold
them.

## Floor

before: green (vitest 1214 in 35 files)

after: green. The foreman re-ran the web floor on `8f8e1b7`: tsc clean,
**1222 tests in 36 files, 0 failures**, 8 new. Round 4's four CSS
absences still pass, 5 of 5. `KNOWN_BELOW` untouched. No Swift.

## Effects

- **The cause, measured, and it is not what the proposal assumed.** At
  390 px with Menlo the heading has 334 px of content: `kitterm` 55,
  "no live session" 108, the select 108, `[new]` 40, two gaps 20 —
  **337**. The name box carried `flex-shrink: 1` with
  `text-overflow: ellipsis`, so a **0.4 px** shortfall ate two
  characters. The widest select label is the page's own "local shell",
  not a bundled profile name; `ubash` is five characters.
- **One line cannot hold all four beside any real name**, and the crew
  said so with the number: whole, the other three leave 47 px, about six
  characters, for the name. So something must give.
- **The give-way order, and the reason that decides it.** The count drops
  first, whole or not at all, because the rows say what is live and the
  fleet line says the total. The crew's argument for putting it first is
  the one that matters: dropping the count keeps **every card's select
  the same width**, where shrinking the select first would give
  `kitterm` a short select and `nghenhan-mt5` a whole one on the same
  page. Then the select shrinks to five cells at 22+ character names,
  because its label is a hint and the accessible name and the opened
  list carry every label whole. Then the select drops at 28+, because
  `[new]` still starts a local shell. The name and `[new]` never give
  way; the name is `flex: none` with no ellipsis.
- **Every real name now reads whole.** Measured in the page at 390 px
  with "local shell" selected: `kitterm` 55, `market-data-pipeline` 157,
  `nghenhan-mt5` 94, `trading-data-api` 125, all whole, select 109,
  `[new]` 40, count hidden, every heading 44 px, scroll width 390, and
  all eight `spawn:*` focus keys present. At 1200 px every count and
  every whole select returns.
- visible: four images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/ss-round2/`. **The foreman
  read `after-stub-390.png`**: all four project names read in full with
  a profile selected, which is the completion condition.
- persistent: four files under `Web/terminal/src/`.

## On record, from the crew

A name of 36 characters or more at 390 px puts `[new]` on a second line,
which the model reports as `fits: false`; no registered project is near
that. A monospace face wider than 0.6 em cuts the count with an ellipsis
rather than dropping it. The select's minimum height is unchanged.

## Gap

class: none

evidence: the cause was measured to the pixel rather than guessed, the
give-way order carries a reason about the page as a whole rather than
about one card, and the proof is the live names at the real width.

## Decision

done. Capability 2 is complete on `goals/name-never-clips` at `8f8e1b7`.

## Reflection

Round 4 of `fleet-catch-up` tested this heading with one profile and
concluded the tally would clip first. It was right about the mechanism
and wrong about the width, by 0.4 px. That is the smallest possible
margin for a visible defect, and it shipped because the test used a
narrower name than the page's own default.

The lesson is in the prompt that fixed it: measure with the real names.
The crew read the widest label from the page rather than from the
bundled profiles, and found that the page supplies a wider one than any
profile does.
