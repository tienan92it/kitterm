# Round 002: the-page-moves-as-the-notes-say

- Goal: landing-page
- Started: 2026-09-20T16:52+07:00  Ended: 2026-09-20T17:00+07:00
- Sessions: B8305237-C359-4E0C-A9CB-05C957EDF1B7   Archives: B8305237-C359-4E0C-A9CB-05C957EDF1B7
- Base: b2e73e6   Result: d153ff6 on `landing-page/round-1`
- Cost: $2.57 · 2459k in (96% cached) · 20k out · 0h 08m

## Prompt

Capability 2: the motion the `Landing notes` frame describes — the
staggered entrance once per page, the `◐` marks turning on one 90 ms
timer, the cursor blink, all off under reduced motion — with timed
opacity samples and Lighthouse as proof.

## Floor

before: green (b2e73e6)
after: green (html-validate 0 errors; Chromium 143 at both widths, 0
console errors, 0 px overflow, every page shown after a scroll through
and still shown after scrolling back)

## Effects

- behaviour: `IntersectionObserver` at 0.4 sets `shown` once; the hero
  on load after a forced style read; `[data-enter]` children go 0 → 1
  and 8 px → 0 over 240 ms, `--i × 60 ms` apart. One `setInterval`
  turns every `◐`; the cursor blinks by a step-end keyframe. Reduced
  motion: CSS forces opacity 1, no transform, no transition, no
  animation; the script leaves `◐`. A `<noscript>` style keeps the
  page visible without the script. No hover transition, no smooth
  scroll.
- visible: the samples. Hero first child 0 → 1 by 300 ms. Page 2's
  four children at 0/100/200/400 ms: `[0,0,0,0]`, `[.50,.15,0,0]`,
  `[.91,.70,.39,.02]`, `[1,1,1,.96]`. The mark at 0/100/200/300 ms:
  `◓ ◑ ◒ ◐`. Reduced motion: every opacity 1 at 0 ms, the mark `◐` at
  every sample, the cursor's animation none. Lighthouse over a local
  static server, mobile, three runs: performance 95, accessibility
  100, best practices 100, SEO 100.
- persistent: `site/index.html` (+59 lines).

## Gap

class: none
evidence: the samples show the stagger and the turn; the scores meet
`goal.md` condition 6 at its threshold.

## Decision

done. One proposal: self-host JetBrains Mono (or preload it) — the
Google Fonts stylesheet costs about 990 ms of first paint and holds
performance at 95. `goal.md` excludes a third-party font host but
Google Fonts; a self-hosted woff2 in `site/` would satisfy it and gain
the points.

## Reflection

Nothing cost time. Eight minutes.
