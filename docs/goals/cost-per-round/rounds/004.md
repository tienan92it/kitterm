# Round 004: the-ledger

- Goal: cost-per-round
- Started: 2026-09-15T17:47+07:00  Ended: 2026-09-16T10:40+07:00
- Sessions: CA9ED286-0F78-45D1-911C-E12433793761   Archives: the same
- Base: 893d20b (goals/the-bill-in-the-record)   Result: 100a760 plus the foreman's commit
- Cost: $15.84 · 8110k in (94% cached) · 96k out · 16h 53m

## Prompt

Capability 4 from `plan.md`, sent as one `send_input` of 6169 bytes, with
round 3's sequencing fix folded in as the crew's to make: serve
`GET /api/archives/<id>/cost` and change Collect to "archive, then read"
in all five sources. It said the ledger reads the archive's transcript or
the record's `Cost:` line and nothing else, and that files come from git
because the bill's line counts read zero on heredoc edits.

**The machine slept mid-round**, at 18:13, with the crew's commit already
made and its Linux build already green. The foreman resumed it the next
morning with one direction: a transcript matched by cwd and time is fine
for the tables in the note, and must not become a code path in the
ledger, because a match is a guess and `goal.md` says nothing is
estimated.

## Floor

before: green (`swift test` 714)

after: red at the crew's commit by construction and green after the
foreman's command: 5 failures at `100a760`, all in the sentence check and
all naming `docs/goals/LOOP.md`; after the command, **726 tests, 0
failures**, 12 new. The foreman re-ran `GoalsLayoutDocsTests` after
applying it: 7 tests, 0 failures. Web: tsc clean, vite build, vitest
1328. Bench `interactive-echo` p95 2.57 ms against a 50 ms gate. **Linux
build green**, `swift:6.1`, 49 s, no warning on the new code.

## Effects

- **`kitterm goal cost <root> [<slug>] [--json]`.** One row per round,
  a totals row, and a footer counting rounds that predate the bill. Every
  number right-aligns under its header. `--json` carries the transcript's
  own field names, unrounded: `2.6361237500000003` on the wire.
- **Two sources, and which wins.** The archive's `agentTranscript` when
  it names a file that exists, read exactly through `TranscriptBill`;
  else the record's `Cost:` line, parsed. Nothing else. In this
  repository today no archive carries the path, because the live daemon
  is v0.28.0 and predates round 1, so every row reads its line.
- **The sequencing fix is in.** `GET /api/archives/<id>/cost` serves the
  same shape as the session route through the archive's path, and
  `ArchiveCostRouteTests` pins the order: after `POST` archive, the live
  route answers 404 and the archive route serves the bill. Collect now
  reads "archive the session, then read `GET /api/archives/<id>/cost`" in
  all five sources, and the sentence check retires the old wording.
- **Files from git, tests from the Floor line.** Files resolve for every
  round in this repository: 10, 9, 5 and 4, 5, 5. "N new" parses where
  the Floor section says it and prints a dash where it does not.
- **The corpus was wrong by one column and the crew was right.** The
  hand-written table in `corpus/01-what-did-that-goal-cost.md` was
  misaligned in its header and two rows. The command right-aligns, the
  test pins that, and the foreman brought the corpus into line, because
  the numbers agreed and only the spacing did not.
- visible: **the foreman ran the ledger on the real tree.** After
  back-filling the `Cost:` lines on rounds 1 and 2 from their worktree
  transcripts, which are unique per round:

  ```
  cost-per-round                      $      in  cached      out  wall tests  files   decision      PR
    001 keep-the-join              4.44   3.08M     96%    27.0k 30m00    +2     10   done           —
    002 read-the-bill              7.93   8.11M     98%    57.0k 39m00   +13      9   done           —
    003 the-bill-in-the-record     4.74   3.33M     97%    36.0k 10m00     —      5   done           —
    total                         17.11  14.52M     97%   120.0k 79m00   +15     24
  ```

  The crew's own tables from matched transcripts agree to the cent.
- persistent: 14 files from the crew, `docs/goals/LOOP.md` and the corpus
  from the foreman.

## What this round's own bill shows

`$15.84`, the dearest round of either goal, and `16h 53m` of wall-clock,
because the session sat open through a night's sleep. `totalDuration`
counts the time the session was open, not the time it worked; the API
time would say ten minutes of that. And the cache share fell to 94% from
the 97% every other round reads, because the pause outlived the cache
and the morning's turns rebuilt it. The ledger's efficiency column
showed a real cost of a real pause on the first goal it measured.

The `PR` column reads a dash on every row, because the foreman writes
`Result:` as a sha on a branch and never goes back to add the PR number
after the merge. That is the foreman's habit to change, not the
ledger's.

## Gap

class: steering

evidence: the plan chose `totalDuration` for the wall-clock, and the
first overnight round showed that it measures elapsed time and not work.
The API time is on the bill too. Whether the ledger should print it
beside the wall-clock is a question for the next budget, and the round
that raised it is the one it happened to.

## Decision

done. Capability 4 is complete on `goals/the-ledger` at `100a760` plus
the foreman's commit, and with it the goal's five completion conditions.

propose (two, for a later budget: print `totalAPIDuration` beside the
wall-clock, so an overnight round does not read as seventeen hours of
work; and have the foreman write the PR number into `Result:` after the
merge, so the `PR` column fills.)

## Reflection

The goal's whole point was to put a number beside every round, and the
first round it measured in full was its own last one, at nearly four
times the cost of its first. That is not a reason to distrust the number;
it is the number doing its job. The crew's own note flagged that the
wall-clock had swallowed the night before the foreman read the bill.

Four rounds, $32.95, and the loop can now answer what a goal cost without
anyone remembering. Beside it, `fleet-catch-up` cost $35.79 to take a
page from 2820 px to 1336. Those two numbers on one screen are the
report the human asked for.
