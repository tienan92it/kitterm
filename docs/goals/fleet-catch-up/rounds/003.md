# Round 003: catch-up-first

- Goal: fleet-catch-up
- Started: 2026-09-15T17:06+07:00  Ended: 2026-09-15T17:28+07:00
- Sessions: FC11D021-E7CA-4EF1-8C72-7C6593F27E4F   Archives: the same
- Base: 302504a (goals/each-fact-once)   Result: 96bd091 on goals/catch-up-first
- Cost: $8.53 · 7522k in (97% cached) · 56k out · 0h 15m

## Prompt

Capability 3 from `plan.md`, sent as one `send_input` of 5233 bytes. It
carried round 2's leftover, the repeated project name on a strip item,
and asked for two decisions: the order inside a project, and whether the
foreman's row stays pinned. It handed the crew round 2's stub server so
it would not rewrite it.

## Floor

before: green (vitest 1321 in 34 files)

after: green. The foreman re-ran the web floor on `96bd091`: tsc clean,
**1328 tests in 34 files, 0 failures**, 7 new. The ratchet is green; its
stale-entry case passed after two rules left `sessions.css`, and
`KNOWN_BELOW` is untouched.

## Effects

- **The order is the reader's.** Title, the strip, the notice, the
  restart line, one count line, the projects, and only then the search,
  the chips and the push switch. Inside a project: rows, one line per
  open goal, a "N done" fold with the record links, archives.
- **The corpus request passes with room.** Measured in the page at
  390 px: the title ends at 50, the two needs-input items at 155 and 248,
  the two failed items at 319 and 391, and the count line "0 working ·
  1 done · 2 idle" at 425. The first screen holds items 1 to 4 with
  419 px to spare under 844, and nothing sits above them but the title.
- **Order inside a project is by state, then recency**, and the crew's
  reason is the right one: a working row quiet for three hours is the one
  that stalled, and it must sit above an idle shell that printed a minute
  ago; recency then says which working row stalled. Recency alone would
  also reorder rows on every poll as output ticks.
- **The foreman's row is unpinned.** It is the first row of its own
  project, and the "Foreman" label is gone. The inventory ranks "is
  anything supervising" fifth of five; the pin cost 80 px above the
  projects at 390 px; the strip's "no foreman running" still answers
  when the answer is no, and when it is yes the row's note says what it
  did.
- **A template goal reads "not written yet"**, by its slug. `isUnwritten`
  matches the template's own tokens, so a real goal that happens to
  contain `<id>` is not caught. The two placeholder cards from the
  before-image, which were this bug live, are now two one-line rows.
- **A strip item prints the project only when it differs from the name**,
  and carries the row's span, because corpus items 2 and 3 ask for how
  long. The card tallies collapsed into the one count line, so a count is
  said once.
- visible: four images under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/fcu-round3/`. **The foreman
  read the 390 px after-image.** Heights at 390 px: 2196 to **1678**;
  at 1200 px: 1742 to 1394. From 2820 and 2304 when the goal started.
- persistent: five files under `Web/terminal/src/`.

## Gap

class: none

evidence: the corpus request is measured in the page rather than eyed,
the two decisions each carry a reason a later round can argue with, and
the crew named what it saw and left: the card head still prints the root
path and the "New session" button, about 80 px per card on a phone, and
`AGENTS.md` still describes the pinned row and the expanded goal block.
Both go to round 4 and the record.

## Decision

done. Capability 3 is complete on `goals/catch-up-first` at `96bd091`.

## Reflection

Three rounds have taken the phone page from 2820 px to 1678 px, and the
first screen now answers the corpus request with half the screen to
spare. None of it added anything. The plan's premise, that the page
already held what a returning reader needs and was hiding it under what
they do not, held.

The card head is the last piece of "web app" the reader scrolls past
before the terminal-looking rows, and the crew's note names it. Round 4
takes the surface, and the card head is surface.
