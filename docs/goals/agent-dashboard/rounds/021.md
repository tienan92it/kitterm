# Round 021: the-no-transcript-first-run

- Goal: agent-dashboard
- Started: 2026-09-21T18:11+07:00  Ended: 2026-09-21T18:28+07:00
- Sessions: 40F7D67B-646A-4C3B-8C06-FB6270617D24 (claude-fable-5-1)   Archives: 40F7D67B-646A-4C3B-8C06-FB6270617D24
- Base: c5ef3f5 (main at #143)   Result: 9885c28 on `agent-dashboard/round-21`
- Cost: $6.70 · 6044k in (97% cached) · 35k out · 0h 09m

## Prompt

Round 20's proposal, drawn by the foreman as `Dashboard 1200 · first
run, no transcripts` and approved by the human: when the rollup holds
no session at all, USAGE prints `$0.00` alone with a hairline chart and
one line, MODELS and LEAKS one line each, every VALUE tile a dash,
WHERE no row.

## Floor

before: green (c5ef3f5: vitest 1463)
after: green (tsc clean; vitest 1476 in 60 files, 13 added; vite build
to scratch; no Sources/ change)

## Effects

- behaviour: `noSessionInRange` (the rollup answered and
  `totals.sessions === 0`) decides the six points one level up, in
  `paint()`; the frozen `modelsPanel`, `leakLines` and `wherePanel`
  behaviours on an empty rollup are untouched. WHERE needed no code.
- visible: `no-transcript-1200.png` and `-390.png` from a daemon with
  `KITTERM_STATE_DIR`, `HOME` and `CLAUDE_CONFIG_DIR` on empty scratch
  directories, read by the foreman beside the frame;
  `sessions-no-transcript.test.ts`, 13 assertions, with round 20's
  shape and today's page pinned unchanged.
- persistent: `Web/terminal/src/sessions-value.ts`, `sessions.ts`,
  `sessions.css`, `theme-contrast-derive.ts`, one test file,
  `AGENTS.md`; the human's saved `corpus/dashboard.pen` of 18:16 with
  the two first-run frames.

## Where the crew chose

The USAGE note prints on a phone, wrapped, where the apportionment note
does not; the prompt said the same lines at 390.

## Gap

class: none
evidence: the shot matches the frame; no assertion replaced.

## Decision

done.

## Reflection

Nothing cost time. Seventeen minutes, no correction.
