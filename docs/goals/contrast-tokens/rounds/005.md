# Round 005: ratchet-known-below

- Goal: contrast-tokens
- Started: 2026-09-11T11:45+07:00  Ended: 2026-09-11T12:05+07:00
- Sessions: 499411AE-B64C-449A-8677-95048F8E40FC   Archives: the same
- Base: 4097037 (goals/ratchet-known-below)   Result: e52edbd on goals/ratchet

## Prompt

Capability 4 as the human's second ruling rewrote it, sent as one
`send_input` of 4979 bytes: make `KNOWN_BELOW` a ratchet, open the three
opacities round 3 left alone, and give every remaining entry its
blocker. The prompt told the crew to establish what `check()` already
enforced before writing anything, and said that a ratchet nobody has
watched bite is a claim rather than a mechanism.

## Floor

before: green (vitest 1333 in 32 files)

after: green. The foreman re-ran the web floor on `e52edbd`: tsc clean,
**1284 tests in 32 files, 0 failures**. This file falls from 775 to 726:
51 tests go because three pairs stopped existing across 17 themes, and 2
are added. `swift test`, the bench and the Linux build were skipped; the
diff holds no Swift.

## Effects

- **The crew measured what already worked before adding to it**, which
  the prompt asked for. Round 1's `check()` already failed an unlisted
  pair under the floor, a listed pair that reached the floor, and a
  listed pair reading lower than its entry. It planted both directions on
  the base commit first and watched them bite.
- **Three checks added, each closing a silent hole.** An entry naming a
  theme that is not bundled used to pass: `"solarized-drak"` sailed
  through 775 of 775. A recorded ratio lowered by hand used to pass:
  `gruvbox-dark` set from 2.83 to 1.00 sailed through. And every entry
  now carries a `blocker` from six groups, checked against the surface
  stack, so a new surface cannot be recorded without naming the lever
  that would clear it.
- **The three opacities are gone and the count held.** `.settings-gear`
  at 0.35 and 0.65 and `.keyboard-toggle` at 0.75, with their dead
  `opacity: 1` resets and transitions. The crew re-derived before and
  after and diffed the rows rather than trusting round 3: **24 gone, 0
  new**, so 134 falls to 110.
- **It corrected round 3's grouping.** Round 3 reported all 33 elevated
  entries as failing even at `--ui-bg`; 10 of them clear there. Each
  remaining entry now records which themes its lever actually reaches.
  Of the 110, levers reach 44 and **66 are the theme's own colours**.
- visible: **the foreman planted five faults itself and watched each
  one fail.**

  | plant | result |
  |---|---|
  | a new unlisted pair, `.card .foreman-probe` | fails, naming the ancestor, the surface and 3.25 against 4.5 |
  | `github-dark` listed under `--ui-text on --ui-bg` | fails: "now passes; drop its `KNOWN_BELOW` entry: expected 16.016" |
  | the theme id `solarized-drak` | fails, naming the id |
  | `gruvbox-dark` 2.83 rewritten to 1.00 | fails: "reads 2.3 and its entry says 1; re-record" |
  | `blocker: "bg"` changed to `"sunken"` | fails |

  The foreman's first attempt at the second plant was malformed: it put
  the key beside `themes` rather than inside it, where it decoded away
  and the suite stayed green. That is the entry shape doing its job, and
  the corrected plant failed as it should.
- persistent: two files, `styles.css` and `theme-contrast.test.ts`, plus
  the header comment rewritten for the ratchet.

## The correction to the goal

`goal.md` condition 1 named `solarized-dark` at 4.32 as the theme whose
own foreground fails on its own background. **That was the foreman's
error, written when the human ruled.** The measured theme is
`synthwave-84` at 4.31; `solarized-dark` reads 4.75 and passes. The
foreman corrected `goal.md` in this round's commit.

## Gap

class: none

evidence: five faults planted and five failures observed, by the crew and
again by the foreman. The opacity count was re-derived rather than
inherited, and round 3's grouping was corrected by measurement.

## Decision

done. Capability 4 is complete on `goals/ratchet` at `e52edbd`, plus the
foreman's commit.

## Reflection

Three rounds running, a crew has corrected a number its brief handed it:
round 2 corrected round 1's reachable count, round 3 corrected round 2's
group of ten, and round 5 corrected round 3's thirty-three and the
foreman's own 4.32. Every correction came from re-deriving rather than
reading. The fact this leaves is already in `facts.md` in its round 3
form, and this round is its third confirmation: **a count in a brief is a
claim, and the round that acts on it re-derives it.**

The cheapest lever left is `--ui-accent-soft`, worth 18 entries and one
token, then the sunken well at 13.
