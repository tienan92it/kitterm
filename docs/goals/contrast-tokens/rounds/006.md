# Round 006: raise-the-accent-soft

- Goal: contrast-tokens
- Started: 2026-09-11T12:33+07:00  Ended: 2026-09-11T12:52+07:00
- Sessions: E5AC7462-1EB8-4D17-AD84-6B54DF38FE03   Archives: the same
- Base: 19420c2 (goals/accent-soft)   Result: 7c68f19 on goals/accent-soft-work

## Prompt

Capability 5 from `plan.md`, sent as one `send_input` of 5135 bytes. It
gave the crew round 5's figure of 18 and told it to re-derive rather than
trust it, because three rounds running had corrected a number their brief
handed them. It also said that `--ui-accent` is the page's interaction
colour, and that making the soft fill stop reading as accent-coloured
would trade one defect for another.

## Floor

before: green (vitest 1284 in 32 files)

after: green. The foreman re-ran the web floor on `7c68f19`: tsc clean,
**1284 tests in 32 files, 0 failures**. `swift test`, the bench and the
Linux build were skipped; the diff holds no Swift.

## Effects

- **The plan's 18 was right and the crew still did not take it.** It
  re-derived the group: 32 entries over 6 pairs, 18 clearing with the
  tint at zero. Round 5's figure held. But every construction that
  clears all 18 leaves the fill at ΔE 0.008 or less against the page on
  `nord` and `gruvbox-dark`, which is a fill the eye cannot find. A plain
  alpha cut reaches 18 only at 1%. The crew quoted the brief's own
  sentence back and declined.
- **It found a second lever instead.** Darkening the accent toward black
  before the alpha is not the same as thinning it: mixing toward
  `--ui-bg` equals a smaller alpha, while mixing toward black leaves the
  composite darker. The token becomes
  `color-mix(in srgb, color-mix(in srgb, var(--ui-accent) 65%, black) 14%, transparent)`.
- **13 cleared, 0 new, 0 worse, and no pair vanished.** `KNOWN_BELOW`
  falls from 110 to 97. The criterion is round 3's own knee: the tint
  must read at least as strongly as the card's surface step on every
  theme, and 13 is the ceiling under it. 16 would need ΔE 0.013 on
  `gruvbox-dark` and 18 would need 0.008.
- **The accent still reads as an accent.** The focus ring and
  `.launch:hover` paint `--ui-accent` itself, so they do not move: 9.73
  and 3.48 against `--ui-bg`, and 8.79 and 2.99 for the hover border
  against its fill, on `github-dark` and `gruvbox-dark`. Accent text on
  the soft fill **improves**, from 6.88 to 8.45 and from 2.83 to 3.24.
- **The five entries that look reachable and are not.** Of the 19 left in
  the group, 14 fail on the bare surface and 5 clear bare by 0.13 to
  0.47: `nord` accent on bg at 4.63, `solarized-dark` text on bg at 4.74,
  `one-dark` muted on bg at 4.82, `tokyo-night-storm` faint on surface at
  4.84, `ayu-mirage` faint on surface at 4.97. No visible tint fits in
  that margin. Each entry now says which case it is.
- visible: four images at 1200 px under
  `/Users/antran/.claude/jobs/b7a30d73/tmp/ct-round6/`, each frame
  holding the focus ring, the hover border and the chip fill at once.
  **The foreman read the `gruvbox-dark` after-image**: on the accent's
  worst theme the ring, the hover border and the chip are all still
  plainly visible. The crew's own pixel diff shows only the chip box
  changed; ring and border are byte-identical.
- persistent: two files, `tokens.css` and `theme-contrast.test.ts`. The
  scratch probe the crew used to search the parameter space is gone; the
  foreman checked both the index and the disk.

## Gap

class: none

evidence: the crew re-derived the group before changing anything,
measured the fill in Oklab against the page on every theme, confirmed the
painted result in Chromium to the pixel, and checked the ratchet in both
directions on its own work: a dropped entry planted back fails with "now
passes; drop", and a kept entry at its old ratio fails with "reads 4.17
and its entry says 3.46; re-record".

## Decision

done. Capability 5 is complete on `goals/accent-soft-work` at `7c68f19`.

## Reflection

The brief handed this round a number and told it to re-derive it. It did,
the number was right, and the round still declined to hit it, because
reaching it would have cost something the brief had named in the same
breath. That is the distinction worth keeping: **a target that is
measured is not the same as a target that is worth hitting**, and the
round that can tell the difference is the one that read why the target
was set.

Levers now reach 31 of the 97 that remain. 66 are a theme's own colours
and the ratchet records them as permanent. The next cheapest is the
sunken well at 13.
