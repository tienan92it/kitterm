# Round 003: the-font-is-first-party

- Goal: landing-page
- Started: 2026-09-25T11:16+07:00  Ended: 2026-09-25T11:27+07:00
- Sessions: 35133FDC-868E-422B-8EA7-2FEE19C71444 (claude-sonnet-5)   Archives: 35133FDC-868E-422B-8EA7-2FEE19C71444
- Base: dfdffbc   Result: 5194017 on `landing-page/round-3`, PR #158
- Cost: $2.33 · 7651k in (98% cached) · 20k out · 0h 08m

## Prompt

Serve JetBrains Mono 400 and 600 from `site/` with `@font-face` and
`font-display: swap`, drop the Google Fonts links, repaint the
favicon's old palette with the dashboard tokens, and add one check that
fails on a third-party font reference or a missing font file.

## Floor

before: green (dfdffbc: html-validate exit 0)
after: green (html-validate exit 0, run by the foreman; headless
Chromium at 1200 and 390 with 0 console errors and no horizontal
scroll, run by the crew; `node site/check-fonts.mjs` passes, run by the
foreman)

## Effects

- behaviour: `site/index.html` loads `fonts/jetbrains-mono-latin-400.woff2`
  (21212 B, preloaded) and `-600.woff2` (21980 B) through two
  `@font-face` rules and makes no request to `fonts.googleapis.com` or
  `fonts.gstatic.com`. The favicon paints `#0e1d25`, `#264653` and
  `#2a9d8f` in place of `#161b22`, `#30363d` and `#3fb950`; the accent
  on the surface is 5.17:1. The files are Google's static Latin-subset
  instances, one per weight, with JetBrains Mono's upstream `OFL.txt`
  beside them.
- visible: Lighthouse, mobile preset, Playwright's Chromium 1234 against
  a local static server of `site/`, three runs each: performance
  95/95/96 before, 100/100/100 after; accessibility, best practices and
  SEO 100 in all six. FCP 1681 ms to 903 ms, LCP 2636 ms to 1203 ms. The
  crew measured these; the foreman did not rerun Lighthouse.
- persistent: `site/index.html`, `site/fonts/` (two woff2 files and
  `OFL.txt`), `site/check-fonts.mjs` (node, no dependencies; the crew
  verified it fails four ways on the file at `dfdffbc`).

## Gap

class: none
evidence: the floor is green, the proof column holds, and every path is
under `site/`, which the Authority table does not tier; `goal.md` scopes
the goal to `site/`.

## Decision

done

## Reflection

The glyphs `◐`, `█` and `↗` are outside the Latin subset, as they were
outside every subset Google served, so they fall back to the system mono
as before. `check-fonts.mjs` sits in `site/`, so Cloudflare Pages serves
it as a public file; it is harmless, but a later round can move it
beside the other scripts. The crew posted its plan note five minutes in,
after it had read the files, which is when the prompt asks for it.
