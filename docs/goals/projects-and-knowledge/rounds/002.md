# Round 002: dashboard

- Goal: projects-and-knowledge
- Started: 2026-09-08T15:57+07:00  Ended: 2026-09-08T16:25+07:00
- Sessions: 57E0D673-625D-48D6-B543-9AD385435F87   Archives: 57E0D673-625D-48D6-B543-9AD385435F87
- Base: 6583a9d (goals/project-identity)   Result: c6b8f0f, a7b2085, 9691e57, b7bd5a6 on goals/dashboard

## Prompt

Capability 2 from `plan.md`, sent as one `send_input` of 4659 bytes: read
the package, corpus request `01-phone-dashboard`, and `rounds/001.md`;
branch `goals/dashboard`; the nine pieces (pure model with tests, three
render layers with kill, archive, and spawn, filter chips and search, the
pinned foreman row, the 390 px layout, the settings-panel link, the poll,
the design pass, the corpus verification with screenshots); the authority
tiers; the floor; commit, do not push; post one note under 1900 bytes.

## Floor

before: green (`swift test` 535 tests 0 failures, vitest 472, bench p95
2.53 ms)
after: green (`swift test` 535 tests 0 failures, vitest 504 passed in 30
files, bench p95 2.52 ms; the foreman re-ran `swift test` and vitest with
the same counts)

## Effects

- behaviour: the crew ran the design skill, wrote
  `Web/terminal/src/sessions-model.ts` with 32 tests, rebuilt
  `sessions.ts` and `sessions.css`, added the settings-panel link, and
  described the page in `AGENTS.md`. Four commits, 17 minutes.
- visible: `/Users/antran/.claude/jobs/b7a30d73/tmp/round2/sessions-1200.png`
  and `sessions-390.png`. The strip lists the approval, then the failed
  pane. The foreman row is pinned. Chips: state, project, crew, made by.
  One card per project with counts, a spawn button, rows with rename,
  archive, kill; a `crew: demo` rule inside the kitterm card. At 390 px
  the row actions collapse into one menu and `scrollWidth` equals
  `innerWidth`.
- persistent: seven files changed, all Free tier
  (`git diff --name-only goals/project-identity`). Corpus request 01
  verified on a scratch daemon on port 3477: allow answered the held hook,
  archive wrote `archive.json` and moved the row under the card, kill
  removed the row from the page and the API, spawn made a session in the
  project root.

## Gap

class: domain
evidence: a registered root that is a parent of a git checkout swallows
that checkout's sessions, because a registered prefix beats the git walk
(capability 1's rule). Registering `/Users/antran/Workspace` for the
corpus fixture hid the kitterm sessions until kitterm was registered too.
The page is right; the resolution rule needs a decision.

## Decision

done. The item `dashboard` is complete on `goals/dashboard` at
`b7bd5a6`, not pushed. The session is archived.

propose (`plan.md` capability 1, the resolution rule: decide whether a
registered root yields to a nested `.git`. The foreman recommends yes:
nearest `.git` first, then the registered prefix, so a registered parent
folder groups only the checkouts that have no project of their own. One
row in the resolution table test changes.)

## Reflection

The corpus fixture wanted a card for a folder with no `.git`, which forced
a registration and exposed the rule. A corpus request that names its
fixture in full finds a rule gap the unit table missed. The rule fact is
in `facts.md`. The crew also ranked a Playwright script for corpus 01 as a
regression check so round 5 does not re-verify by hand; that is a candidate
for the `scaffold-and-docs` round or a later one.
