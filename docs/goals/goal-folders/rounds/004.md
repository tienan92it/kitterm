# Round 004: skill-and-docs

- Goal: goal-folders
- Started: 2026-09-09T15:10+07:00  Ended: 2026-09-09T15:40+07:00
- Sessions: 8676E622-C3A5-4615-8CAD-7BA8F9765CDC   Archives: 8676E622-C3A5-4615-8CAD-7BA8F9765CDC
- Base: 33c2089 (goals/goal-folders)   Result: 8576926, 6033deb, 3baa209, 5eef132 on the same branch

## Prompt

Capability 4 from `plan.md` with the streamlining the human chose on
2026-09-09 (two scopes, `facts.md` by topic with a prune step, the short
`STATE.md`), sent as one `send_input` of 5638 bytes: read `LOOP.md`'s
two-scope paragraph and directions, the streamlined `facts.md` and both
`STATE.md` files as the model, this goal's files, rounds 001 to 003, the
previous goal's rounds 004 and 008, the skills, the templates, and the
docs; stay on the branch; the six pieces (the skill scans every goal
folder, runs only `active`, records under the folder, handles `done` and
`new goal`, commits after every round, posts each digest as a note,
appends only repository facts, sends an arrow key through the input
route; the two other skills' record path; the templates derived from
this repository's files; the docs and AGENTS.md with a check that fails
on a stale path; `skills install` carries the new skills; a hand check).

## Floor

before: green (`swift test` 605 tests 0 failures, vitest 625, bench p95
2.86 ms)
after: green (`swift test` 609 tests 0 failures, vitest 625, bench p95
2.63 ms; the foreman re-ran `swift test` and vitest with the same counts)

## Effects

- behaviour: the crew rewrote `foreman-loop.md` (sections Rules, The
  package, Read before you type, Scan, Schedule, One round, Monitor,
  Reports, Direction, Stop rules, Round record, Labels), updated the two
  other skills' record path, derived the three templates, rewrote the
  layout parts of `docs/foreman.md` and AGENTS.md, and added
  `GoalsLayoutDocsTests`. Four commits, 30 minutes, no correction.
- visible: `kitterm skills install --dir <tmp>` writes three files
  byte-equal to `examples/foreman/`; `kitterm goal new demo` writes the
  short `STATE.md` with `## Done` and no narrative section; the facts
  template carries the topic headings and the prune rule.
- persistent: twelve files changed. Tests: `GoalsLayoutDocsTests` (4): no
  `docs/goals/STATE.md`, `goal.md`, `rounds/`, or `done/` path in the
  docs, the examples, or the embedded strings (checked to fail on a
  planted line); the skill names goal files by folder, `post_note`, the
  commit rule, and the input route; the state template is the short
  shape. Pinned strings changed: the three embedded skills and the three
  embedded templates, which the new text replaces by design. The foreman
  grepped the docs and examples for the four stale paths: none.

## Gap

class: none
evidence: every completion condition of `goal.md` now has its piece:
the CLI (round 1), the route (round 2), the card (round 3), the skill
and the docs (this round), and corpus request 04 on this package.

## Decision

done. Capability 4 is complete on `goals/goal-folders` at `5eef132`. The
session is archived. The goal's completion conditions 1 to 5 hold on the
branch; the goal is done pending the review gate, the push, and the merge.

## Reflection

The streamlining cost the foreman one commit and the crew one template
pass, because the repository's own package was the model the crew
copied. A convention that lives in the repository first and in the
template second stays consistent by construction: the golden tests pin
the template to the example, and `GoalsLayoutDocsTests` pins the docs to
the layout. The proposal from the dogfood that the skill commit after
every round and post each digest as a note is now in the skill.
