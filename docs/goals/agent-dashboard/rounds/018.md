# Round 018: a-window-is-never-dropped

- Goal: agent-dashboard (resumed for one round; a chore on its surface)
- Started: 2026-09-21T17:10+07:00  Ended: 2026-09-21T17:35+07:00
- Sessions: F64B8927-EAA6-4CD5-A830-630CF765002B (claude-fable-5-1)   Archives: F64B8927-EAA6-4CD5-A830-630CF765002B
- Base: 9a271c8 (main at v0.31.1)   Result: 2166959 on `agent-dashboard/round-18`
- Cost: none recorded (no bill yet)

## Prompt

The human saw the QUOTA Session row vanish, label and bar, then return
after the foreman opened the page. The foreman read the cause: the
statusline posts Claude Code's `rate_limits` as it is, that object does
not always carry `five_hour`, the daemon replaced the reading whole,
and the page drew only the windows the latest post carried. The prompt
asked for a per-window merge with a time per window.

## Floor

before: green (9a271c8: swift test 842, vitest 1449)
after: green (swift test 847; Linux docker build 61 s; tsc clean; vite
build to scratch; vitest 1452 in 58 files)

## Effects

- behaviour: `UsageLimits.merging`: a post replaces each window it
  carries at the post's time and keeps the rest at their last value and
  time; the file is version 2 with a time per window and a version-1
  file loads at the file's time; `GET /api/usage/limits` serves each
  window with its own `receivedAt` and `ageSeconds`, the top-level ones
  the newest post's. `quotaPanel` prints `resets today 21:40 · read 12m
  ago` when a window is over 60 s older than the newest post; the
  past-reset form uses the window's own age.
- visible: `UsageLimitsRouteTests` +6, `sessions-quota.test.ts` +3; the
  crew's screenshot after a both-windows post and a `seven_day`-only
  post shows the Session row standing with its age.
- persistent: `Sources/KittermDaemon/UsageLimits.swift`,
  `HTTPAPIHandler.swift`, one Swift test file, `sessions-model.ts`,
  `sessions-quota.test.ts`, `AGENTS.md`, `docs/architecture.md`.

## Chartered replacements

`testTheNewestPostReplacesTheLast`, which pinned whole-reading
replacement. Named in the note.

## Gap

class: none
evidence: the floor is green on both platforms; the tests pin the
partial post; the diff is Free.

## Decision

done.

## Reflection

The crew posted its plan note in four minutes and its done note in
seventeen; no blocker. The three-notes rule cost nothing and would have
caught a wrong charter early.
