# Round 020: the-first-run-state

- Goal: agent-dashboard
- Started: 2026-09-21T17:53+07:00  Ended: 2026-09-21T18:05+07:00
- Sessions: 111D3026-7414-4E29-A0DA-4733B3F580FA (claude-fable-5-1)   Archives: 111D3026-7414-4E29-A0DA-4733B3F580FA
- Base: 5b915ae (main at v0.31.2)   Result: 64491e2 on `agent-dashboard/round-20`
- Cost: $6.41 · 7356k in (98% cached) · 30k out · 0h 10m

## Prompt

The human asked what a new user sees; the foreman ran a daemon on an
empty state directory and found four gaps: zero counts in state
colours, VALUE tiles of dashes with a caveat, WHERE a single amber row
in foreman vocabulary, SESSIONS pointing at a control the page no
longer has. The foreman drew `Dashboard 1200 · first run` in Pen; the
human approved it.

## Floor

before: green (5b915ae: vitest 1452)
after: green (tsc clean; vitest 1463 in 59 files, 11 added; vite build
to scratch; no Sources/ change)

## Effects

- behaviour: a zero count in the band wears the fact colour and no
  link; with no project registered, VALUE prints dashes and `Register a
  repository to count what it shipped:  kitterm project add <path>`,
  WHERE prints the remainder row and `Spend groups by project once one
  is registered; goals and tasks follow docs/goals/:  kitterm project
  init <path>`; an empty tree prints `No live sessions. Open a shell at
  / to start one.` with `/` a link. USAGE, QUOTA and MODELS are
  unchanged.
- visible: `first-run-1200.png` from a scratch daemon on an empty state
  directory, read by the foreman beside the frame;
  `sessions-first-run.test.ts`, 11 assertions, including one registered
  project proving nothing else changed.
- persistent: `Web/terminal/src/sessions-model.ts`, `sessions-value.ts`,
  `sessions.ts`, `sessions.css`, `theme-contrast-derive.ts`, two test
  files, `AGENTS.md`.

## Chartered replacements

None; every existing assertion held.

## Gap

class: none
evidence: the shot matches the frame; the floor is green.

## Decision

done.

## Reflection

A user with no transcript at all still sees USAGE at $0 and an empty
MODELS panel; the frame does not draw that case and this round did
not change it. A proposal, below.
