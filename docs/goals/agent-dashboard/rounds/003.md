# Round 003: a-task-is-the-fourth-level

- Goal: agent-dashboard
- Started: 2026-09-18  Ended: 2026-09-18
- Sessions: 54DE753D-5FAF-46AB-925A-1933F2127D53
- Base: 86c2da6
- Cost: $10.99 · 13363k in (98% cached) · 61k out · 0h 18m
- Result: `goals/task-level`, PR #127

## Prompt

Capability 3 of `plan.md`. The page renders three levels; add the
fourth, which is the unit a crew actually runs. Parse it in Swift from
`STATE.md`'s `## Queue`, `## Done` and `## Failures`; join a live
session's `task:` label in the model; render it under its goal at the
indent the foundation's Hierarchy section gives.

## Floor

before: green (86c2da6: swift test 780, vitest 1264 in 41 files)
after: green (Linux `swift:6.1` build complete in 64.5 s, run first;
swift test 788, 0 failures; tsc clean; vite build ok; vitest 1276 in 42
files; bench interactive-echo p95 2.58 ms)

## Effects

- behaviour: `KnowledgeSummary` gains `tasks: [Task]?` — slug, state,
  round, PR — and `GET /api/projects/<id>/knowledge` serves it. The page
  paints one line per task under each open goal, with the crew's own row
  nested under the task it runs.
- **The parser reads prose without being fooled by it.** An item is a
  column-0 bullet or numbered line, cut at the first comma, and every
  backticked kebab token in it is a slug. That yields two tasks from
  `green-ci`'s "`a` (1) and `b` (2)", and it excludes a sha, a
  `rounds/002.md` path, and a continuation line's `sessions.css`.
- The crew settled the two questions the prompt named:
  - **A slug in two sections resolves to one task**, and **failed beats
    pending beats done**, because failed needs a person, pending says
    the loop will run it again, and done needs nothing. `working` — a
    live `task:` label under the same `goal:` — overrides all three in
    the web model, because it carries a session the reader can open.
  - **No heading at all yields `nil`, not `[]`**, and the goal renders
    exactly as it did. Eleven of the thirteen goals on this machine have
    an empty `## Queue`, so the distinction is the common case, not the
    edge.
- visible: two scratch daemons, 3971 holding a snapshot of the binary
  and `dist` from before the change and 3972 the new build, each with
  its own `KITTERM_STATE_DIR` and this worktree registered, so the
  **real** `docs/goals/` tree loaded. A spawned session carried
  `crew:agent-dashboard goal:agent-dashboard round:3
  task:a-task-is-the-fourth-level`. 3418 was never touched.

  The page drew seven task lines under this goal, the crew row nested
  under the working one, and `[done] round 2 PR #126` on the two that
  are finished.

  | | before | after |
  |---|---:|---:|
  | 390 px | 968 | 1276 |
  | 1200 px | 650 | 846 |

  `scrollWidth` is 390 at 390 px and no line overflows. Every capture
  was checked against `getBoundingClientRect` tops, which `facts.md`
  requires.
- persistent: `Sources/KittermDaemon/KnowledgeSummary.swift`,
  `Tests/KittermDaemonTests/KnowledgeTaskTests.swift` (new, 8),
  `Web/terminal/src/sessions-model.ts`, `sessions.ts`, `sessions.css`,
  `sessions-tasks.test.ts` (new, 10), `sessions-css.test.ts` (+2 `it`s,
  none removed), `AGENTS.md`.

## What this round leaves to capability 5, said plainly

- The working mark is the **static** accent glyph. The character cycle is
  capability 5.
- The done mark is the existing dash, not the foundation's `✓`, for the
  same reason: the marks are still masked SVGs.
- A long done slug truncates with an ellipsis at 390 px, because the
  facts are `flex: none`. The foundation's line drops facts from the
  right instead, and that is capability 5's rule.
- **The 390 px page is 1276, which is 76 px above the completion
  condition's 1200.** Capability 5 is the one that takes it back. The
  crew named this rather than leaving it to be found.

## Gap

class: none

## Decision

done

## Reflection

The item added +308 px at 390 px, seven lines at the 44 px `--line-h`
the foundation sets, and the crew reported that it had pushed the page
over the goal's own completion line rather than hoping nobody measured.
That is the behaviour the loop wants from a round that is one of six.

The parser is the durable part. It reads a section written for a human
and does not trip on the prose around it, and the eight tests name every
shape thirteen goals actually use — including this repository's own
`agent-dashboard/STATE.md`, which is the fixture that will change under
it every round.
